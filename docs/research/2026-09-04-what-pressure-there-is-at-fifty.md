# What pressure there is at fifty — 2026-09-04

**IN PROGRESS. This file is a work-in-progress checkpoint and every section
below is a placeholder until it carries a reading.** It exists at this commit
because `tests/foundation/documentation-links-contract.test.ts` fails on the
instrument's own `Findings:` citation while it does not, and because a note
that exists is a note a container restart cannot destroy.

The question, the ruling that commissioned it and the instrument are in
`tests/browser/playtest-2026-09-04-what-pressure-there-is-at-fifty.playtest.ts`.
The four-prisoner precedent is the note named
`2026-09-04-what-pressure-there-is.md` in this directory on branch
`measure/what-pressure-there-is` (PR #971). It is named without a rooted path
deliberately: that branch is unmerged, so a rooted citation of it dangles here
and `tests/foundation/documentation-links-contract.test.ts` fails on it --
which it did, and this sentence is the fix.

## Readings so far

None. No act has completed on this instrument.
