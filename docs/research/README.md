# Research records

Dated evidence gathered to answer a specific open decision, kept because the
decision cites it.

These are **not** architecture decisions and they are not documentation of how
the code works. An ADR records what was decided; a file here records what was
known at the time it was decided, including what could not be established. They
are read-only history: when the code moves on, a record here does not become
wrong, it becomes older. Do not update one to match current `main` — write a new
one and let the ADR cite that instead.

## What makes a record trustworthy here

Each report labels every factual claim with how it was obtained, and the tiers
are not decoration:

- **VERIFIED** — a source was opened and read. In this repository's network
  environment that mostly means a shipped game data file or decompilation
  mirrored on `raw.githubusercontent.com`, which is reachable, and quoted
  verbatim.
- **SEARCH-SUMMARY** — a real page exists and a search tool summarised it, but
  the page itself was never opened. Weaker than VERIFIED and deliberately kept
  as its own tier rather than folded into it.
- **FROM MEMORY** — believed, not checked. Useful, and honest about being the
  weakest kind of claim.
- **UNKNOWN** — could not be established.

The tiers exist because of a specific failure. An earlier research round
produced roughly two hundred citations, and spot-checking twelve of the specific
factual claims found: two refuted outright (a keybinding attributed to a base
game that belongs to a mod, and one that does not exist at all), a starting
balance wrong by more than an order of magnitude, a currency range that silently
depended on a DLC nobody mentioned, and **a design opinion attributed by name to
a real developer that could not be sourced anywhere**. The claims that failed
were disproportionately the confidently precise ones.

So the standing rules for anything added here:

- Never invent a patch number, version, date, price or quote.
- Never average two figures you are unsure of into a range described as version
  variance. Say you are unsure of both.
- A forum post, a video tutorial or a search snippet is not a source for what a
  designer intended.
- Currency figures from other games are unusable for balancing this one unless
  genuinely sourced. Derive from this game's own costs instead.
- Name your own weakest claim, and say what would change your mind.

## Records

**Every new record appends its row at the same place, so any two branches that
add one conflict here by construction. Resolve it by keeping BOTH rows, in
landed order: the row already on `main` first, the arriving branch's second.**

That is written down because it was resolved by hand **seven times on
2026-09-03 alone** — #834, #838, #839 (twice), #844 (twice) and #876 — and each
time the resolver had to re-derive the same answer. It is not a defect in any
of those branches and it is not something to fix by editing somebody else's
row: the mechanism is that this table has one append point and passes work in
parallel.

**Do not resolve it by taking one side.** Both rows are records of work that
happened, this file is read-only history by the rule three paragraphs above,
and dropping a row loses the only index entry a note has. A `git checkout
--theirs` here silently deletes a record.

**Landed order rather than date order, deliberately.** Date order reads better
and is worse to merge: it puts the insertion point in the middle of the table,
so two branches adding notes from the same day conflict *and* have to agree on
an ordering nobody can compute from their own side. Landed order is decidable
from one side alone.

**Twenty-four rows had drifted OUT of the table, and that is the mechanism by
which this index lost records rather than a second, separate defect.** Every row
from *2026-09-03 what the owner still owes* to *2026-09-05 does the game say
there is no door* was appended after the closing bullet of the "Findings"
section below — with no header row above them and no blank line between them and
the bullet they follow — so the append point named two paragraphs up had quietly
become the end of the *file* rather than the end of the *table*. They are moved
back into the table on 2026-09-05, in the order they were already in, and **not
one character of any of them is changed**: the twenty-four lines are
byte-identical before and after the move, which is checkable by `sha256sum`-ing
them out of either revision. Nothing is dropped, per the rule above; they are
the same rows in a different place.

**Ten records that had never been indexed at all are added in the same commit**,
found by comparing the directory against the table rather than by reading either
— eight dated notes, this directory's one undated audit distillation, and the
`design-search-2026-08-29/` corpus, which nothing in the repository linked to by
path. One of them, *what a sweep costs the response*, is cited by
[ADR 0073](../adr/0073-who-orders-a-contraband-search.md)'s own amendment while
being absent from the index that is supposed to name it. **The absence lasted as
long as it did because nothing checked**, which is now
`tests/foundation/research-index-contract.test.ts`'s job: it fails when a record
has no row, when a row sits outside the table, when a row names a record that
is not there, and when a subdirectory of records is neither rowed nor linked to
— which is every shape this paragraph and the one above it describe.

| Record | Question it answered | Decision it fed |
| --- | --- | --- |
| [2026-08-25 room zoning gesture](./2026-08-25-room-zoning-gesture.md) | What gesture designates a room, and where does the control live? | ADR 0022 |
| [2026-08-25 room occupancy](./2026-08-25-room-occupancy.md) | Where does a room's occupancy capacity come from? | ADR 0023 |
| [2026-08-25 economy rate](./2026-08-25-economy-rate.md) | What does the state pay per prisoner-day, on what cadence, from what balance? | [#29](https://github.com/matmaxalez/lockstate/issues/29), within ADR 0017 |
| [2026-08-26 failure modes](./2026-08-26-failure-modes.md) | What should failing look like, and what is the cheapest honest route to it from what already exists? | None yet — it states two shapes and declines to pick. **§3's mutual-exclusion finding is corrected in place** (#396): an *unhoused* arrival's `safety` does decay to zero, so overcrowding is the reachable pressure and a riot fires at tick 15,600 |
| [2026-08-26 repository audit](./2026-08-26-repository-audit.md) | Across every discipline at once, what is wrong with this repository at v0.0.108, and what should be done first? | None yet — it ranks work and names the decisions that need an ADR |
| [2026-08-28 risk tier and income](./2026-08-28-risk-tier-and-income.md) | Does a prisoner's `riskTier` change what the state pays, or what the prison spends? | None — it refutes an audit finding and proposes no change. ADR 0017 decision 6 stands |
| [2026-08-28 navigation tick budget](./2026-08-28-navigation-tick-budget.md) | What does a navigation tick cost, and may its budget be a wall clock? | **ADR 0066.** This cell read *"None yet — it carries an ADR draft awaiting a centrally assigned number"* and had been false since that number was assigned: the draft landed as `docs/adr/0066-what-a-navigation-tick-may-cost.md`, which the record's own §6 already links. Corrected here rather than left, and marked rather than overwritten, because a row that keeps saying "awaiting a number" after the number arrives is how this directory stops being readable. ADR 0009 stands unamended |
| [2026-08-29 sentence length at admission](./2026-08-29-sentence-length-at-admission.md) | Where does a sentence length come from — which thread, which RNG stream — and how long should it be? | [#535](https://github.com/matmaxalez/lockstate/issues/535) decision 5, and **ADR 0069**, whose draft this record carried and which has since landed with that number assigned centrally. The range itself is a proposal for the owner, not a settled call; §5's `priorIncidents` finding fed [#540](https://github.com/matmaxalez/lockstate/issues/540) |
| [2026-08-29 playtest: ordering and the second room](./2026-08-29-playtest-ordering-and-the-second-room.md) | Is *ordering* — walls before zoning — the wall a new player hits, and what is on the route past it? | None yet. It proposes no change. Its §7 is a finding with a measured mechanism and is handed to whoever owns `src/ui/**` and `src/rendering/**`; §2 is a product question for the owner; §3, §5 and the trackpad question are empty categories with the numbers behind them |
| [2026-08-29 coverage provisions the safety need](./2026-08-29-coverage-provisions-the-safety-need.md) | Should guard coverage provision `safety`, at what rates, and does coverage actually suppress incidents? | [#588](https://github.com/matmaxalez/lockstate/issues/588), under the owner's ruling on [#599](https://github.com/matmaxalez/lockstate/issues/599). **ADR 0078.** This cell read *"None yet as an ADR — it carries a draft decision awaiting a centrally assigned number"* until the number was assigned; the draft landed as `docs/adr/0078-what-keeps-a-prisoner-safe.md` in the same commit as its index row, and the cell is corrected here rather than left, for the reason the navigation-tick row above gives — a row that keeps saying "awaiting a number" after the number arrives is how this directory stops being readable |
| [2026-08-28 drawing guards](./2026-08-28-drawing-guards.md) | Should a guard be drawn while `GuardRecord` still teleports, the question ADR 0059 open question 4 left open? | Answers that open question in place, within ADR 0040 slice 2 (issue #414's surviving half); no new ADR number taken |
| [2026-08-29 what a day actually pays](./2026-08-29-what-a-day-actually-pays.md) | What was the brief's *"a neglected twelve-prisoner prison earns about 150/day"* actually measuring? | **[#601](https://github.com/matmaxalez/lockstate/issues/601)**, which deliberately declined to choose between three readings. Measured by playing: roster 12, residents 3, credited **900** at each of two day boundaries. Reading 1 is true about the prison and reading 3 about the number; #601's own arithmetic — 150 between `2 x 60` and `3 x 60` — assumed a floor that never applies, because a housed prisoner's needs stay met. With three guards the loop nets **+660/day**, not the −90 the brief's *"closes tightly"* implies. The reproduction stays unmerged on `agent/playtest-mouse-2`; it is a harness, not a gate |
| [2026-08-29 mouse playtest](./2026-08-29-mouse-playtest.md) | Playing with the mouse only, where does a new player stop being told what the game wants? | **None — [#569](https://github.com/matmaxalez/lockstate/issues/569) should be closed as not-a-defect.** The record originally concluded that a refused designation's explanation never reaches the player; §1 carries the correction, by the author, hours later: it reaches `.hud__refusal`, measured at 1440x32 and 900x32 with `role="status"`, which is #220's fix for the very 0x0 alerts row this re-derived. The measurements stand, the inference did not. The reproduction stays unmerged on `agent/playtest-mouse-route`; it is a harness, not a gate |
| [2026-08-30 the naive route](./2026-08-30-the-naive-route.md) | Is *ordering* — walls and bricks before zoning — why the owner could not build a prison? | **None. It eliminates the candidate rather than proposing a change.** [#569](https://github.com/matmaxalez/lockstate/issues/569)'s retraction left ordering as the last evidence-backed candidate; played naively on `9c453be`, the route **works**: a zone-first `ZoneRoom` is refused for 0 money, twenty-four wall orders are *accepted* against an empty stock, buying sixty bricks at tick 5,300 drains the queue by tick 7,321, and one Designate press then takes `ROOMS` 0 → 1. What the pass found instead is §3 — the game never states that a Brick wall needs two bricks (`src/simulation/construction/definition.ts:89`), so the obvious guess of one brick per wall builds **exactly half** a perimeter and stalls at `12 waiting · 0 being built` for two in-game days with `/material/i` false across the whole visible HUD and 24,040 still in the treasury. That is a product question and the copy is the owner's under `AGENTS.md`, so it is reported and stopped. §6 corrects this pass's own instrumentation in place |
| [2026-08-29 a prison that cannot buy its first bed](./2026-08-29-a-prison-that-cannot-buy-its-first-bed.md) | Can a legal purchase end a session, and can one plank pay for more than one resident? | **ADR 0075 and ADR 0076**, whose drafts this record carried (§5 and §6) and which landed with both numbers assigned centrally in one pass after a sweep across every remote head. **The owner ruled on 2026-08-29 and the ADRs are not the drafts**: on 0075 they took three decisions together — **development grants at population thresholds**, a shape the record's option list did not contain; the ADR 0017 degradation ladder with an explicit refusal of bankruptcy (*"Bez bankructwa, tylko minus i pożyczki"*, which is a decision not to overturn ADR 0017 decision 8, with loans as the exit); and sell-back at a loss — leaving out the starting plank grant the record recommends first, because of §2's payroll measurement. On 0076 they took full materials by both routes, which the record recommends no option on, and **relocate**, where the record recommends revalidating the payment; the ADR writes those two as one decision in two parts rather than as a reversal, because the record's argument is the reason the invariant cannot be dropped. §5 and §6 are kept unchanged as what was known and recommended *before* the ruling, marked rather than overwritten. This cell read *"None yet — it carries two ADR drafts awaiting centrally assigned numbers"* and is corrected here rather than left, for the reason the navigation-tick-budget row records: a cell that keeps saying "awaiting a number" after the number arrives is how this table stops being readable. §8 hands the two `docs/adr/STATUS-QUEUE.md` §2 entries over as text rather than filing them, because an unmerged branch is moving the same four counts. Both audit findings reproduce on `main` at `ec10451` (v0.0.206): 625 bricks at 40 spends the whole 25,000 and no command sequence earns a minor unit afterwards; and place-admit-undo turns one 65 plank into three residents earning 4,380 against a control's 1,460. Amends nothing yet; the second draft would narrow **ADR 0028** decision 2 |
| [2026-08-30 authored content with no producer](./2026-08-30-authored-content-with-no-producer.md) | [#642](https://github.com/matmaxalez/lockstate/issues/642): is `gang-retaliation` a missing producer or a leftover of a design change that removed gangs — and what else of that class is in the tree? | **None. It settles the question and proposes nothing**, because both routes out of #642 are the owner's under `AGENTS.md`. **Settled: a producer is missing; gangs were never removed.** Both halves — the `IncidentType` member and `GangRegistry` — arrived in one commit, `31c51ef` (2026-08-23, #28), whose own message says the registry was wired *"with no fabricated content (asserted directly)"*; the assertion still runs at `tests/unit/new-session-runtime.test.ts:123`. #28's dependency list deferred the population to [#39](https://github.com/matmaxalez/lockstate/issues/39), which is open and unstarted, and ADR 0061 decision 5 names the same gap. **The sweep found six further items and one correction.** New: `TunnelRegistry`, `resolveEscapeOpportunity`, `summarizeIncidents`, **both `UtilityNetwork`s** (`addNode`, `connect`, `setFailed` and `evaluate` have no caller in `src/` in any commit on any branch, yet both are snapshotted and restored) and **the whole parcel land-purchase economy** (`setParcelOwned`, `canPurchaseParcel`, `getParcelPrice` uncalled; `registerParcel` reached only from `fromSnapshot`), plus the ungated `'medical-supply'` capability. Correction: `hud.build.note` is the **opposite** shape to `gang-retaliation` — its renderer was deleted (`67e366e`), so #642's *"same class"* is right and its implied same-shape is not. #642's own two citations are corrected in §1: `trigger-system.ts:211-214` is the docblock admitting the gap, not the gate (which is `:510`), and the key is at `messages.ts:157`. Enum pass returned empty across all 42 `SIMULATION_ENUM_GROUPS`, backed by the numbers. Does not overlap `agent/632-unreachable-thresholds` — that record is about values no input can reach, this one about content nothing creates |
| [2026-08-30 what the game never says](./2026-08-30-what-the-game-never-says.md) | [#629](https://github.com/matmaxalez/lockstate/issues/629): what does Lockstate require the player to know, that it never tells them? Swept **by playing**, not by reading. | **None. It reports and stops**, because every route out needs player-facing copy, which `AGENTS.md` reserves to the owner. Two findings, ranked by how early a player meets them. **§1, second zero:** a new session's clock is constructed paused (`src/simulation/worker/state-machine.ts:216`) and no word on screen says so — the whole sighted clock readout is `Day 1 / 0% / ×1`, and `×1` is what a *running* clock prints too; ADR 0051 makes every command answer during the pause, so 2,400 leaves the treasury for sixty bricks the delivery block calls *"ON THE WAY"* and twenty-four wall orders sit at `24 waiting · 0 being built` indefinitely. The one shipped sentence that ties building to the clock, `hud.build.note`, has had **no renderer since 2026-08-23** (`67e366e` deleted the Build panel footer it lived in). **§2, minute one:** `PayrollSystem` re-charges a guard's 80 at every in-game day boundary while `hud.security.hire-hint` says *"Taken from the treasury **on hire**"*; measured 25,000 → 24,920 → 24,840 → 24,760, with `/wage/i`, `/per day/i` and `/daily/i` all false across the whole HUD, and the only sentence implying a continuing wage inside the `ON THE PAYROLL` fold, which starts shut and carries no trailing count. **Three candidates are FINE with evidence** and should not be re-checked: #538's *"nothing says so"* is refuted at `e5f597f` (the Intake panel reads `3 waiting with no bed to sleep in`, unfolded, on every press), an unpayable payday reaches a full-width 1440x32 `role="status"` band, and the Rooms panel states every room requirement before Designate. §7 and §8 each correct this pass's own reading in place — §8 because Run 1 supported an interface criticism that Run 2, differing only in when it sampled, refuted |
| [2026-08-30 what a classification can reach](./2026-08-30-what-a-classification-can-reach.md) | [#632](https://github.com/matmaxalez/lockstate/issues/632): does [#540](https://github.com/matmaxalez/lockstate/issues/540) collapse into it, and what is dead if neither threshold moves? | **None. It measures and stops** — ADR 0017 decision 5 puts every value it names with [#29](https://github.com/matmaxalez/lockstate/issues/29), and no source file was touched. **They do not collapse: three independent gates, and no one fix reaches two of them.** #632's arithmetic is confirmed at every cited line. #540's front door is confirmed (`src/main.ts:867` sends `priorIncidents: 0`, so intake tiers are `[0, 1]` and tier 3 needs 2) — and lifting #632's 200,000 threshold would still only reach tier 2, so it cannot be #540's cause. **#540's headline is refuted**: `room.solitary-cell` is reached two ways from a HUD admission, by sanction (10 relocations measured) and by the accommodation branch after a review promotes a prisoner *still queued for a bed* (9 placements in a bed-short prison, 0 in an adequately-bedded one) — the second route was known to neither issue. #540 comment 3's *"no sentence in the shipped range reaches a first review"* is the `r = 0` row generalised to all 24,000 phases; the correction was already in the tree at `src/simulation/contraband/introduction.ts:126-136` since `cfda696`. **Measured: 14.0% of prisoners are reviewed** (11.8% observed over 119 admissions and 120 in-game days, 0 per-prisoner mismatches against the model), 0% below 10 days, and every reviewed prisoner in a neglected prison reached high risk while none did in a well-run one. Two further unreachable balance values are found and unreported anywhere: **no prisoner can ever receive a second review**, and **`MAX_CLEAN_CONDUCT_CREDIT = 2` can only ever hold 0 or 1**. Its §9.1 records this directory hitting a contract of its own: a harness committed as a `.ts` beside the record was refused by `tests/foundation/typecheck-coverage-contract.test.ts`, and the two contracts — *"every module file `git` knows about has to fall inside one project's `include`"* against this file's *"a record here does not become wrong, it becomes older"* — are incompatible for a `.ts` in `docs/research/`. The harness is a fenced code block in §12 instead |
| [2026-08-30 what stays readable under the pseudo-locale](./2026-08-30-what-stays-readable-under-the-pseudo-locale.md) | [#664](https://github.com/matmaxalez/lockstate/issues/664) second half: ADR 0011's `en-XA` exists and nothing had ever driven the *assembled application* with it — what text reaches a player without passing through the catalogue? | **None. It reports and stops**, because every route out is either player-facing copy or a brand decision, both the owner's under `AGENTS.md`. The brief's *"nothing in the tree appears to run it"* is **corrected**: three consumers exist, but two are unit tests of the transform and the third (`tests/browser/ui-harness.ts:259-261`, reached only by `mountSavePanel`) covers one panel — which is UXA-19 in the 2026-08-26 audit, still open. **31 states swept on the real `index.html` + `src/main.ts`, 9 readable strings, 6 sites, 0 unresolved keys.** Sharpest: **seven call sites in `src/ui/save-panel.ts` splice a raw `Error.message` into a localised template**, so the screen reads `⟦Çóúļđ ñóţ çřéáţé á ƥříšóñ: Simulation worker fault (already-initialized): Kernel is already initialized.⟧` — *while the catalogue already carries the translated sentence for that fault code* (`src/content/default-locale-en.ts:414`, mapped at `src/ui/simulation-alerts.ts:108`), and while the HUD one panel over maps the same failures to keys (`src/ui/hud/hud.ts:1127-1128`). Also: `index.html:11`'s `aria-label="Lockstate game application"` is the application's accessible name and never a key, and the brand is a key in the catalogue (`'brand.wordmark': 'LockState.io'`) and a literal in `index.html:8` (`Lockstate.io`) — **with different casing**. §3.2 corrects this pass's own detector in place: deleting `⟦ … ⟧` spans, the obvious rule, scored the headline finding **zero**, because `pseudo.ts` copies placeholders through untouched and the English sits *inside* the brackets. Both mutations run and watched going red. §6 hands over a defect reproduced against the **unmodified** app: one click on any `.ui-tab` before the first `New prison` makes that press fail `already-initialized`; a second press succeeds; cause not determined |
| [2026-08-30 how a second catalogue reaches a running page](./2026-08-30-how-a-second-catalogue-reaches-a-running-page.md) | [#662](https://github.com/matmaxalez/lockstate/issues/662): `MessageCatalogLoader` has had no implementation since #36 — by which of three routes should a second locale's catalogue actually reach the browser? | **A recommendation that needs nothing from the owner, plus a prototype: route 3, a dynamic `import()` of a code-split chunk.** Measured against real production builds at `898a16a`, with the English catalogue re-tagged `pl` standing in for a translation that does not exist yet (579 keys, 31,968 bytes): **bundling it too costs +8,574 bytes gzipped on the first page for every player forever, per locale** (+1.88 % of 456 kB), against **+633 bytes** for the dynamic import, whose 8,810-byte chunk is not preloaded from `index.html` and so is not fetched until a player asks. The decisive fact is not size but the emitted path — the chunk lands at `/assets/<name>-<hash>.js`, one path segment and content-hashed, which `public/_headers`' existing `/assets/:file` rule already covers, so **no deploy configuration changes**. Route 2's `/locales/pl.v1.json` matches none of the four `_headers` rules — `docs/DEPLOYMENT.md` line 504 names "localization bundles" as exactly this case — and buys **74 bytes on the wire**; §5 says what would have to change and stops there, because that is the owner's. **ADR 0011 needs no amendment**: a content-hashed chunk URL *is* the "versioned URL" it asks for. `import()` of a plain `.json` code-splits identically (32,058 raw / 8,862 gzip), so #661 may author data rather than code. Prototype at `src/services/localization/chunk-catalog-loader.ts`, **not wired into `src/main.ts`**, tree-shaken to a byte-identical bundle while unused; 11 tests drive real `import()`s through the three states #662 demands, and four mutations of the production code were watched going red. Two UNKNOWNs are marked: what Cloudflare serves on the wire, and whether a rejected dynamic import can be retried |
| [2026-08-30 candidate Polish translations](./2026-08-30-candidate-polish-translations.md) | [#661](https://github.com/matmaxalez/lockstate/issues/661): what would a Polish catalogue say, and which sentences cannot survive being translated one key at a time? | **None, and nothing in it is approved.** It is a proposal the owner — a Polish speaker — says yes or no to, line by line; `AGENTS.md` reserves player-facing copy and #661 names the failure a merged machine translation would be. **No `src/content/*-pl.ts` was created** and `src/` was not touched. Covers **579 keys**, not the brief's 387: `default-locale-en.ts` *exports* 563 (388 authored plus 175 derived by `simulationEnumMessages()`), and the running `Localizer` is built from `defaultMessageCatalogEn` (`src/main.ts:1076`), which adds the services layer's 16. **Eleven sentences need reshaping and ten of the eleven are Polish-only** — same parameters, no English change — because the fix is a colon, a middle dot or `×`, not case machinery; §1 records why the ADR #661 warns against is not needed. The eleventh is Class B: ADR 0076's relocation notice, the owner's own approved sentence, whose `{name}` is the subject of a past-tense verb that inflects for gender against a name pool with **no gender model** (`src/simulation/identity/name-pool.ts:35`). **The plural finding overturns the issue's premise**: `pluralFormsSchema` does accept `few`/`many`, but `src/content/` is typed `Record<string, string>` and cannot hold a plural form at all, nothing in `src/` calls `formatPlural`, and `format()` takes `.other` (`localizer.ts:74`) — so a perfect `pl` plural catalogue would render `other` at every count, silently. Every candidate is therefore written count-agnostic. Three of the brief's own examples are corrected: `Buy {count} × {material}` is *not* a case problem in Polish (`×` licenses the nominative), `Costs {total} … in wages` is not a key, and `save.list.item`'s predicted `one`/`few`/`many` need does not arrive. Thirteen questions are asked rather than guessed, including whether `hud.tab.rooms` may be *Strefy* because *Pomieszczenia* (13 characters) does not fit ADR 0022's measured six |
| [2026-08-30 which word for a prisoner](./2026-08-30-which-word-for-a-prisoner.md) | [#661](https://github.com/matmaxalez/lockstate/issues/661): the owner ruled a **mixed** register on Q1 — *osadzony* in official labels, *więzień* in event text — so which word does any given key take, and does the official word fit? | **None, and nothing is approved beyond Q1 itself.** Turns the ruling into a **criterion** rather than a word list, because the ruling's own stated cost is that every future key has to ask the question again. **The criterion reuses a line this repository already draws and tests**: *osadzony* wherever the message is a label — `src/content/simulation-message-keys.ts:95`'s *"dense panel labels, not prose"*, argued at length in `src/ui/simulation-alerts.ts:14-23` and pinned by `tests/unit/simulation-message-keys.test.ts` — and wherever a whole sentence is the simulation's own (`refusal-`, `fault-`); *więzień* only where a sentence is the game narrating (`event-`, `src/ui/simulation-events.ts:158`); and a message naming no person takes neither, which outranks both. **The family hypothesis the ruling proposed was tested and decides 1 key in 12.** Across `hud.alert.refusal.*` (41), `hud.alert.fault.*` (12) and `hud.refusal.*` (12) — sixty-five institutional sentences — **not one names the inmate**, because §6's impersonal `-no`/`-to` device has no subject; the event family names somebody exactly once. **Eleven keys move to *osadzony*, one stays *więzień*.** **The width worry is half backwards and does not constrain the seam.** Measured in Chromium against `tests/browser/ui-harness.html` at the five viewports the browser suite visits: *Osadzeni* is **70.1px against *Więźniowie*'s 85.2px** and the English *Prisoners*' 76.8px, so the official word makes the status strip *less* crowded — which already overflows by 236–941px in English; *Osadzony {id}* is one 7.8px monospace character wider than *Więzień {id}* against **49.4px** of column slack, with `lastLineBottom`, `panelOverflow` and every row height identical across all four cases at all five viewports. No tab is named for the population, so ADR 0022's constraint is not reachable from Q1 at all. **A finding for the owner that is not an answer:** the same run found that a 13-character `hud.tab.rooms` leaves the bar at 12.0 … 363.0 at 375×812 with `tabs.x >= 0` and `tabs.right <= 375` **both passing** — it clips `Overview` by 2.2px and overlaps `Security` by 10.3px instead — so Q5's cited assertion does not fire, and Q5 is **re-asked with the new evidence rather than closed**. Two sentences in `src/` are false as measured and are handed over, not corrected. R1 is untouched: it is an `event-` row whose candidate names `{name}` and never the class. Q1's second half — the masculine against a name pool with no gender model (`src/simulation/identity/name-pool.ts:35`) — stays open, and **no gender model is proposed** |
| [2026-08-30 what the whole economy costs](./2026-08-30-what-the-whole-economy-costs.md) | [#641](https://github.com/matmaxalez/lockstate/issues/641), under the owner's ruling of 2026-08-30: price every buildable, object, room and staff role, separate the one-off from the recurring, cost the minimum viable prison, and say on what day it breaks even — costed against today's 2–16 day sentences **and** against the weeks-long range the owner ruled for on [#593](https://github.com/matmaxalez/lockstate/issues/593). | **None. It measures and stops — choosing the lever is the owner's, and no balance value is touched.** It supports none of the three levers the ruling names, and says why: the economy is not short of money. A minimum viable prison is **890** (a 2×3 `room.cell`: 9 walls, a door, a bed, a toilet — 3.6% of the 25,000 opening balance, which buys 28 of them), it **breaks even on day 4** — 8 real minutes at ×1 — and one of every room type in the game at minimum size is **21,660**, inside the opening balance. The private cell is also the worst deal in it: a 6×6 cell with twelve beds costs **240 a place** and a 10×10 with forty costs **156**, both breaking even at the **first** day boundary, because a perimeter is a fixed cost and a bed is a marginal one. Hiring to `ceil(P/8)` guards is profitable from **two** prisoners onward and never stops being; ten guards against one prisoner takes **41 in-game days** to reach arrears. What actually ends a session is unchanged and is #641's own subject: money → plank → bed → place → money is four single-supplier links, cut anywhere below **65**, and 312 wall segments at 80 reach it. The one lever the numbers fit is **ADR 0075 decision 1's first threshold**, already Accepted, with a floor this record supplies: ~1,000 restores a prison from nothing, 65 from a walled cell. §7 is the two rulings costed together — a cells-only prison's mean grant falls **233.5 → 199.9 per place-day (−14%)** under weeks-long sentences and a fully provisioned one does not move at all, so the sentence change is what makes need provision the thing the economy is about, and should land first. §6 names the gap: the browser playtest could not be run on a contended machine |
| [2026-08-30 tier three at intake](./2026-08-30-tier-three-at-intake.md) | [#540](https://github.com/matmaxalez/lockstate/issues/540): is `priorIncidents: 0` a placeholder or a decision, what else is gated on tier 3, and how much of the security half of the game has never run? | **None. It measures and stops** — ADR 0017 decision 5 routes the value to [#29](https://github.com/matmaxalez/lockstate/issues/29), and §7 is the numbers for that decision rather than a recommendation. **Settled: it is neither a placeholder nor a balance decision but a recorded *abstention*.** The introducing commit `1db8c16` (#306) says both halves of `ADMISSION_REQUEST` were *"deliberately the least eventful values in range"* because *"neither number is a balance decision this file is entitled to make"* — and the other half, `sentenceLengthTicks: 10_000`, has since been through exactly this: #541 moved the draw into the worker, the owner ruled on #593, ADR 0079 set the range. That commit's *second*, defensive reason — a high-risk arrival into a cell-only prison hitting the terminal `'failed'` stage — **is retired**: `hasAccommodationTarget` now asks for every group (`src/simulation/prisoners/intake-system.ts:396`) and the property *"no classification outcome can reach `'failed'`"* is asserted exhaustively. **The brief's main hypothesis is refuted: the escape mechanic is not dead and has not been since #659.** #643's *"escape-attempt at zero across four `priorIncidents: 0` runs"* was the sentence range, not `priorIncidents` — measured here at the current range with `priorIncidents: 0`, **30, 13 and 1 escape attempts** in three prisons, and all 31 subjects were admitted at tier 0, 1 or 2 and promoted to tier 3 by exactly one review, which is the run #643's §12 said would change its own mind. **Nine of the ten tier-3 consumers are reachable today**; the three that are not all read the tier *at the classification stage*. What is left is **one** thing — `classifyPrisoner` cannot return 3 at `priorIncidents: 0` (`1 + 0 + 1`, clamped) — and its consequence: **`contraband.weapon` is impossible, not rare**, because `contraband.introduce` has exactly one caller in `src/` and it passes the intake tier. Exhaustive intake distribution at priors 0: **63.64% / 33.33% / 3.03% / 0.00%**. Corrects #643's inventory row 7 (the introduction probability caps at 0.3, not 0.2) and §8 fixes two comments #659 left false that #669's sweep did not reach |
| [2026-08-30 both shapes, measured together](./2026-08-30-both-shapes-measured-together.md) | The owner's ruling of 2026-08-30 on [#677](https://github.com/matmaxalez/lockstate/issues/677) took **both** shapes — ADR 0080's review-time contraband draw *and* a non-zero `priorIncidents` — and asked for the eight candidate distributions to be re-priced **with ADR 0080's change in force**, because every figure in that ADR was taken at `priorIncidents: 0`. | **ADR 0080**, whose amendment cites it, and [#29](https://github.com/matmaxalez/lockstate/issues/29), which owns the number. **It sets no value anywhere in the tree**: `priorIncidents` is varied on the `AdmitPrisoner` command, which the schema has always accepted, and `src/main.ts:899` still reads `{ priorIncidents: 0 }`. **The premise of the question is refuted.** The two producers are **mutually exclusive per prisoner**, because the review asks the introduction question only when `assessment.riskTier > previousTier` (`src/simulation/prisoners/classification-review-system.ts:364`) — so a prisoner who *arrives* at tier 3 is never *raised into* it, and a non-zero `priorIncidents` moves prisoners **from** the review producer **to** the intake one rather than adding to both. Measured in the neglected prison over 1,196 admissions per candidate: weapons 74, 74, 78, 78, 74, 70, 79, 93 while the split swings from `0 / 74` to `38 / 55`; riots 592 → 589; escape attempts 223 → 235, which is less than one seed's noise. The gate is saturated — 94 % of that prison's admissions reach tier 3 anyway — so ADR 0080's *30 → 56 escape attempts* is the whole of the change and no distribution adds to it. **ADR 0080's invariant is stronger than it claimed and does break, but not where the ruling expected**: at `priorIncidents: 0` the well-built prison produced **0 weapons, 0 escape attempts, 0 riots and 0 review-introduced items over 2,990 admissions and 6,000 in-game days on ten seeds** (that ADR's own weakest claim rested on four seeds of 300 days); every non-zero candidate breaks it, and **52 of the 65 weapons that break it came from intake**, not from the review. **`escaped` is 0 in all 118 well-built runs**, `always 2` included — the well-run prison sees contained attempts and never loses anybody. **One correction to ADR 0080's prose, in both directions**: its *"every intermediate distribution moves tiers 1 and 2 far more than tier 3"* is right about tier 2 and wrong about tier 1, which is pinned within a point of 33.33 % for every candidate but `always 2`. With tier 1 out of it, **`always 1` spends ten tier-2 points per tier-3 point and is strictly dominated by `60/30/10`** — more tier 3 (160 prisoners vs 104), more weapons (16 vs 9), *less* total contraband (503 vs 591). **Recommends `60/30/10`**, with `80/15/5` as the conservative alternative. Weakest claim: no module under `src/ui/` renders a contraband category, so every frequency argument is about a registry rather than about what a player notices |
| [2026-08-30 playing main after fifteen changes](./2026-08-30-playing-main-after-fifteen-changes.md) | Fifteen changes landed on `main` on 2026-08-30 and nobody had played the result. Does [ADR 0079](../adr/0079-a-sentence-long-enough-to-be-a-history.md)'s **×5.8 steady-state occupancy** prediction hold, what can a player see about a 14–90 day sentence and about the reviews [#659](https://github.com/matmaxalez/lockstate/issues/659) turned on, and does [#660](https://github.com/matmaxalez/lockstate/issues/660)'s relocation notice appear in play? Played, not read. | **None as an ADR. It measures, corrects one ADR consequence, and stops** — every other route out needs player-facing copy, which `AGENTS.md` reserves to the owner. **The ×5.8 is right as arithmetic, and the brief's framing of it needs narrowing in both directions**: [#668](https://github.com/matmaxalez/lockstate/pull/668) measures a *build-waste* multiplier and does not read population at all, and [#653](https://github.com/matmaxalez/lockstate/pull/653) already costs per **place-day** — what is stale in it is one table row measured against *14–28* days when the range that landed is 14–90, which is a re-measurement rather than a re-costing. `W_new/W_old` really is 5.78 — intake overhead is ~10 ticks (`sentenceEndTick` is written at the `'classification'` stage, `intake-system.ts:493`, reached at one stage per 5 ticks) against a 124,800-tick mean. But **there is no arrival process**: `AdmitPrisoner` has one producer in `src/` (`src/main.ts:2522`), one consumer (`session-commands.ts:266`), and `requestAdmission` refuses only `no-accommodation` and `population-full` — never *"the beds are full"*. And what the economy reads is `occupiedPlaces` (`income.ts:418`), which is `min(roster, furnished capacity)` and which no sentence length moves — **measured at all 129 samples of a 25-minute run, zero exceptions**: twelve admitted into six beds held `prisoners 12 / occupiedPlaces 6`, every departure above six beds was refilled from the queue inside one sample, and only when the roster fell below six did it track the roster. So #659 multiplies the wait between presses and the count with no bed, not the paying population. The guard bill is *not* capped that way — it scales on the whole roster (`sector-staffing.ts:190`, `deployment-system.ts:107-113`), so a prisoner with no bed costs and pays nothing. **Nothing renders a sentence**: the only read model carrying one is `hud/prisoner-detail`, pinned unread at `tests/foundation/projection-reachability-contract.test.ts:317-318`; a sweep of eleven words over the whole visible HUD returned **all eleven absent** at admission, and at the end of a fifty-in-game-day session the only three present were the one string *"1 released — their sentences are served."* **A review reaches the player as nothing at all** — `SIMULATION_EVENT_TYPES` has eight members and none is a review — so its whole footprint is one word on at most four roster rows: measured `Low/Minimal/Low/Minimal` at tick 28,575 → **`High/High/High/High`** at tick 49,192 → three back to `Medium` by tick 99,766. Tier 3 is the tier ADR 0079 says **no admission can produce**, and it is also `ESCAPE_ATTEMPT_MINIMUM_RISK_TIER` (`flashpoint.ts:113`). **#660's notice does appear in play**, first time through the assembled page: *"Jonas Costa had nowhere to sleep and moved to Cell."*, 1440×32 at y=80, with `occupiedPlaces` held at 1 while capacity fell 2→1. **#640 was live on the commit both runs played**: six wall orders at `6 waiting · 0 being built` at tick 5,020 and 5,287 with `/material/i` **false** across the whole visible HUD — **and §5.1 therefore describes a game that no longer exists**, because `a87b0d3` landed on `main` at 17:03 UTC, between run A's act 3 and run B's act 1. It is kept as a measurement of `898a16a` and marked as history, with the successor cited rather than re-run: `16d8b2a`'s own research record — *"does a wall get built without ever pressing Buy"* — played the same route six times without ever pressing Buy and found *"the naive route completes"*. (Cited by commit and title, not by path: this branch is cut from `898a16a` and that file is not in its tree.) The two runs also inherited `waitForQueueEmpty`'s unanchored regex (fixed in `16d8b2a`), so *"the queue is empty at tick N"* is untrustworthy in both — audited in the provenance, and no claim rests on it, because every structural step is confirmed by a counts value (`rooms` rising proves enclosure; `accommodationCapacity` proves the beds). §5.2 is new — after one Designate the Rooms tool stays armed and the panel folds, so the control that looks like the way to draw a second room is the one that stops it; it cost this pass a run. Its weakest claim was named as a **method** one — a fall in `prisoners` is not proof of a discharge, because `releasePrisoner` has two callers (`discharge-system.ts:184` and the escape hook at `new-session.ts:1118`) — and **run B's `simulation/event` dump settles it: of four falls, three are `prisoners.discharged` and one is an escape**, whose `incidents.escape-attempt-opened` is at tick **48,000**, one tick after the review pass at 47,999 that put the population at the tier `ESCAPE_ATTEMPT_MINIMUM_RISK_TIER` requires. Run A's nine falls are therefore an upper bound on its discharges, not a count. **A successful escape has no player-facing sentence at all** — `SIMULATION_EVENT_TYPES` has the attempt opening and nothing for it succeeding, so the player gets the same all-clear a contained attempt produces |
| [2026-08-30 the waste multiplier](./2026-08-30-the-waste-multiplier.md) | [#641](https://github.com/matmaxalez/lockstate/issues/641), under the owner's ruling of 2026-08-30 to raise the starting treasury and then to **measure the waste first**: what does an unaided first prison cost, against what a perfect one costs? It is the single measurement [2026-08-30 what the whole economy costs](https://github.com/matmaxalez/lockstate/blob/agent/641-economy-costing/docs/research/2026-08-30-what-the-whole-economy-costs.md) §9 (on `agent/641-economy-costing`) asked for as the thing that would change its mind. | **None. It measures and stops — the treasury figure is the owner's.** **2.2×–2.8×**, measured by playing on `agent/627-just-in-time-materials` across **ten sessions in three runs** (`3 passed (8.1m)`, `3 passed (13.1m)`, `4 passed (12.3m)`), against a denominator measured in the same harness: a flawless 2×3 cell costs **905**, twice to the minor unit, which is `10 × 80 + 65 + 40` and sits exactly `80 − 65` above the costing's canonical 890 — the door-for-a-wall substitution an unaided player does not know about. So this is the costing's **3× case, not its 10× case**, and its §8 survives. **The shape matters more than the ratio**: under just-in-time materials `Undo` refunds no money but returns the bricks in kind, so a 6×6 traced and taken back leaves 1,080 sitting in a container the HUD has no readout for and destroys **nothing** — half of a careful session's "waste" is illiquidity. What is genuinely destroyed is geometry left standing out of `Undo`'s one-transaction reach (1,200 in profile B) and a finished object removed (65, which `hud.build.remove-hint` states in advance). A bed placed before any room exists is **refused for nothing** — an empty category with its evidence. **The 65 floor was never approached by waste**: session minima were 22,430–23,015. It is approached by *drawing*, and profile C measures how far: **306 wall segments in 22 drags with the camera never moving, closing on 520** — six segments short of ADR 0075's lock, stopped by running out of reachable tile edges rather than money, with `grep -oiE "(afford\|money\|funds\|low\|warn\|short\|cannot\|unfunded\|treasur)"` over the entire `.hud` matching exactly one token, `FUNDS`. So the lock is **one screenful**, which is what the record contributes to the treasury choice: 50,000 puts it at 2.0 screenfuls, 100,000 at 4.1, 250,000 at 10.2, and it recommends **100,000** while saying plainly that no figure on that table closes the trap — ADR 0075 decision 1's grant still does. §6 keeps an **invalid session** on the record: a swallowed click left the Remove tool armed, the mistake never happened, and the run reported **1.02×**, caught by a command count and not by the multiplier |
| [2026-08-30 a wall that buys itself](./2026-08-30-a-wall-that-buys-itself.md) | On `agent/627-just-in-time-materials` (PR #640): does a wall actually get built with no procurement press, what stops the player next, and how close does an ordinary session get to ADR 0075's lock? Played four times, never once opening the Buy fold. | **None on `src/`. It confirms the fix works, corrects issue [#641](https://github.com/matmaxalez/lockstate/issues/641)'s framing, and reports three gaps whose remedy is copy or plumbing the owner owns.** **§1, the answer to #627:** one drag lays six wall segments, 25,000 → 24,520 at the press (exactly 6 × 80), and all six stand by tick 879 with nothing else pressed; the whole route — 24-segment perimeter, `Designate`, bed, toilet, two admissions, one guard — completes with no `Buy` and costs **2,105 of 25,000**, identical in all four runs. **§2:** nothing stopped the player, and that is the finding; four slower-or-quieter things are ranked instead, of which the sharpest is §2b — the purchase the game makes on the player's behalf is announced **only inside the procurement fold**, the exact fold #627 exists so the player never has to find (`deliveriesBlock` is the last child of `buyRow`, `src/ui/hud/build-panel.ts:1293-1311`, and `buyRow.hidden = true`). **§2b is written in both directions on purpose:** it first read *"the block never shows a just-in-time delivery"* on four runs of evidence, and a clock-stopped experiment refuted that — with the fold open the row is there, with its own `Cancel`, and the run spent 80 rather than 480 because it netted off ten bricks the player had bought, which is ADR 0017 decision 7 measured. **§3:** `purchase.insufficient-funds` is reachable, at 1440x32, and reads *"The materials were not ordered — there are not enough funds."*; it names no wall, no tile and no number, and it stands unchanged for four minutes while eighty-one walls go up behind it, because `src/simulation/construction/system.ts:699` discards the scheduled-tick report. The shortfall figure the projection computes reaches no pixel: `HudBuildQueueViewModel` has no field for it. **§4:** #641's arithmetic is confirmed to the segment — 313 ordered, 312 funded, **40 left** — but its *"a single sustained drag reaches it"* is refuted: one drag is at most 20 segments, one screenful is 116, and 313 took 24 drags over six and a half minutes. **§7 corrects the shared harness in place:** `waitForQueueEmpty`'s unanchored regex matched `10 waiting · 0 being built`, and every playtest here inherited it |
| [2026-08-30 two subsystems with no entrance](./2026-08-30-two-subsystems-with-no-entrance.md) | [#648](https://github.com/matmaxalez/lockstate/issues/648) and [#649](https://github.com/matmaxalez/lockstate/issues/649), settled the way [#642](https://github.com/matmaxalez/lockstate/issues/642) was: for the two `UtilityNetwork`s and for the parcel land-purchase economy, is a producer missing or was the feature abandoned? | **None. It settles both and proposes nothing**, because building either producer, deleting either subsystem and setting any price are all the owner's under `AGENTS.md`. **Neither was abandoned — on 630 refs no producer, consumer or command for either has ever existed, and neither file has ever been deleted.** **#648: the missing half is a producer**, and the record adds a second absence the issue does not claim — **the consumer is missing too**, `evaluate()` has no caller, so a producer alone would light up nothing (the opposite of #642, whose consumer runs every tick). `2da9cb3` (#25) settles it in its own message: *"empty electricity/water UtilityNetworks, with no fabricated default content"* and *"this code is not wired into main.ts"*; `new-session.ts:688-691` still says *"Empty until a session/scenario places real generators/consumers"*; `ROADMAP.md` Phase 7 still lists *utilities*. **No test asserts the emptiness** — unlike #642's — and the one test that touches them fills them, because #375 measured deleting both `loadSnapshot` calls as a 217-file/2,463-test survivor. `ObjectDefinition` authors no capacity or demand figure and no catalogue object is a producer. **#649: the missing half is a command**, `PurchaseParcel`, plus a one-line producer. **#642's method does not reach it and the record says so**: `a71c955` is the owner's own bare one-line commit from before the issue workflow. The intent is in `ROADMAP.md` Phase 2 instead — *purchased parcel model*, *world expansion pricing hooks* — written **4h45m before the code**, which delivers those two lines and no others (`git merge-base --is-ancestor f326407 a71c955`). ADRs **0019**, **0045** and **0047** each re-state the gap, two naming `PurchaseParcel`; ADR 0019 plans for *"the first content module that defines parcels"*. Unlike #648 the **consumer half is live**: `isTileOwned` gates build, zoning and object placement, the renderer outlines owned land, and three shipped strings tell the player *"you do not own that land"* — with one 32×32 chunk and `setOwned` called once, ever. **For [#641](https://github.com/matmaxalez/lockstate/issues/641)**: `Treasury.spend` has exactly three callers, so land would be the game's fourth money sink and the only one that converts money into playable area; `basePrice` and both pricing hooks already exist with no value ever chosen. Deleting differs in cost — the utility fields are **required** in every save, the parcel fields are `.optional()` |
| [2026-08-30 what an escape says](./2026-08-30-what-an-escape-says.md) | [#683](https://github.com/matmaxalez/lockstate/issues/683): a successful escape produces the same all-clear a contained attempt does — is it a distinct event kind, or the same kind with an outcome field? | **None. It settles the plumbing and stops**, because the sentence itself is a new player-facing promise and `AGENTS.md`'s fourth exclusion reserves those to the owner. **#683's claim survives, measured:** two runs of the real `IncidentResponseSystem` one input apart — three guards (`requiredResponderCount(6)`) against none — put the *same two rows* on the events channel, *"A prisoner is trying to break out."* then *"The prison is under control again — no incident is still open."*, while one arm lapses with `escaped: true` and hands its participant to the departure port and the other does not. Committed as `tests/integration/escape-outcome-visibility.test.ts` with three mutations red-then-green. **The plumbing answer is that the question has two layers and only one shape is open at each:** the incident *record* already has the outcome field (`IncidentOutcome.escaped`, persisted in `save-schema.ts`, projected with `summary.escapes`), and at the *event channel* a distinct kind is the only host — `incidents.escape-attempt-opened` is published at open, before the outcome exists, and `incidents.all-clear` is an aggregate whose own schema refuses to carry a figure and which coalesces two closures into one row. **Neither shape is a save-format change** (ADR 0038 is not engaged: the event log is deliberately not persisted) and no fingerprint moves (they are computed per run, not baselined). **Measured cost of the kind:** 36 lines across 5 files, `tsc -b` exit 0, prototyped and reverted — three compile-forced sites (`EVENT_PRESENTATION`, `eventParameters`, the test's `SAMPLE`) **and one the compiler does not force**, `eventParameterMessages`'s early return, where a `{name}` would ship literally. **The suite forces *a* resolvable sentence and not a *new* one**, so a wrong-but-existing sentence compiles and passes; the gate is a person. **The owner's decision is one sentence, and the only question inside it is whether it names the prisoner** — naming is the shape ADR 0076's relocation notice already proves end to end, and its identity rule needs one narrow ruling because an escapee, unlike a relocated resident, is gone by the time the main thread reads the event (safe from the payload, unsafe only in the `"Prisoner 3"` fallback, which no path in `src/` can reach). Corrects `docs/INCIDENTS.md` in both directions: *"`IncidentRowViewModel` … is what a player sees"* has been there since `31c51ef` (2026-08-23, #28) and has never been true — nothing under `src/ui/` or `src/rendering/` has ever read `hud/incidents` on any ref |
| [2026-08-30 playing into the lock](./2026-08-30-playing-into-the-lock.md) | Now that [#640](https://github.com/matmaxalez/lockstate/pull/640) makes ADR 0075's hard lock reachable by dragging: what does the player see on the way down, what happens at the bottom, does an ambitious build get there, and which of ADR 0075's three accepted decisions would have caught it? Played four times on `v0.0.257` (`b143791`). | **None on player-facing surfaces. It reports, proposes four things the owner owns, and makes three corrections that are neither copy nor a number.** **The descent reproduces to the segment** — 312 funded, the 313th refused, **40 left**, `40 | FUNDS` on the strip — and so does the 24-drag count, in two runs whose `DESCENT RESULT` lines are identical character for character. **§1 corrects the wall-clock figure beside it in both directions**: the same descent took **145 seconds** at load 3.1 against [`a wall that buys itself`](./2026-08-30-a-wall-that-buys-itself.md) §4's *"six and a half minutes"*, so the gesture count is the game's and the duration was the machine's. It also corrects **its own instrument twice**: a money-word grep over the whole HUD answers `true` on the first drag of a fresh session, because `Funds` and `Buy` are permanent furniture, and this pass's segment number of 325 is 313 read through worker publications that lag the press. **§2 is the answer to the first question: nothing warns.** Over the 24 drags that spend 25,000 down to 40, the only lines newly on screen are the day-progress percentage, the autosave notice and the queue count; **the first sentence about money in the whole session arrives on the first press the prison cannot pay for.** The Funds chip reads `tone=null` at every balance, by an explicit decision at `src/ui/hud/projection.ts:400-438` **half of whose recorded reason had expired** — corrected in both directions here, with `916ac46` (#455, recurring wages) and `a87b0d3` (#640) as what falsified it and `git merge-base --is-ancestor 4f711d5 916ac46` ordering them — and the **class swept**: a mirror gate for the charge direction of a claim the suite already gated in the credit direction found **two** sites, red then green. **§2b corrects [`a wall that buys itself`](./2026-08-30-a-wall-that-buys-itself.md) §3 in both directions**: the shortfall figure *does* reach a pixel on `main` — *"Waiting for 1,040 to buy materials."*, outside the fold on purpose — and `git log -S` puts both string and wiring inside #640's own merge, after the playtest that reported them missing. **Part B works eight escapes with a mouse, and seven of them move the balance by zero**: waiting three minutes (the prison builds itself behind the sentence saying there is not enough money), twelve Cancel presses on a fold that offers **3 rows against 224 waiting orders** and heals the queue invisibly while the treasury does not move, five `Undo` presses, three Remove presses on walled tiles (`"there is no object on that tile"` — correct, and the **only shipped sentence about refunds is attached to the tool that cannot touch a wall**), a plank purchase refused by a main-thread pre-check that holds both numbers and produces a sentence naming neither, a bed refused for zoning rather than for money, and 5,100 ticks past two day boundaries. **The eighth is the finding the pass did not expect and ADR 0075's table cannot have**: with the clock never started — the state a new session arrives in — a wall run is **fully refundable** in the procurement fold, which says *"15 bought · 1,200 back if cancelled"* and delivers it, 23,800 → 24,760 — **and six seconds after pressing Play it is all taken back out**, 24,760 → 23,800, because the orders are still queued and the scheduled pass re-buys. **The one control that puts money back is undone by the clock, and nothing says so.** **Part D corrects this pass's own hypothesis**: one ambitious prison wing is **121 segments and 9,680, 39% of the treasury**, reproduced in three runs with `tone=null` throughout; three wings is **68.8%**, not the floor — the arrival is arithmetic on a measured wing cost, named as the weakest claim. Two things are FINE with evidence: **changing your mind is free** (nine cancellations then nine redraws cost 0, twice), and a *working* prison is still 2,105. **Part C answers the fourth question from the ADR's text: decision 2 is the only one of the three that catches this.** Decision 1 cannot fire — a threshold paid for population cannot be crossed by a prison that can never house anyone, and a grant of G moves the floor from `floor(25,000/80)` to `floor((25,000+G)/80)`; the ADR says so itself, *"the first threshold alone does not close the class"*. Decision 2 **is** the remedy, in the ADR's own sentence — *"`Treasury.spend` refuses rather than overdrawing … **Building this means changing that**"* — and that refusal is the whole lock, four lines at `src/simulation/economy/treasury.ts:111-116`. **Decision 3 does not reach this prison as written**: it converts *stock* back into money and was justified against a prison holding 625 bricks, but `tryAllocate` **consumes** and the just-in-time sink buys only the deficit net of stock and in-flight, so the wall prison holds **0 bricks and 312 walls** — **#640 moved the illiquidity out of the warehouse and into the map.** Also recorded: the owned world is one 32 x 32 chunk (`new SparseWorld(32)`, `src/simulation/runtime/new-session.ts:385-387`), 2,048 tile edges of which the treasury funds 312, so the map is not what stops anyone |
| [2026-08-31 playing the nine changes](./2026-08-31-playing-the-nine-changes.md) | Nine player-visible changes landed on `main` on 2026-08-30/31 and nobody had played the result. Does [#691](https://github.com/matmaxalez/lockstate/pull/691)'s escape sentence reach a pixel; is the weapon [#681](https://github.com/matmaxalez/lockstate/pull/681) makes reachable anything the player can see; does [#690](https://github.com/matmaxalez/lockstate/pull/690)'s extra press per room read as friction; and does the money story [#640](https://github.com/matmaxalez/lockstate/pull/640)/[#693](https://github.com/matmaxalez/lockstate/pull/693) tell make sense from the chair? Played on `main` at v0.0.273 (`6b5dc8d`) in five acts, including the first escape in this repository reached **by playing** rather than by a fixture. | **None on `src/`.** It confirms #650, #685, #690 and #693 working in a real session, and hands the owner five surfaces where a fact the simulation knows reaches no pixel — each one copy or layout, which `AGENTS.md`'s fourth exclusion reserves to them |
| [2026-08-31 what a reload keeps and what it says](./2026-08-31-what-a-reload-keeps-and-what-it-says.md) | the 2026-08-31 playtest record *“the alerts log was opened, and every sentence in it is cut to thirteen characters”* (unmerged, `playtest/play-the-twelve`, so cited by title rather than by path) §13 recorded two things about a reload by playing and diagnosed neither: a prison that came back with `2 CONTRABAND` and no sentence anywhere saying what the two were, and a twelve-prisoner prison reading `0 COVERAGE` under a green `Covered`. Which is a defect, where does it live — and does `docs/PERSISTENCE.md`'s exclusions list still hold when it is walked against what a player sees rather than against the schema? | **None as an ADR. One defect fixed on `fix/restore-what-a-reload-says`, one finding refuted, one entry added to the contract.** **Finding 2 reproduces headlessly on the first attempt** through the real save boundary: `BEFORE { prisoners: 4, covered: 4 }` / `AFTER { prisoners: 4, covered: 0 }`. **The brief's framing of it is corrected**: the chip's number and its badge are read off the *same* three fields (`projection.ts:471`, `:473-474`), so they do not disagree — `0 / 0 / 0` is what an empty prison looks like and *Covered* is the right word for one. The input is wrong, not the projection, and the rule the playtest floated (*"0 covered with prisoners present is not Covered"*) would have gone amber on every busy intake, because a prisoner in transit stands on no rung. **`SafetyCoverageSystem` re-derived its census on its first scheduled update, ten ticks in — and a restored session is paused**, so `handleInitialize`'s deliberate publish-before-any-tick (the one whose comment exists to stop *"a row of zeros"*) carried the empty census for however long the player left the game paused. Fixed by deriving it at the load instead: no payload field, `SAVE_SCHEMA_VERSION` unmoved, `takeCensus` being `update`'s own loop at zero elapsed ticks so there is still one place that decides a rung, and provisioning nobody. **Finding 1 is refuted, and the measurement that does it is one the playtest could not take: the chip's *name* survives a reload** — `contrabandNameKey: 'contraband.drug.name'` on both sides, for one find and again for two of the same kind, so #703 ruling 13's *"a found phone stops being the character `1`"* is kept across a save. What §13 met is `soleDiscoveredContrabandNameKey` refusing to describe two *kinds* with one word, which §2 of the same record measured it doing **before** the save as well. The `SimulationEventLog` exclusion is therefore measured true rather than wrong — item state, ledger, count and name all persist — and both fixes a reader would reach for are worse: snapshotting the log re-announces a past tick, synthesising a row at load authors a sentence. **The gap is a missing panel, not a missing field**: `hud/contraband` carries the whole ledger and has no reader, gated as such at `tests/foundation/projection-reachability-contract.test.ts:324-325`, so a player who reloads is in the position of a player who never did and asks a day later. **The exclusions walk found one entry missing from the list altogether** (the census, now added, with the half of the rule the list lacked: derived state is recomputed *and recomputed by the restore itself where a paused session would otherwise show the underived value*), **one that costs a player a word** — a projection-by-projection diff of every route before and after a reload shows a guard mid-journey coming back `"travelling" -> "on-post"` on `hud/staff` and `hud/held-guards`, both of which the UI reads — and **one that is currently a benefit**: `RefusalLog` is not snapshotted, so a reload is presently the only way to clear the band §9 of the playtest found pinned for a whole session. `everAdmitted: true -> false` reproduces and reaches no pixel on a populated prison (`regime-panel.ts:647`). **One mutant survived and is reported rather than hidden**: passing `0` instead of the restored tick at the call site is unkillable on the shared fixture, every schedule in which is a `constantDeploymentSchedule` |
| [2026-08-31 playing the twelve](./2026-08-31-playing-the-twelve.md) | Twelve pull requests landed on `main` on 2026-08-31, most of them the owner's rulings on [#703](https://github.com/matmaxalez/lockstate/issues/703), and several had only ever been measured in isolation. Played at `/index.html` with a mouse at **1280x800 and 1920x1080** on v0.0.294 (`ea97c6d`), in thirteen acts: does the now-open alerts log read; does a contraband find write its sentence; does a `HIGH RISK` chip pinned at 0 read as broken; is the Regime roster's tier sort observable; is the spend outside the Buy fold reachable; and do nine chips fit. | **None on `src/`.** Ruling 1's log opens and scrolls and every sentence in it is ellipsised to 88px of a needed 538 at **both** widths; the contraband sentence and the `HIGH RISK` chip are confirmed working; and it hands the owner one non-determinism (three same-tick purchases list in a random order, five orders in six rounds), one lifetime defect (a refused *Remove* stands over the world for the rest of the session) and four labels painted outside their box, including the only instruction for drawing a room |
| [2026-09-01 playing after the rulings](./2026-09-01-playing-after-the-rulings.md) | Six changes landed on `main` in the hours before this pass and **none of them carried browser evidence**, three saying so in their own commit messages. Are build orders really carried out in the order they were placed ([#731](https://github.com/matmaxalez/lockstate/pull/731), ADR 0082); does the FUNDS chip say `{remaining} left` and `0 left` at the floor ([#723](https://github.com/matmaxalez/lockstate/pull/723), ruling 18); does the worker say the host's words for the same refusal ([#729](https://github.com/matmaxalez/lockstate/pull/729), ruling 23); does a guard reloaded mid-journey say `Returning` and walk back ([#732](https://github.com/matmaxalez/lockstate/pull/732), ruling 24); and can a sentence in the alerts log be read ([#726](https://github.com/matmaxalez/lockstate/pull/726), [#720](https://github.com/matmaxalez/lockstate/issues/720))? Played with a mouse at **1280x800, 1280x720, 900x600 and 1920x1080** on `main` at v0.0.305 (`8185243`), in seven acts including a full first half hour. | **None on `src/`.** **Four of the five claims are confirmed on the assembled page and the fifth could not be produced at all.** Six walls drawn in a deliberately scrambled order were offered for cancellation, and executed, in exactly that order (ticks 677/749/800/856/927/983 — a uuid sort matches that once in 720). The floor was reached **exactly** at −2,500 with 681 bricks and 4 planks: no badge at 25,000/13,000/1,000, `260 left` and tone `warning` at −2,240, **`0 left` and tone `danger` at −2,500**, and both authored refusal sentences verbatim. Ruling 23's purchase half was seen from **both** sides in one session, on a measured discriminator — a host refusal writes only `.hud__refusal`, a worker refusal writes the band **and** the log — with the just-in-time route producing the worker's copy with no press on Buy. **`Returning` was never produced**: two acts, ~4 in-game days and every roster sample either took (95 counted, in act 6) never showed `Travelling` either, because `continueDeploymentTravel` ends a deployment with `setTile(guardId, postTile)` — one jump, not a walk — so the phase spans 12–100 ms against a roster pulled twice a second, and `loadSnapshot` rewrites only `'travelling'`. **The finding of the pass is that #726's fix did not change the width.** The alert row is 200px at **every** viewport up to 1920x1080; a 109-character sentence gets `88x165` and **eleven line boxes** at ~10 characters each while the word `Warning` beside it gets 64px, which is the same 88px `2026-08-31 playing the twelve` measured before the fix — the sentence went from cut to slivered. At 900x600 one alert already overflows the log (181px of row in a 137px box) and by in-game day 7 an untouched prison held **eight** rows. Also hands the owner: the strip hiding **133px** of itself at 1280x800 on a populated prison (confirming #723's own note with a number), the Rooms panel hiding **219px** at 1280x720, the only instruction for drawing a room still spilling 231px, three instructions cut by the `max-height: 700px` clamp at 900x600, an *Admit* press still costing **~1.8s** each, an alerts log that accumulates identical rows with no time and no dismissal, and a reload that empties it |
| [2026-09-01 the measurements that were owed](./2026-09-01-the-measurements-that-were-owed.md) | Five figures that documents on `main` derive, cite or explicitly leave open, and that no pass had been able to take in a browser: the alerts row's label width **with a dismiss control actually rendered** (ADR 0084 decision 3 derives ~36px, and every fixture in `ui-shell.spec.ts` carries no `occurrences` so none of them draws the control); the 109-character refusal sentence's line count at candidate label widths and `.hud__rail`'s real outer width (ADR 0085 decision 1, which names ~430px as *judgement rather than measurement*); the post-#723 `EVERY_BADGE` strip (`ui-strip-badged-width.spec.ts`: *"a measurement, not an arithmetic, and this file is where it should be taken"*); the FUNDS badge under the owner's chosen `{remaining} left before deliveries stop`, +23 characters with no measurement anywhere in the tree; and ADR 0086 §2's own weakest claim — 30 s of `hud/prisoner-roster` pulls in a real browser. | **None. It reports and stops.** Confirms ADR 0084's 36px exactly (and finds it is the *worst* of a 36–61px range the severity badge sets, which nothing had said), confirms the row is taller than the list box at three of four viewports while measuring it **~25% taller than derived** (407–422px against ~330px); confirms Finding D1's eleven line boxes at 88px, measures `.hud__rail` at **288px** against the cost note's derived ≈290px, and shows ADR 0085's 430px guardrail leaves the longest sentence **four** line boxes and 182px of world at 900×600 — one pixel off its own stated half-the-budget rule; confirms the strip's worst case at **1,503.2px**, above the top of the reasoned 1,407–1,499px range, with **`funds` eighth of nine and among the two chips actually lost at 1280**; finds the owner's chosen FUNDS wording **does not fit** — +133px of chip, pushing FUNDS itself off a scrollbar-suppressed row at 1280×800 for the whole four-digit range — reproducing an independent measurement on `feat/name-the-rung-on-screen` to the hundredth of a pixel; and splits ADR 0086 §2 in two: **118 pulls in 30 s exactly as predicted**, four runs, with the counts channel silent — and **no gap above 260 ms falsified**, median 253–260 ms and a tail to 292.8–299.6 ms |
| [2026-09-01 what survives a reload](./2026-09-01-what-survives-a-reload.md) | The owner's 2026-09-01 rulings on ADR 0084 changed the answer to the previous reload record's own most interesting finding: the alerts log is snapshotted now, so does it come back reading the same rows, counts and days; does a dismissal survive; does the refusal band (ADR 0087, unmerged and unaccepted) still not; and can a still-standing refusal be told from a lost one? Played on `docs/playtest-reload-2026-09-01`, cut from `main` at v0.0.325 (`27400bcd`), three full attempts at a twelve-admit, day-7 scenario (one lost to the box's own load, not reported as a finding) plus a small isolated refusal repro. | **None on `src/` for the reload questions; one small self-correction to the instrument's own regex.** **`RefusalLog` does not survive a reload, confirmed on two independent scenarios, and the game's only disclosure sentence — "Restored: … Not carried by this save version: …" — never mentions it in either direction**; `CURRENT_SAVE_RESTORED_SCOPE` (`restore-session.ts:202-217`) has no slot for it, and ADR 0087 decision 3, which argues for keeping it that way, is a draft ("Proposed… Not self-approved"), not a ruling. **ADR 0084 decision 3 is confirmed end to end on the third attempt**: a run's `4× Day 8` survives a reload byte for byte, a row dismissed before the save stays dismissed after it **and** after a second save/reload, and a restored row's `×` control still works — no hidden functionality in this specific case. **A defect found adjacent to the surface, not itself about persistence**: a host-side refusal that later resolves (an unaffordable purchase, then an affordable one) permanently evicts a still-true *simulation* refusal from `.hud__refusal` with no reload at all, while the alerts list, reading the identical fact, keeps reporting it standing — `hud.ts:1244-1256`'s ordinal-equality guard cannot tell "still showing this refusal" from "showed it once, was overwritten, and cleared," and `clearRefusal` (`hud.ts:1220-1223`) hides the line unconditionally rather than falling back to `RefusalLog.last`. The loan ledger's restore boundary is re-verified rather than newly found: `loanTerms` reaches no session under `src/`, so there is nothing to play across |
| [2026-09-01 playing the people surface](./2026-09-01-playing-the-people-surface.md) | Residents, staff, risk classification, guard deployment and coverage, contraband searches, and incidents — the one surface of five untouched today. Does a player learn what a risk tier means anywhere; does a contraband find name itself and does a search show anywhere else; is hiring while broke refused consistently now that #785's starter rung has landed; and can a player follow an incident from opening to resolution under ADR 0084 decision 4's new dwell floor? Played on `docs/playtest-people-2026-09-01`, cut from `main` at v0.0.341 (`5cc6227b`), four acts. | **None on `src/`; one instrument bug found and fixed in the same pass.** `.hud-minimap` sits over the world canvas with `pointer-events: auto` (measured `x:12 y:317.8 w:398 h:401` at 1280x800) and silently ate three of six wall-placement presses with zero commands produced, traced by `elementsFromPoint` rather than inferred — the same panel a 2026-08-29 record already found blocking tiles at 900x600 and ruled "not a defect claim, the camera pans"; this pass's `buildResilientCell` now reads the panel's live rect and builds clear of it. **Risk tiers**: the roster badge (`createStatusBadge`) carries no `title` and no screen-reader text on any of four measured rows, where the codebase's own `createStatChip` (used for FUNDS) has exactly that mechanism and this pass measured it in use; only `Minimal`/`Low` are reachable through the Intake panel's `priorIncidents: 0` (`prisonersHighRisk` read 0 across 34 admissions in three prisons), and `Medium` collapses onto the same `general-population` regime block as `Minimal` with nothing on screen saying so. **Coverage, search and contraband: checked and correct** — `Unguarded`/`Understaffed`/`Covered` read exactly as coded across a real hire sequence, the Held Guards fold names a search (`Guard · Contraband Search`) while it runs without dipping coverage, and a real find read `Contraband found: Currency.`. **The starter rung (#785): checked and correct at every layer** — a hire refused at exactly `-1,185` (not the ordinary `-1,250`) on a fresh unfurnished prison, the FUNDS badge agreeing to the minor unit (`55 left` = `-1,130 - (-1,185)`), and the refusal wording confirmed still matching ruling 23's host/worker unification five days and many commits after the money-surface record quoted an older sentence for the same code path. **Incidents**: ADR 0084 decision 4's dwell floor holds a live assault (177 frames) and its resolution (110 frames) cleanly on a real `IncidentResponseSystem` outcome, on a recording armed before the clock started after a first attempt armed too late (carried a stale tick from a different recipe) and recorded nothing but an already-settled screen; but a fight and a riot resolving close together merge into one `2× Day 4` "all-clear" row that names neither incident, by the alerts channel's own documented merge rule for identical sentences rather than a bug in it — the opening sentences name their incident, the one closing sentence never does |
| [2026-09-01 are the risk tiers reachable](./2026-09-01-are-the-risk-tiers-reachable.md) | [#788](https://github.com/matmaxalez/lockstate/issues/788): is the tier ladder above `Low` dead code because `priorIncidents` is pinned at `0`, or does sustained neglect still reach it, and how slowly? | **None. It measures and stops** — ADR 0017 decision 5 puts every threshold and weight it names with [#29](https://github.com/matmaxalez/lockstate/issues/29). **`priorIncidents` never moves, confirmed rather than assumed**: grepping every write to `priorIncidentsAtIntake` under `src/` finds exactly one site, at intake, and its only source under `src/` is `main.ts`'s hard-coded `0` — that settles §788's own question without a run. **But the tier ladder is not dead code**: `ClassificationReviewSystem` writes a prisoner's tier a second time from disciplinary findings folded out of real incidents, entirely independent of `priorIncidents`, and a single lapsed riot's 3 points alone saturate the cap that clamps a review straight to `High`. Run in the real kernel on `f0d4b01e`: an unguarded, bed-only prison — 8 admissions at `priorIncidents: 0`, confirmed pinned throughout — reaches **High risk at tick 47,999** (~19.6 in-game days, ~39 real minutes unaccelerated per `main.ts:858`'s own "three real hours" figure for a 90-day sentence), while an identically-timed staffed, needs-met control produces **zero incidents** and settles every prisoner at **Minimal** through clean-conduct credit. Also corrects #788's own framing: `Medium` needs no neglect at all — a real, if narrow, slice of ordinary long-sentence (84–90 day) admissions already scores it at intake, confirmed by the shipped `tests/unit/prisoners-classification.test.ts:108-109`. **Named as unresolved rather than smoothed over**: two attempts (0 guards; 1 guard against a requirement of 2) both produced only *lapsed* incidents, so a real-kernel case landing on `Medium` via review rather than jumping past it to `High` was not produced in this pass, though the arithmetic permits it |
| [2026-09-02 the first five minutes](./2026-09-02-the-first-five-minutes.md) | Five playtests the same day each played one mechanism (money, rooms, save/reload, alerts, people); nobody had played the game as someone who has never seen it. Using only what the screen says: can a player zone a room, put a bed in it and get a resident without being told; what does the first refusal a new player is likely to see actually say; does the ordinary shape of the first mistake (zone before walls exist, then fix it) leave a false refusal on screen; and does anything explain what the nine status-strip chips mean? Played on `docs/playtest-first-five-minutes`, cut from `main` at v0.0.343 (`f0d4b01e`), seven acts. | **None on `src/`; two instrument bugs found and fixed in the same pass.** An off-screen tile pair (chosen by analogy with `.hud-minimap` rather than measured) hung a `.hud-rooms__confirm` click forever, because this config sets no default `actionTimeout` — fixed by reusing the on-screen `(12,12)-(17,17)` footprint every other playtest here already uses, and by adding a 30s `page.setDefaultTimeout` that then caught a second instance of the same mistake in seconds; and `calibrate()`'s own Remove-tool probe overwrote the very refusal a later act was measuring, producing a false "nothing was removed" reading where the real one was about zoning — fixed by calibrating once and reusing the origin. **The onboarding chain itself works and is checked as such**: the Intake panel's hint names "a cell" and the Rooms tab's catalogue uses that exact word; selecting Cell states its full requirement (`2 × 3 tiles`, `enclosed`, `1 × Bed`, `1 × Toilet`) before a tile is drawn; a refused designation resets to a clean, retryable state; and the whole chain from New Prison to a housed resident took 3,688–3,750 simulation ticks across two runs, all of it readable from the screen the player was already on. **The first refusal a new player is likely to see says nothing**: pressing Admit with no cell reads only `"Nobody was admitted — the request was refused"`, while `main.ts:2682` throws a genuinely specific `Error` ("no room to hold a prisoner") that ADR 0011 deliberately keeps off the screen, and the locale file already carries the exact right sentence (`hud.alert.refusal.admit.no-accommodation`) for a different code path that this one never reaches — `HostRefusalReason` has exactly one member and admission is not it. **The ordinary first mistake really does leave a lying refusal band**, reproduced end to end with no scripted detour: fail to zone on open ground, build walls, succeed at a different rectangle, and `.hud__refusal` keeps reading the first failure verbatim more than a second after a real success — confirming `2026-09-01-playing-the-rooms-surface.md`'s own named weakest claim, that this is the *ordinary* shape of play and not a corner case. **None of the nine status-strip chips explain themselves** at any point in a successful five-minute session — the one chip with a description (`FUNDS`, added 2026-09-01) only switches on in overdraft, which an ordinary opening five minutes never reaches. **Soft finding**: a cell with a bed and no toilet is `NOT READY` on the Rooms panel yet already houses a resident — correct per ADR 0028, but a label a first reading plausibly takes to mean "not usable yet" |
| [2026-09-02 playing the world view](./2026-09-02-the-world-view.md) | The one surface six prior playtests never played: panning, zoom, reachability, and how the HUD interacts with the canvas underneath it. Is `.hud-minimap` — found twice before blocking clicks and ruled "not a defect, the camera pans" — a player problem, measured rather than argued: what fraction of the world does the HUD cover at 1280×720, 1280×800, 1440×900 and 1920×1080; does a blocked click give any feedback; does the minimap navigate; is panning bounded; and what happens at the edge of the one owned 32×32 chunk? Played on `docs/playtest-the-world-view`, cut from `main` at v0.0.344 (`e41a8482`), five acts. | **None on `src/`; three instrument bugs found and fixed in the same pass.** **42.04%–44.42% of the canvas sits behind a `pointer-events:auto` HUD element at the four named viewports** (exact rectangle-union and an independent 8px grid-sample agree within 0.65 points at every one), `.hud-minimap` alone 14.5% of it at 1280×800 with a live rect (`x:12 y:346.8 w:398 h:372`) that has moved since both prior records and must be read live rather than assumed. **A blocked click and an unblocked click one tile over are indistinguishable to the player**: both leave `.hud__refusal` and `.hud__event` untouched, so a swallowed click looks exactly like a click that landed and had nothing to report — there is no third state for "this did not reach the world." **The minimap does not navigate**: three presses on `.hud-minimap__surface` (`hud.ts:1415-1422`'s own comment calls it "a placeholder... minimap rendering belongs to the renderer") sent zero commands and left `calibrate()`'s screen-to-tile origin bit-for-bit unchanged. **Panning has no clamp in any direction** (grepped the whole of `world-scene.ts`; only `ZOOM_BOUNDS` bounds anything) and **no "return to prison" control exists anywhere in `src/ui` or `src/rendering`** — `frameCameraOnFirstWorld` centres the camera exactly once, per `world-scene-input.spec.ts`'s own #202 docblock, already on record. Measured exact and reversible with a deterministic pixel-distance drag (3,200px out, 3,200px back, origin identical to the unit); the far screenshot shows solid black with the HUD strip, Build panel and minimap all reading normally and nothing on screen saying which way to go. **The frontier is checked and correct**: building on the owned chunk's edge is accepted without refusal, materialises the neighbouring chunk on completion (not submission, matching the source's own documented timing), and a same-position screenshot decoded pixel-for-pixel before and after reads exactly `VOID_COLOR` (`0x0B0E12`) before and the `appearance.ts`-predicted unowned-terrain shade after. **Discoverability, READ**: the only camera-control sentence in the game is Build-tab-only, never mentions the wheel or WASD, and `input.action.camera.zoom.*` locale keys have a description but no renderer anywhere under `src/ui/` — no remapping surface exists despite the binding mechanism being in place. **Own instrument bugs, each found and fixed before it produced a false finding**: a sweep-line union returning `NaN` from an `-Infinity - (-Infinity)` sentinel, caught by its own grid-sample cross-check disagreeing; a multi-step OS-level middle-drag silently coalesced under machine load (measured `-800, +666, 0, +666` px on four identical 800px drags), fixed by dispatching synthetic events in-page instead of through Playwright's OS-level mouse API; and a frontier build point computed off the un-panned camera origin landing off-screen entirely |
| [2026-09-02 playing the clock](./2026-09-02-playing-the-clock.md) | The one surface never played across seven prior passes: pause, play, the speed ladder, day boundaries, and what the game does when the player changes the passage of time. Does Play resume at the speed a pause interrupted; does the pause/dimming contract (#639) hold; does a reload keep the clock's speed; is a wage payment or state income legible at a day boundary; are the paused-command siblings of #774 (ADR 0051) clean beyond the four #749 already covers; and does ADR 0084 decision 4's dwell floor survive ×4, established by ticks rather than by this box's own wall-clock load? Played on `docs/playtest-the-clock`, cut from `main` at v0.0.349 (`2025f7d7`), five acts. | **One real discrepancy, corrected as documentation plus a new test rather than a unilateral behaviour change; everything else checked and correct.** **Play always resumes at ×1 after a pause, discarding whatever speed the player was fast-forwarding at** — measured twice: fast-forward to ×4, cycle to ×2, Pause, Play reads ×1, not ×2 — while Fast-forward pressed directly out of the same kind of pause correctly continues the ladder from the retained speed (pause at ×4, Fast-forward reads ×2). `transportIntent`'s own docblock (`hud.ts:2149-2166`) claimed the uniform, false version of this ("unpausing resumes at the speed the player chose") since the file's first commit; corrected to state the measured asymmetry, and `tests/unit/ui-hud-transport-intent.test.ts` — the function had no test anywhere before this pass — pins all three branches, red-then-green run by hand. Whether Play *should* preserve the speed is left to the owner as a real, two-sided design question (Fast-forward's own behaviour and two independent docblocks argue yes; `transportPressedStates`'s `play: !fast` argues the current behaviour is its own coherent contract). **Checked and correct**: the `PAUSED` word and the day/speed dimming (#639); the `1 → 2 → 4 → 2 → …` speed cycle; a reload always returns to a paused session with no retained speed, consistent with ADR 0051's own decided scope; five of six reachable paused-command siblings of #774 (`PurchaseMaterials`, `RemoveObject`'s refusal path, `UnzoneRoom`, `AdmitPrisoner`'s refusal, `HireStaff`) took effect on the panel at once with the tick provably unmoved throughout the whole sweep; and ADR 0084 decision 4's severity-promotion rule held on a real, unscripted incident collision at ×4 (a riot 40 ticks — 500ms at ×4 — after an all-clear correctly preempted it immediately, painting for 5 frames rather than #700's zero). **The protective (delay) half of decision 4 was not independently re-run live in this pass** — it is covered by an existing synthetic browser spec and a pure-function unit test, neither rerun here, on the judgement that a timing-sensitive suite is exactly what should not be re-run fresh on a box this loaded (`uptime` read 5–18 throughout this pass). **Day boundaries are silent in either direction**: state income and the day's wages land on the identical scheduled tick and net to one unexplained treasury jump (+220 measured: +300 income, one occupied place, −80 wages) with no event, no band flash and no alerts row — `recordUnpaidWages` only fires on failure, and there is no event for a met payday or for income arriving at all; the two components are separately readable elsewhere (the Security tab's wage bill, the `EARNED TODAY` chip) but nothing narrates the moment they combine. **`ReleaseGuard`** (a held guard's release, not the roster's `Dismiss`) is the one ADR 0051 command this pass's sweep did not reach. **Two own instrument bugs found and fixed in the same pass**: act 3's day-boundary checkpoints targeted fixed absolute ticks already passed by the time setup finished, fixed to target relative to the tick setup actually finishes at; act 4's `RemoveObject` row targeted a hard-coded tile the cell's own minimap tile-shift had already moved the beds off of, exercising a refusal rather than the removal path, reported rather than silently re-targeted given the same pattern held across the sweep's other five gestures |
| [2026-09-02 playing what landed today](./2026-09-02-playing-what-landed-today.md) | The two changes merged to `main` on 2026-09-02 that reach a player and that nobody had played: guards walking to their post (#740, ADR 0088, `9dd6e601`) and `Medium` risk as a real waypoint (#788, ADR 0090, `75a3797b`). Not "does it work" but ADR 0088's own open question — *can a player ever actually see a guard walk, and does it read as movement or as a glitch?* — and #788's — *a prisoner sits at an unexplained `Medium` for ~18 in-game days, what does a player see and can they act on it?* Played on `docs/playtest-what-landed-today`, cut from `main` at v0.0.351 (`0e2eb7fb`), four acts, everything asserted in simulation ticks. | **A guard never walks, measured rather than argued: 4,978 ticks of dedicated sampling across three prisons, 2,700 render-delta publications, 7,945 guard records, six completed contraband sweeps and one Release press — and not one record with a non-zero velocity or an off-tile position.** The reason is not that the walk is brief: of ADR 0088's two errands one has no distance (every `HireStaff` origin is `src/main.ts:618`'s (16,16), which is exactly `deriveDefaultSecuritySectorPostTile`'s answer, so `beginDeployment` always takes its `isAtPost` fast path) and the other has no route (`deriveDefaultSecuritySector` authors no `patrolRoute` by decision, so `PatrolSystem` is inert in every session a player can start). The third route — `walkBackToPost` or a re-assignment after a contraband sweep — is gated on a staffing shortage coinciding with a spare guard standing off-post, which nothing in ordinary play arranges: `claimableGuardIds` draws only from `'unassigned'` guards so the posted one never leaves, and a six-resident prison asks for exactly one guard. Every sweep leg observed was a teleport of **6 tiles in 2–3 ticks** against the one-tile-per-two-ticks `DEFAULT_WALK_SUBTILE_UNITS_PER_TICK` allows. `Travelling` never reached the roster; `On Search` did, seven times. Pressing Release (the one gesture that manufactures a shortage) produced no walk either — reported **with its limit**: that run had no spare off-post, proven by its own zero position changes, so it does not settle whether Release can produce one. **On #788: `Medium` arrives and the screen does not react.** Neglect fixture played with the mouse (beds, no toilet, no guards, twelve admissions); eleven prisoners reached tier 2 at tick 26,514, and across the next 3,098 ticks the act recorded exactly **one** distinct screen state over six sampled fields. `badgeTone` stayed `neutral` — the same tone `Minimal` carries — so **the badge does not even change colour**, which is stronger than the brief's hypothesis and is two sound decisions composing badly rather than a bug: `describePrisonerRow` ties tone to the classification *group* deliberately, and the early warning is capped at `Medium` precisely so it can never move a group. `HIGH RISK` stayed 0 (it counts `riskTier >= 3`), `INCIDENTS` read `Clear`, and the alerts column narrated the riot that caused the reclassification and never named the reclassification. No `title`, no `aria-label`, no row `title`; hovering adds nothing; pressing the row produced 0 commands **with the press verified inside the row by `elementsFromPoint`**; the Regime panel has no control at all. **One defect neither feature owns:** `.hud-minimap` does not merely swallow a click — at 1280x800 with origin (-384,-624) it makes four world tiles genuinely unbuildable, a drag dropping exactly the three segments whose screen points `elementsFromPoint` places inside it and keeping the three outside, and single presses on them reaching the panel too. Third sighting, first with the cause read per tile rather than argued from load; shifting the fixture two tiles east fixed all four wall runs in every later run. **Confirmed correct (a result):** ADR 0090's cap holds end to end through worker, projection and DOM; the incident channel narrates a fight, a riot and an all-clear in order with an occurrence count; #533's empty-sector exemption reads `0 of 0 / Covered` truthfully. **Corrections:** three to the brief (there is no `--suite playtest` and cannot be; its first reference model file does not exist; "deploy a guard somewhere far" is not a gesture the game has), and one to `2026-09-01-playing-the-people-surface.md` §1b, which said `classifyPrisoner` can only reach tiers 0 and 1 — already false the day it was written, since `9a25700c` on 2026-08-30 made 84-day-and-longer sentences score the long-sentence point. **Seven sentences owed to the owner** are tabulated (badge copy, an alerts sentence, the tone decision, whether tier 2 is counted on the strip, what a blocked build gesture should say, whether a walk should ever be seen, whether Admit should disable itself as Buy now does), plus one handover: `default-sector.ts:98`'s "the first hire is visibly posted" was falsified by #533. Act 2 also found that **#788's ~18-in-game-day window is a property of its fixture**: on a prison built badly (24 arrivals against six beds, no staff) every one of the 24 read `Medium` by in-game day 6, and since the roster is ordered by descending tier a uniform tier makes the ordering carry no information either. **Two weakest claims named**: no run got a spare guard off-post *and* pressed Release in the same session; and `Medium` written at *intake* stays READ rather than MEASURED — the act designed to catch it had its attribution collapse when the prison rioted first (`incidents` read `1`, not `0`) |
| [2026-09-02 the open issue backlog](./2026-09-02-the-open-issue-backlog.md) | Of the open issues that are not the owner's own, how many describe a `main` that no longer exists? Read 85 of the 98 open issues against the code at `af999415` (v0.0.350), one verdict each, never taking "fixed" from a commit message or an ADR. | None — it is a triage proposal the owner decides. **25 already fixed, 41 still open, 8 partly, 11 resting on a rotted premise**, so 42% of the backlog cannot be worked as written. Its three load-bearing findings: #540's *"solitary is unreachable"* is refuted by `tests/integration/risk-tier-neglect-reachability.test.ts` while the `priorIncidents: 0` half of it stays true (and duplicates the in-flight #788); `src/ui/simulation-events.ts:237` and `:634` deny a shipped decision that `src/ui/hud/event-band-dwell.ts:52` implements; and the `git merge-base --is-ancestor` test #291 and #292 both rest on has stopped meaning "merged" now that `main` is squash-merged — 3 of 435 remote heads pass it. Weakest claim: 28 of the 41 STILL OPEN verdicts rest on an absence, and `docs/AGENT_WORKFLOW.md` §4 says that is the sentence that rots first |
| [2026-09-02 where a guard stands](./2026-09-02-where-a-guard-stands.md) | The owner asked for *"ustawienie posterunku/dyżuru w konkretnym miejscu lub na konkretnej trasie jak w prison architect"* — a post or a duty at a place or on a route. How much of that is already built; does it need a save migration; which ADRs already decide parts of it; what is genuinely absent and what does each option cost; what is the smallest honest gesture and what does it collide with. Read on `docs/where-a-guard-stands-and-what-route-they-walk`, cut from `origin/main` at v0.0.358 (`9b8c8e85`). | **All of it is built, and the answer is a value rather than an engine — MEASURED in a real session, not inferred from the unit suite.** A guard hired through `HireStaff` is `on-post` at (16,16) with `isWalking: false` and every patrol metric zero; author a three-waypoint route on the same sector and the same guard spends **2,392 of the next 3,000 ticks walking**, visits the **16 tiles of the authored rectangle's perimeter** (not the four waypoints), publishes a non-zero velocity on **2,392 of 3,000** render samples at 128 sub-tile units a tick, and closes **74 loops, all on time, none late, none missed**. So [ADR 0088](../adr/0088-does-a-guard-walk-to-its-post.md)'s accepted walk is starved rather than broken, and its own consequence — *"A player who hires a guard, or whose sector runs a patrol route, now sees the guard cross the prison"* — is unreachable in **both** halves for **two different reasons**, which is the sentence its amendment does not yet have. **The load-bearing cost is persistence, and it is not the schema:** `patrolRoute` has been in the V5 schema since it was written, so ADR 0038 decision 1 gives **no bump and no migration** — but `restoreSessionSystems` (`src/simulation/runtime/session-systems.ts:731-733`) skips the payload's copy of any sector the runtime already holds, and a bundle hand-edited to carry `postTile: (20,20)` and a two-waypoint route restores to **(16,16) with no route, silently**, MEASURED. ADR 0036 says both halves of this itself and they point opposite ways — *"The payload's row for this sector is therefore never read"* against *"Anything the payload already carries wins"* — because the subject changes between them: the definition is skipped, the schedule and watch entry are payload-wins. **`SecuritySectorRegistry` cannot be told anything twice** — append-only, `register` throws on a duplicate, and ADR 0036 decision 4 point 2 states that as a decision and hands the replace question forward by name — so a movable post cannot be *expressed* today, which is why this is a decision and not a wiring job. **Three unauthored capabilities beside the two the brief named:** `expectedPatrolLoopTicks`, `DeploymentBlock`'s multi-block form (and `assertGaplessDeploymentSchedule`, a validator with **no caller in `src/` at all**), and `'restricted'`, which has no producer. **A sector has no extent** — no rectangle anywhere in `SecuritySectorDefinition` — so a drawn sector and a placed post are different features, and **there is no ADR on sector geometry** because there is no geometry; the brief's guess at ADR 0004 is chunk sizes. **The gesture:** a post is `ObjectTool`'s shape and costs nothing new; a route is a sequence and **no HUD tool produces one** — arm, append, confirm is the smallest honest form, and the held ordered list is the genuinely new chrome. It collides with `.hud-minimap`, and the brief **understates** that: not four tiles but **14.5% of the screen**, silently, and a one-press post has no run to salvage where a wall drag does. `hud/security` still has **no reader** while `projectSecurity` already publishes both the post tile and the whole waypoint list, so drawing a post is part of the cost. **Corrects the brief three times, each in the direction of it being right:** the file it named as the shape for a pinning test does not exist (the right sibling is `tests/foundation/deployment-phase-producer-contract.test.ts`, and this pass wrote `tests/foundation/security-sector-authorship-contract.test.ts` in that shape, red then green); *"read in four places"* is four files and nine occurrences; ADR 0004 is chunk size selection. **Prison Architect is read at SEARCH-SUMMARY tier only** (both wiki pages refused to open) and departed from twice on purpose: its route is a **painted region** drawn by click-drag with no order, which cannot produce the loop, the budget or the on-time/late/missed counters `PatrolSystem` already keeps — **copying the gesture would not fill the field** — and its station is per-guard-in-a-zone where ours is per-sector-on-a-tile, so a 24-prisoner prison stacks three guards on one tile. Weakest claim named: a shortened route throwing a `RangeError` out of `Kernel.step()` is REASONED from two lines and **unreproducible until the mutation entry point exists**. Six sentences are owed to the owner and listed, plus one question — does *dyżur* mean a shift? | [ADR 0092](../adr/0092-who-decides-where-a-guard-stands.md) |
| [2026-09-02 the gates that cannot fail](./2026-09-02-the-gates-that-cannot-fail.md) | All 75 contracts in `tests/foundation/` and `tests/determinism/`, read on purpose for the class three of whose instances were found sideways the same day: which assertions are vacuous, tautological or self-referential? | None yet — it proposes five repairs and makes none of them. **Two gates demonstrated unable to fail on the defect they name**, each shown with a planted offender that the gate caught and then stopped catching once its walk lost one directory (`documentation-claims-contract`, whose own positive control recomputes its threshold from the walk it is guarding; `times-sign-contract`, which went 31/31 green with a hard-coded `x` sitting in `src/ui/`). Six of eight mutations went red as designed and are recorded as such. Six rotted tallies and two drifted pins **fixed** in `tests/` comments, in both directions and dated; the class-level finding is that **one file in seventy-five records having mutated its own walk** |
| [2026-09-02 post and carry, replayed](./2026-09-02-post-and-carry-replayed.md) | Playing (not reading) ADR 0092's ground and ADR 0093's incoherence on a fresh worktree cut from `origin/main` at `b2941064` (v0.0.372): does hiring a guard visibly move anything, does the Staff panel ever say why not, and can a player reach the two-authorities incoherence `tests/foundation/two-authorities-one-prisoner-contract.test.ts` pins? | None — both existing surfaces stay `Proposed`; this is independent re-verification plus one new angle. **Guard-walk finding re-confirmed on a separate checkout**: four hires, 400 ticks, 330 render-delta publications, zero non-zero velocities, one tile (16,16) — matches `2026-09-02-playing-what-landed-today.md` exactly. **New**: the Staff panel's text after four hires names no post, location or route anywhere — nothing on screen tells a player why a guard is not moving. **The carry incoherence is unreachable by a player, played rather than assumed**: `jobWorkers.register(` and `jobs.submitCarryItem(` have zero call sites under `src/` outside the pinned test (grepped fresh at v0.0.372/`b2941064`, matching ADR 0093's own v0.0.364 (`1547c7f6`) ground truth three releases later); `JobSystem` runs at order 260 in every real session with an empty board and an empty pool forever. Not a defect in ADR 0093 — it says as much about itself — but a played, positive confirmation that no gesture through the shipped panels can produce the state the contract test pins today. Weakest claim: the unreachability rests on a grep and one test run, not an audit of every save-migration path; named, and no falsifying hit found |
| [2026-09-02 release an off-post guard](./2026-09-02-release-an-off-post-guard.md) | `2026-09-02-playing-what-landed-today.md` §9's own named weakest claim: does a guard genuinely off its post walk when Released, and if Release alone cannot do it, can anything? Played on `playtest/release-an-off-post-guard`, cut from `origin/main` at v0.0.376 (`c2755012`). | **No — measured twice, in two different confirmed-off-post states, and the reason is structural.** A 10-bed cell, 6 residents, 2 guards: the sector's one post is always filled by the lower-id guard at the first deployment cycle, leaving the higher-id guard as the only one a contraband sweep can ever claim — confirmed off-post on the render channel *before* anything was pressed (tile (14,12) against post (16,16)), once mid-search (held, `claim: 'search'`) and once fully parked and unassigned (`ON DUTY: 1 held · 1 free`, held by nothing). Pressing Release on the *other*, posted guard produced, across both runs, **976 guard render-delta records over 488 publications in two 500-tick post-press windows, zero with a non-zero velocity, the spare's tile unchanged in both**. Read rather than guessed: `assignUnassignedGuards` (`deployment-system.ts:180-200`) walks `claimableGuardIds` in **ascending entity id** and calls `beginDeployment` for at most `shortage` of them; a freshly-released posted guard is always the lowest id in the claimable pool (it was posted first, by the same ascending rule, at session start) and is always at its own post already, so it exhausts the one-guard shortage before the loop ever reaches the higher-id spare. General form given: a spare that has ever gone searching can only be reached once the shortage equals the *whole* claimable pool, which needs the required count to equal the total unassigned guard count — a condition under which no guard would have been spare to send searching in the first place, so the two conditions cannot coincide. **The second question — does growing the requirement past what the posted guard satisfies (no Release) move the spare — is attempted and left open, honestly**: the roster and coverage panel do report the spare `On Post` after occupancy crosses `ceil(9/8)=2`, but 968 render-delta records carry zero non-zero velocity and zero transitions inside the sampled window, so this pass cannot tell a real walk from the spare's own independent contraband sweep coincidentally landing it on the shared hub tile (16,16) — every hire's and every admission's arrival tile — before the window opened, repeating §1's hazard one layer downstream. Named as unreached rather than guessed at, with what would resolve it. One instrument bug caught and corrected in place: the first Act B run left the Staff tab unswitched after `admit()`, reading an empty roster and "not laid out" for 2,000 ticks — not a finding, fixed before being reported |
| [2026-09-02 the unit gates that cannot fail](./2026-09-02-the-unit-gates-that-cannot-fail.md) | The companion pass over `tests/unit/` (218 files) and `tests/integration/` (71 files) — 289 files, 3,567 tests — that the same-day foundation/determinism audit did not touch: which of these cannot fail on the defect they claim to guard? | None — four fixes made directly, red-then-green evidence for each, no `src/` touched. **Only eleven of 289 files walk a real directory at all; all four survivor gates found live inside those eleven**, all fixed and re-proven red on the same mutation that had survived: `navigation-no-phaser.test.ts` never recursed into a subdirectory (a real Phaser+DOM offender one level down stayed invisible, floor `> 0` against 15 files); `services-layer-boundaries.test.ts` and `ui-hud-messages.test.ts` both recursed correctly but carried only a floor loose enough that dropping one real subtree (five `services/challenges/` files; nine HUD files including `status-strip.ts`) still cleared it while the same kind of offender sat unscanned — the second of these hid a HUD module directly constructing a live simulation runtime, `AGENTS.md` boundary 1's exact defect; `ui-orchestration-boundaries.test.ts:587` was a pure tautology (partitioning an array by a predicate and summing the parts always equals the whole, proven by arithmetic, no mutation needed) sitting beside two other, real, checks in the same test. The self-referential-control archetype was hunted hard and found once, already closed (`docs/TESTING.md:519`, `hud-projections.test.ts`). Weakest claim: ~248 files got a structural grep sweep rather than a mutation or a full read; named, with the two files the sweep flagged and did not get to |
| [2026-09-02 what ruling five would cost](./2026-09-02-what-ruling-five-would-cost.md) | The owner's 2026-09-02 ruling on ADR 0092 open question 6 — "the player's sector wins" — costs out rather than argued: what arbitrates between two sectors today and by what rule; where would an authored-vs-derived mark live and what would it touch; does it need a save-schema bump; how visible is the change measured rather than guessed; and what tests pin today's ordering. Measured on `main` at v0.0.380 (`1eb1b5b6`). | None — ADR 0092 stands as ruled; this only prices it. **Today's arbiter is ascending string sort on sector id, confirmed by reading `DeploymentSystem.assignUnassignedGuards` (`deployment-system.ts:180-198`) and by running it**: a throwaway instrument (deleted before push) raced two sectors for one claimable guard and found the winner flips with the id alone — `security-office` beats `security-sector.prison`, `aaa-player-placed` beats it too, `zzz-player-placed` loses to it — so today's rule already looks like "the player wins" by accident about as often as it doesn't, on a string no interface shows. **Exactly one call site needs new logic**: of every reader of `SecuritySectorDefinition` across `src/` (a full census, tabulated), only `assignUnassignedGuards`'s outer loop chooses *between* sectors: everything else — `PatrolSystem`, `IncidentResponseSystem`, both projections, the restore relay — already operates on one sector already resolved. **No save-schema bump**: an `authored?: boolean` leaf on `securitySectorDefinitionSchema` is ADR 0038 decision 1's shape exactly, and absence is unambiguous for every save that exists (decision 7's placement command is unbuilt, so no save has ever carried a player-authored sector) — with the same `.strict()`-refusal-by-an-older-build cost ADR 0038 §4 already accepted for `masterSeed`. **The visibility measurement the brief asked for, exact rather than approximate**: six residents ask for exactly one guard (`Math.max(1, Math.ceil(6/8))`); a second guard is required at nine residents, not before (`ceil(9/8)=2` against `ceil(8/8)=1`); and a starting prison has zero guards — `HireStaff` is entirely player-initiated, confirmed by grepping every `.hire(` call site in `src/`. **What breaks, named and mostly nothing**: `security-sector-authorship-contract.test.ts` would NOT need to change for the recommended shape (its pins are about which module *originates* a `patrolRoute`/`postTile` value, a different "authored" from this ruling's sector-authored-by-a-player — flagged as a false cognate a future editor could confuse); the two `deriveDefaultSecuritySector` identity pins (`tests/integration/security-default-sector.test.ts:200-202`, `tests/unit/security-default-sector.test.ts:92`) would also survive, but **only if the new field is left `undefined` for a derived sector rather than written as `authored: false`** — verified directly against this repository's real `vitest` 4.1.11 that `toEqual` treats an explicit `undefined` property as equal to an absent one and a `false` one as not, so this is the measured reason the field's absence must carry the meaning rather than an explicit falsy value. **Recommended shape**: partition `sectors.all()`'s already-ordered output by `authored` before the claim loop, leaving `.all()` itself and every other reader untouched; **its determinism argument**: `register()` throws on a duplicate id, so id alone is already a total order over the sectors in play, and prepending a two-valued authored rank cannot introduce a tie |
| [2026-09-02 audit the absence claims](./2026-09-02-audit-the-absence-claims.md) | Every sentence in the repository asserting an absence or a count (`docs/AGENT_WORKFLOW.md` §4), checked mechanically against the code, bounded to: all 89 ADR `## Status` sections, `docs/adr/README.md`'s matching rows, and a spot-checked grep sweep of `has no caller`/`no reader`/`never reached`-shaped comments in `src/` and `tests/`. | **One falsified row still live at v0.0.381, and a race caught live on the other half of the same finding.** `docs/adr/0092-who-decides-where-a-guard-stands.md`'s Status line and its `docs/adr/README.md` row both said "nothing below/in it is implemented" after `4a53d292` (#825) shipped decision 3 without opening the ADR — this sweep found both stale independently, then `git fetch origin main` (run immediately before merging, as instructed) showed `origin/main` had already advanced to v0.0.381 via `2e1f0225` (**#829**, the commit this sweep's own brief had conflated with #825), fixing the ADR's own Status line the same day by a different route (`STATUS-QUEUE.md`'s anchor pass). **`docs/adr/README.md:250` was out of that commit's scope and is still false** — corrected here, both directions marked, status word untouched; the redundant draft fix to the ADR file itself was withdrawn rather than pushed as a competing edit. Neither `adr-numbering-contract` nor `adr-status-reference-contract` (matches a status *word*, not implementation prose, by its own stated design) catches this shape in either file. **Six claims verified as still true**, including the `STATUS-QUEUE.md` §2 "is the only one" / "39" instance this sweep's brief also named — already correctly self-corrected on `main` before this pass began, and re-measured current (still 39 at v0.0.380, `1eb1b5b6`). The bulk of `src/`/`tests/` absence claims sit behind dedicated, code-deriving contract tests (`unconsumed-content-contract`, `unreachable-invariant-contract`, `projection-reachability-contract` and others) and were spot-checked rather than fully re-audited. Weakest claim: `docs/*.md` beyond ten spot-checked files, `docs/research/`'s interior, and ~138 of ~150 grep hits behind passing contract tests were read for plausibility, not independently re-derived; named as unswept rather than assumed clean |
| [2026-09-02 classifying 475 branches without trusting ancestry](./2026-09-02-classifying-475-branches.md) | Issue #292 measured that of 22 branches not ancestors of `main`, every one was already merged or superseded by squash merge, which `git branch --merged` cannot see, and issue #537 records that the existing cleanup deleted an active worktree's branch on exactly that wrong assumption. Built and ran `scripts/classify-branches.sh`, a re-runnable, non-destructive classifier over all 475 remote branches: what verdict (KEEP/DELETE-SAFE/REVIEW) does each get, using the GitHub API's `merged_at` rather than ancestry to see through a squash merge, and how many of each. No branch was deleted. | **475 = 41 KEEP + 348 DELETE-SAFE + 86 REVIEW, and `--is-ancestor` would have agreed with the merged-PR verdict in zero of 346 cases** — every merge commit checked has one parent, not the branch tip, confirming this repository squash- or rebase-merges exclusively (e.g. PR #455's `merge_commit_sha` `916ac46`'s sole parent is the prior `main` tip, not the branch's own `27194162`, and `git merge-base --is-ancestor` returns false). Re-running the classifier an hour apart caught PR #835 merge live, flipping its branch from KEEP to DELETE-SAFE with no code change — the API state, not a cached scan, is what it trusts. 86 REVIEW splits into 4 closed-never-merged PRs (content not compared, by design) and 82 branches with no PR at all whose `git cherry` diff against `main` is not empty — provably unmerged, not provably safe either way. Also found: `deletebranches.sh` lives at the repository root, not under `scripts/` as assumed; and `git branch -r` cannot be trusted for the branch count here — 1,330 local `refs/remotes/origin/*` entries against the 475 `git ls-remote --refs --heads origin` actually reports, the other 855 being stale refs from an old one-off fetch outside the configured refspec. Proposed, unarmed: `scripts/wip-reap.sh`, a `wip/*` retention policy (`< 72h` keep, `72h`–`7d` delete only if the source branch/worktree is inactive, `> 7d` delete by default with a `keep/<name>` ref opt-out) gated entirely behind an explicit `--apply` flag nobody has passed — dry run over the live 75 `wip/*` refs: 11 kept under 72h, 3 kept as still-active, 61 would delete. Weakest claim: the 82-branch no-PR/not-patch-equivalent REVIEW bucket was never opened for content, only diffed by patch-id, so a rewritten (not merely squashed) duplicate of already-merged work would sit there undetected |
| [2026-09-02 playing incidents and response](./2026-09-02-playing-incidents-and-response.md) | Played (not read) incidents, contraband and security response — the surface `docs/AGENT_WORKFLOW.md` marked played by nobody: what does a player see when an incident starts, who responds, and does a closing sentence say how it closed? Measured on `main` at v0.0.383 (`aa762112`). | None yet — two findings handed to the owner, no sentence proposed (`AGENTS.md`'s fourth exclusion). **A 600-tick response deadline is invisible and was played losing to it**: a riot opened at tick 19,923, five guards hired in direct reaction (the exact reaction `security-default-sector.test.ts` proves resolves this class of riot) did not land until tick 22,054 — 1,531 ticks past `responseDeadlineTicks` (`response-system.ts:26`) — and arrived on ordinary `'Sector Post'`/`'Contraband Search'` duty, never `'Incident Response'`, with no countdown, refusal or acknowledgement anywhere telling the player their five-click reaction was already too late. **A riot's own alert never says how it ended, generalized from escape to the other three incident types**: `lapse()` (`:659`) and `advanceResponse`'s resolved branch (`:850`) both call the same outcome-blind `reportAllClearIfCalm`, so "A riot has broken out" and "The prison is under control again" sit on screen together with nothing connecting one to the other or saying which outcome occurred — confirmed live in the same run and at the two call sites directly. Weakest claim: whether that riot resolved or lapsed rests on claim wording plus tick arithmetic, not a direct read of `IncidentRecord.state` |
| [2026-09-02 what Cancel does not say](./2026-09-02-what-cancel-does-not-say.md) | The brief's named-recent surface, played rather than read: does the Build queue's Cancel button let a player predict its refund before pressing it, now that `refundSurplusOf` (`system.ts:820`) makes that refund run from a buildable's full price down to zero? Played on `playtest/the-economy-played`, cut from `origin/main` at v0.0.380 (`1eb1b5b6`). | None -- reported, nothing in `src/` changed (`AGENTS.md`'s fourth exclusion: the replacement sentence is the owner's). **The queue row shows no money in any state** -- `'{buildable} · {x}, {y} · {edge}'` plus a state word, never an amount, unlike the pending-deliveries row three lines above it which states its refund outright (`'{total} back'`). **Measured live: cancelling a row reading "In Progress" paid back exactly zero** (23,400 -> 23,400), matching ruling 20's table by playing it rather than by reading it. **The one sentence in the whole panel that tries to describe a refund is false and has been reported false since 2026-08-31**: the Remove tool's hint still reads live on `v0.0.380` (`1eb1b5b6`) "its materials come back", which ruling 20 made untrue two days before this was played -- money comes back, not materials, and nothing at all once the crew has started, a case the sentence does not mention. Corroborated at the code level (read only; PR #812/#717 stays untouched per the brief) that the same state label can mean two different payouts across a ten-tick window nothing on the row marks. Weakest claim: the `assigned` state's payout was not independently confirmed with a live treasury delta -- the run caught it moving to `in-progress` before the pause landed; §1's table for that state is a code citation, not a second live measurement |
| [2026-09-03 what Cancel actually gives back](./2026-09-03-what-cancel-actually-gives-back.md) | [#853](https://github.com/matmaxalez/lockstate/issues/853) reports a queue row advertising `80 back` at `assigned` whose Cancel moved the FUNDS chip by nothing across eight seconds, and `tests/integration/construction-queue-row-pays-what-it-shows.test.ts` (#843, `4652dbfc`) asserts that same state pays the catalogue value of the material it holds. Which is describing the real thing, measured from the worker's own state rather than from the chip? Played on `docs/what-cancel-actually-gives-back`, cut from `origin/main` at v0.0.401 (`d0957d6b`). | None -- reported, nothing in `src/` changed and no player-facing string touched (`AGENTS.md`'s fourth exclusion). **Both are true, and neither the state nor the chip is what separates them: it is the tick the command executes at.** That test's `dispatch` submits with `executeAtTick: runtime.kernel.tick` and steps once, so its press runs at the tick the row was priced at; a browser press runs at `SimulationCommandSender.projectExecuteTick`, which adds an elapsed estimate plus `DEFAULT_LEAD_TICKS` (20) whenever the clock runs (`src/ui/simulation-commands.ts:77,188-192`) -- **measured at 36 to 55 ticks**, against an `assigned` order that becomes `in-progress` on the next scheduled construction pass ten ticks away. **With the clock paused the browser's own lead is 0 (recorded per press) and every state pays exactly what its row advertised**: `approved` and `materials-pending` +80 each through a cancelled just-in-time delivery at its recorded price (seven orders took the treasury 24,440 -> 25,000, exactly 7x80), `assigned` +80 through the catalogue value of its allocation **with the container's brick count unchanged at 26** -- ruling 20's "money instead of bricks" observed rather than read -- and `in-progress` 0 against a row that already said `0 back`. **With the clock at 1x, 3 of 10 instrumented presses on a row reading `80 back / Awaiting the Crew` were paid it; 1 of 8 on the topmost paying row, 2 of 2 two walls further back.** #853's chip observation is confirmed by the worker (treasury flat at 23,400 across eleven samples and 152 ticks), so **the projection-lag explanation this pass was asked to test is refuted -- there was no credit to lag behind**; its inferred mechanism is not: nothing came back as material in any state, and its `approved:0`/`materials-pending:0` were correct only because it bought 40 bricks by hand first and `refundSurplusDeliveries` will never cancel a non-`jit:` delivery (#687), measured directly. Also corrected: **six** states are cancellable and the method prices **three**, not four; `planned` is unreachable through any command (`system.ts:398`) and `completed` has no queue row at all. Three side findings recorded: a pooled queue row can submit a `CancelBuildOrder` for a different order than its label described (`build-panel.ts:1893`, two presses of seven); a just-in-time purchase is silently skipped as `duplicate-order` when a cancel at the same tick lowers `inFlightBefore` (six orders placed, nothing bought); and the QUEUED block is collapsed on arrival, so a `data-order` locator never resolves and the click hangs. Weakest claim: 14 instrumented presses on one machine is not a distribution, and the first-row/last-row split rests on two presses on its second arm |
| [2026-09-02 intake shortfall and a Medium badge](./2026-09-02-intake-shortfall-and-a-medium-badge.md) | Played on `playtest/intake-and-classification`, cut from `origin/main` at v0.0.380 (`1eb1b5b`): when does `hud.intake.no-place` and the strip's `PRISONERS` badge actually appear, do the two agree, and — verifying the brief's own correction — what can a player tell apart on the risk-tier badges, including someone who cannot see colour? | No decision changed; two positive confirmations and one new, measured finding. **Intake**: with headroom (10 beds/6 admits) neither surface appears; with a forced shortfall (3 beds/8 admits) both appear together and agree exactly (`"5 waiting with no bed to sleep in"` and `"5 with no bed"`) — working as designed, sub-second divergence during the pipeline's earlier stages neither confirmed nor ruled out. **Classification**: the brief's correction holds, played rather than re-read — `Medium` (tier 2) renders `caution` tone distinct from `Minimal`/`Low`'s `neutral`, with a real, tier-specific word as the badge's only accessible name (no `title`/`aria-label` on any tone), so a screen reader loses nothing. **New**: measured WCAG contrast between the `caution` and `neutral` foreground colours actually painted is **1.07:1** — indistinguishable by lightness alone, confirmed by a captured screenshot where the two badges read as the same muted chip at a glance; the tone issue #788 added to separate `Medium` exists in the DOM and the token layer but is close to decorative as a colour signal, distinct almost entirely by hue rather than lightness. Tier 3 (`High`) and any group change were not reached — the full review needs 24,000 ticks, two orders of magnitude past this pass's real-time budget on a contended box (one `buildAndPopulate` run measured 9.4–10.0 minutes and never finished, `ps`/`uptime` showing other agents' suites running; a later, apparently uncontended run of the same routine finished in 3.1 minutes total) |
| [2026-09-02 what does not survive a reload](./2026-09-02-what-does-not-survive-a-reload.md) | #825 fixed one field a save carried and a restore silently discarded in favour of a fresh derivation (a security sector's `postTile`/`patrolRoute`) — is that the only field with that shape? Enumerated all 16 addressable sections of the V5 save payload and hand-mutated a legal, differing value into each of 7, restored through the real `restoreSimulationRuntime`, and read the rest. Measured on a worktree cut from `origin/main` at `aa762112` (v0.0.383). | None — reports a latent gap, fixes nothing (persistence is ADR 0038 territory, out of this pass's authority). **ADR 0092's fix re-verified independently, holds**: the default sector's `postTile`/`patrolRoute`/`expectedPatrolLoopTicks` all survive a hand-edited, differing payload through the real restore path. **One field with the same shape #825 had, MEASURED, not yet covered by any test**: the default security sector's `gradeId` and `doorIds` are excluded from `SecuritySectorRegistry.redefine` by design (ADR 0092 decision 2's own, reasoned exclusion, protecting the `normalDoorStates` baseline invariant) — but the runtime a restore is handed always already holds the derived default sector, so *every* restore takes the `redefine` branch for it, never `register`, and a payload row with a differing `gradeId` (mutated to `grade.high-security`, restored as `grade.general`) or `doorIds` (mutated to `['door-1']`, restored as `[]`) loses that difference silently, every time. **Currently inert**: `redefine`'s only caller is the restore path itself, and no command in `src/` changes a sector's grade or governed doors, so no save a real session can produce today carries a differing value here — the same latent state #825's bug was in before a route-drawing command existed to reach it. Two existing tests differ a sector's fields on purpose and neither touches this: the unit test's case bypasses `redefine` entirely (registers the sector directly), and the integration test's differing-value case explicitly asserts the restored `gradeId`/`doorIds` equal the *derived* ones, which is the untested boundary rather than evidence against it. **Everything else measured survives**: `masterSeed`, room-instance `width`/`height` (already-recorded rows are authoritative over the zoning-plane recovery), a scenario-authored sector's `postTile` (fresh `register`, not `redefine`), an existing search policy's tuning, `intelligenceSequence`, a guard's `sectorId`, `payroll.unpaidWagesMinorUnits`, identity names, and `alerts.dismissed`. **The inverse (runtime state absent from the schema) turned up nothing new**: the loan book/overdraft floor, the refusal log, `InsolvencyRungSystem`'s crossing memory and the navigation/room caches are all already documented gaps with their own reasoning and, mostly, their own tests. **What the four named existing round-trip suites do not cover**: none of them call the full restore pipeline with a payload row for an id the runtime already holds before restore *and* assert a differing value survives — the exact shape both #825 and this finding live in; the two Playwright specs test storage mechanics only and would pass unchanged either way. Weakest claim, stated in full: only 7 of 16 sections got a direct hand-mutation this pass; the rest are READ verdicts, each with the structural reason (no register-or-skip branch in the restore path) that makes the #825 shape impossible there, but weaker than measurement |
| [2026-09-02 accepted ADRs against main](./2026-09-02-accepted-adrs-against-main.md) | Of the 48 `Accepted` ADRs, which contain a concrete claim `docs/adr/STATUS-QUEUE.md` §5 does not already name and `main` no longer keeps? Checked mechanically (grep the constant, read the order, run the test) rather than read for tone, on a worktree at `9821672c` (v0.0.382). | None — reported, not repaired; no `src/` change, no ADR status touched. **One found**: `docs/adr/0005-entity-storage-model.md:28` still says "nothing in `src/` calls `EntityStore.destroy` and no index is ever recycled", false since `54cdde1b` (2026-08-28, #458) — `src/simulation/prisoners/release.ts:194` and `src/simulation/security/guard-roster.ts:147` both call it in production. The fix already exists elsewhere: `src/simulation/entity/query.ts:16-36`'s own docblock — the exact file ADR 0005's sentence cites as authority — was self-corrected the same day and says so in its own words; it was never carried back into the ADR. Not a restatement of `STATUS-QUEUE.md` §5's ADR 0026 entry, which discusses the same call site landing but never names ADR 0005. **19 of 48 Accepted ADRs mechanically checked this pass (40%)**, 21 more already covered by §5, 8 not reached (the newest, highest-churn band: 0016, 0038, 0075, 0076, 0080, 0082, 0084, 0088); 17 of the 19 checked verified TRUE, two (0020, 0040) found already self-corrected in place. Weakest claim: three of the nineteen (0024, 0039, 0041) are READ rather than MEASURED — a cited determinism test and a browser pixel measurement were not independently re-run |
| [2026-09-02 five audit claims reverified](./2026-09-02-five-audit-claims-reverified.md) | An external audit recommended closing #274, #280, #657, #540 and #662 — does each hold up against the code and against the documents it cites, checked independently rather than relayed? | None yet — five per-issue verdicts for the owner to act on. **KEEP OPEN #274** (A17's 2-line `DEFAULT_TILE_CHUNK_SIZE` fix still unmade; Q2/Q4/Q9 still open owner decisions, re-grepped today). **CLOSE #280** (re-ran `verify-supabase-sql.mjs` myself: 321/321 assertions, 11/11 suites green; only F13, a one-line `docs/SECURITY.md` pointer, remains). **CLOSE #657 as superseded** (ADR 0087 decision 2 answers it verbatim, the owner's ruling is signed on #767, #767 is closed `completed` per the API directly, and `PrisonCondition`/`computeStandingPrisonConditions`/`InsolvencyRungSystem` are wired into the real session build and pass `economy-payroll-loop.test.ts`). **RETITLE #540, not a duplicate of #788**: `priorIncidents` is still hard-coded to 0 at `src/main.ts:918`, but `tests/integration/risk-tier-neglect-reachability.test.ts` (run today, 2/2 green) proves tier 3 *is* reachable through `ClassificationReviewSystem` independent of admission — so #540's "so solitary is unreachable" is now false while its real, undecided question (should an admission ever carry priors) is untouched by #788/ADR 0090, which explicitly leaves `priorIncidents` "untouched". **RETITLE #662**: the issue's own disproof grep is now non-empty (`createChunkCatalogLoader`, tested 17/17 green), so an implementation exists, but the file's own header says "PROTOTYPE … nothing in `src/main.ts` uses this yet" and `grep` confirms no call site — the gap is wiring, not existence. Also finds and names a disagreement with a same-day companion pass (`2026-09-02-the-open-issue-backlog.md`) over whether #280 should stay open, and does not resolve it in either document's favor |
| [2026-09-03 what the owner still owes](./2026-09-03-what-the-owner-still-owes.md) | Nobody had ever assembled the backlog of player-facing wording that only the owner can decide (`AGENTS.md`'s fourth exclusion), so the owner could not see the size of their own queue. Every draft string awaiting a call, every place a sentence was deliberately not written, every `STATUS-QUEUE.md` §2 row and every ADR owner-owed section, derived rather than recalled and checked against `main` at v0.0.403 (`f948b8e2`). | None — a reading pass; no `src/` file and no player-facing string was touched, and **no question in it is answered**, which is the point. **The queue is smaller and more concentrated than the brief assumed: thirteen wording obligations** (six draft strings on screen over eight locale keys, seven deliberately-empty surfaces), plus eight `STATUS-QUEUE.md` §2 signature rows and one un-put economy decision — and **five of the thirteen were created in the previous two days**. **Three of the commissioning brief's premises are false on `main`**: §2 holds **eight** entries and not nine (its own heading opens *"Eight entries"*; the ninth exists only on PR #863, open), `action.carry`'s `'Errand'` has **not** landed, and the unstaffed-sector sentence is **no longer owed** — the owner supplied it verbatim on 2026-09-03 and PR #857 merely awaits merge. **The marking convention is three phrases, not one**, and a grep for the two calibration strings alone would have found one of the six: `draft for the owner's review` (1 hit), `flagged for the owner's review` (3) and `owner-pending` (6) — ten raw hits collapsing to six obligations because one string is marked in three files by design and one marking covers three keys. **The 27 `fourth exclusion` comments in `src/` split 7 live / 6 discharged / 11 not-an-obligation / 2 standing-option / 1 stale**, and the stale one (`src/ui/affordability.ts:157`, *"still open"*) plus a second stale absence claim (`src/simulation/runtime/new-session.ts:731-736`) are reported rather than fixed, because a sweep trusting them would report debts the owner has already paid. **Top three by what an answer releases**: the Buy/Hire refusal copy (two shipped mechanisms are mechanism-only and ADR 0087 decision 2 is *"Recommended, gated on copy"*), the server entry point's pre-merge approval (nothing in `deploy.yml` will ever pause to ask for it), and ADR 0017 §9's reopened ECON-002 hard lock — *"not put to the owner"* by its own heading, and the one item here that blocks a player rather than an agent. **Two categories could not be enumerated mechanically and say so**: the literal heading *"What is owed to the owner"* exists in exactly one of 88 ADRs, and widening finds 38 owner-naming headings in at least twelve distinct shapes, so §8 is classified but not claimed complete; and issue-comment bodies were never swept, with the issue numbers actually read listed instead. Weakest claim: "waiting since" dates the marking rather than the underlying gap, and the three "settled" verdicts rest on the tree's own prose plus one verification each rather than on asking the owner |
| [2026-09-03 does the errand walk](./2026-09-03-does-the-errand-walk.md) | [ADR 0093](../adr/0093-a-carry-is-an-action.md) was accepted and the mechanic landed the same morning, and `tests/foundation/job-production-contract.test.ts` gates it through `Kernel.submitCommand` — but **nobody had ever seen the errand happen through the user interface**, and the playtest that tried was cut off before its watch window. Played on `playtest/the-errand-walks`, cut from a fresh `origin/main` at v0.0.412 (`5a2756d1`), with every order placed through the Build panel's typed route and every room designated through the Rooms panel's. | None — reported; the only `src/` change is a corrected comment and no player-facing string was touched (`AGENTS.md`'s fourth exclusion). **The errand works when a player watches it, and the roster says "Errand".** Nine samples at 1x: the prisoner leaves the cell at (12,14), walks (10,16) → (8,14) to the bay, dwells `performing`, turns round with the bay emptied, walks (12,16) → (14,14) to the storeroom, dwells again, and `construction-materials` goes 15 → 25 — **114 ticks** from selection to `completed`, both legs walked in opposite directions, the bay's stock reserved at *selection* (`[10, 10]` with the prisoner still in the cell, which is decision 1's departure from ADR 0029 decision 2, observed). **The label landed**: `'action.carry': 'Errand'` is on `main` in `cd41a1d6`, so `docs/research/2026-09-03-what-the-owner-still-owes.md`'s *"Not landed"* was true only of v0.0.403 — and on screen it appears in **one** place and two forms, `Errand` while performing and **`Heading to Errand`** while travelling (4 of the 9 samples), which is a sentence and therefore the owner's. **Three things a player is told nothing about**: a delivery sat `available` in the bay for **394 ticks** waiting for a work block with `.hud__refusal` not laid out and `.hud-alerts` absent at every sample (the standing condition ADR 0093 decision 2 requires and its item 9 deliberately did not build); **there is no detail panel at all** — `hud/prisoner-detail` has a route and no reader — so `STATUS-QUEUE.md`'s *"the roster and the detail panel will say Errand"* is wrong about half of itself and the third owed sentence has nowhere to be said; and at **4x the whole 114-tick errand takes 1.4 wall-clock seconds**, which is why run 2 watched the same prison and saw nothing but a completed job. **Four in-tree statements assert the feature does not exist** and are fixed here: `procurement.ts`'s own header (*"the physical route in the middle … is not here"*, 380 lines above the `carryRoute` call), `new-session.ts` (*"no session instantiates that room"*, in the function that constructs `DeliveryBayCarryRoute`), and the `UNROUTED`/`UNGATED_BY_ANY_ACTION` reasons for both rooms and both capabilities — which `delivery-route.ts`'s header points at and calls *"that consumer"*. Also measured, from a prison walled wrong: a prisoner sealed out of their own cell stood on the arrival tile for **12,000 ticks**, never moved, failed the carry `unreachable`, and sat at hunger 0 for ~10,000 ticks with nothing on screen but a need bar at the floor. Weakest claim: the `Heading to Errand`/`Errand` split is four of nine samples on one geometry, and a bay twenty tiles from the storeroom would read almost entirely `Heading to Errand` |
| [2026-09-03 does building feel good](./2026-09-03-does-building-feel-good.md) | Building is this genre's primary verb and every earlier pass asked whether a drag *works*. Does performing it feel good — what is satisfying, what is fiddly, what is invisible, what actively misleads, and how many interactions is one usable cell? | **None as an ADR; ten findings ranked, each with its reproduction and the sample that could have refuted it.** **One usable cell is 19 interactions and it works first try** — 8 for the walls (four drags, all 16 perimeter segments), 5 for the room, 6 for a bed and a toilet, plus 6 transport presses; `rooms=1 roomCapacity=1 accommodationCapacity=1`, 1,385 spent of 25,000, exactly right. Nine of the nineteen go on the interface rather than the prison. **The machinery is honest and the telling of it is not:** the Build panel shows **zero digits** across all 21 catalogue rows in the state it arrives in — no price anywhere, in text or in any attribute — while the Security tab one tab over reads *"Hire Guard · 80 / Costs 80 now and 80 a day in wages"*; **18 of 21 rows are refused** on a fresh prison for want of a zoned room with nothing said in advance, while the Rooms tab pre-states *"NEEDS 1 × BED / NEEDS 1 × TOILET"* and afterwards names *"Cell at 13, 11 is missing 1 × Bed, 1 × Toilet"*; the placement hint reads *"Click a tile edge to place a wall"* for a Bed, a Toilet and a Storage Rack alike; the panel body holds **765px in a 437px box** with `overflow-y: visible`, so `ENTER COORDINATES` and the whole `QUEUED` section render **outside** it. **The first Undo of a session is a no-op that announces itself** — one `{"type":"Undo"}` out, treasury and queue unmoved across 500 ticks, all six orders built, and *"The last change to the build queue was undone."* on the band the whole time; the refused-Remove explanation is refuted on two prisons differing by one press. Three readings from earlier drafts are **withdrawn as artifacts of HUD occlusion** ([#878](https://github.com/matmaxalez/lockstate/issues/878)): a press on a covered point submits nothing at all, which reads exactly like a game ignoring it. Also records that **the game normalises a tile edge to the lower-numbered tile** — the south edge of (13,14) is `13,15 north` — which is what made an earlier act read a correct 16-segment perimeter as eight missing segments |
| [2026-09-03 can a player read this](./2026-09-03-can-a-player-read-this.md) | Can a person sitting down at this game work out what is happening to their prison, and what to do about it, from what is on screen? Not whether the text is grammatical — whether the numbers are labelled, whether a change is visible when it happens, whether a warning names an action, whether two things that mean different things look different, and whether anything on screen is quietly untrue. Answered **by playing** at 1440x900 through nine acts of `tests/browser/playtest-2026-09-03-the-whole-screen-at-once.playtest.ts`, none of them a CI gate. | **None. It reports, and every remedy it names is a player-facing sentence, which `AGENTS.md`'s fourth exclusion reserves to the owner — so each finding states what the words must convey and authors none.** **The one-paragraph answer: a player can read every number on this screen and cannot read their prison.** The labelling is good and the pass's own opening hypothesis is **refuted** — 158 of 163 visible number tokens name themselves one or two hops away, the one that does not is the clock's `×4`, and the boolean version of that probe reported 71 unlabelled numbers that were all an artifact of its own grouping walk stopping on `ui-value`. The urgent things also arrive where the eye already is: admitting six prisoners into a two-bed prison put `8` and a `6 with no bed` badge on the strip **while the player was on the Build tab**, reproduced in two sessions, and a 24,000 brick order put `-100` on the FUNDS chip while they were on Regime. What is missing is everything between a number and an emergency. **Four defects, ranked.** (1) Four figures about guards sit on one screen — `3 STAFF`, `6 · COVERAGE · Covered`, `GUARD COVERAGE 1 of 1 Covered`, `1 held · 2 free` — of which the two called coverage are different fields of different view models (`prisonersCovered` against `{assigned} of {required}` guards) rendering the same word out of one shared string set; and holding the population at six, **hiring a second guard moves neither**, measured `1 of 1 · Covered` at one guard and at three while `ON THE PAYROLL` goes `80 a day → 160 → 240`. The escalation this pass expected — that the two could print contradictory words during a guard's walk to post — is **refuted by 125 one-second samples taken in a single page evaluation**, all `same word`, with the `Unguarded → Covered` transition one sample wide. (2) The minimap panel's only sentence is `Minimap is not available yet`; **one press in its 224x224 surface and it becomes `No map is drawn here yet — click to jump the camera there`**, a swap gated on the navigate call returning true, so it is itself proof the camera moved — the only player ever corrected is the one who ignored the sentence, and for a keyboard player (`div`, `tabIndex -1`, no `role`, no `aria-label`, no `title`) the swap can never fire at all. (3) Between an empty prison and one running six prisoners and three guards at day 8, **52 of the Overview tab's 58 lines are word-for-word identical**, and every line that arrives is a number already on the always-present strip, plus the speed readout, a refusal from tick 0 and an autosave notice — **not one sentence about the prison**, with the Intake panel still showing the hint it carried when the prison was empty. (4) A Regime roster row is `Ines Ivanov / Idle / Hunger / Medium` with no heading over any column, and **this pass read the last two as a pair** — `Minimal/Low/Medium/High` is the risk tier, not a need level. Also: **#629 §1 is closed** — the clock reads `Day 1 / 0% / PAUSED` and the pressed transport button paints at luminance 0.4141 against 0 for its two neighbours, cross-checked against the worker's tick (`242 → 424` after Play, `1691 → 1691` after Pause); **#894 reproduces on day 7** with the same tick-0 sentence in the refusal band *and* the alerts list simultaneously; **all nine chips are inert** on a listener census installed before the app's first script, with four forced presses moving nothing but the clock; and the sentence explaining the overdraft badge's bare `1,150 left` is `title` plus a 1px `.ui-sr-only` span, so a sighted player who does not hover is told a number with no unit — recorded rather than filed, since the owner ruled on it on 2026-09-01. **One finding is confirmed and extended rather than new, and is the largest instance of its class anybody has measured**: run for the first time, the sibling pass's own `probeLegibility` reports the Build catalogue hiding **701px of 924px (76%)** and the Rooms catalogue **585px of 837px (70%)** at 1440x900 on a fresh prison, each with a **0.0px scrollbar gutter** and 36 and 29 elements laid out entirely outside the viewport — so roughly five of nineteen buildables and four of eighteen room types are on screen, **`Cell` below the fold on the tab whose purpose is designating one**, with no scrollbar, chevron or count anywhere. `2026-08-31-playing-the-twelve.md` §11 and `2026-09-01-playing-after-the-rulings.md` already carry the class for the save panel and the alerts list; `docs/HUD_PROJECTIONS.md` calls the rooms list *"the one box here that is meant to hold more than it shows"*, so the scrolling is deliberate and the gap is that it is invisible. **Weakest claim named in the record**, along with what would change it: §3's label, not its count |
| [2026-09-04 can this prison fail](./2026-09-04-can-this-prison-fail.md) | The owner suspended the unmet-need penalty on 2026-09-03 to judge ease by playing (*"usuń na razie kary, zobaczymy jak pogram i ocenię łatwość"*), so the open question was whether any pressure is left: **can a player lose this prison, and if so does the screen tell them it is happening?** Answered by playing three shapes from fresh prisons — neglectful, spendthrift, idle — with every figure read off the HUD rather than computed from source. | **None as an ADR; six findings, and the headline is a state the interface cannot leave.** **A prison can reach an unrecoverable floor, and the one sentence that names it tells the player money is on its way.** 60 guards and 288 bricks on day 1 pins the treasury at **−2,500** by day 4, unmoved across 12 samples and four paydays while arrears climb 4,800/day (3,220 → 27,220); every lever is at the floor simultaneously — buy and hire both `aria-disabled=true` (*"you need 1,355 more"*, *"you need 1,395 more"*), admit refused three times, dismiss unreachable — and income needs a bed, a bed needs a cell, a cell needs money. The FUNDS badge reads *"The treasury is exhausted — nothing can be spent at all **until the state pays what it owes**"*, and **the state owes that prison nothing**: `stateIncomeAccruedTodayMinorUnits` is `0` at all 29 samples. What the words must convey is that the floor was reached because the prison is not earning and what it would have to hold to be paid — **no replacement wording is authored here, that being the owner's alone**. Second: **the payroll cannot be stopped.** `ON THE PAYROLL — 4,800 a day` beside `0 held · 60 free`, while `.hud-staff__held-list` is *not laid out* — six rows exist in the DOM, three of them **enabled** `Dismiss` buttons, all six `visible=false`, because the block draws only *assigned* guards and a prison with no rooms has no post. Third: **neglect is free and worthless** — 24 prisoners, nothing built, ten days, `22,600 · FUNDS` and `0 · EARNED TODAY` at every reading, with `roomOccupants: 0` throughout; and four `Critical` riots on days 3, 5, 7 and 9 (*"24 prisoners have stopped taking orders"*) **every one self-resolved with `staff: 0` and no press**, no escape, no death, treasury untouched. **Two of the commissioning brief's premises are false and are corrected here**: the flat 300 is per **occupied place**-day and not per prisoner-day (`stateIncomeForOccupiedPlaces` folds over place ids), so a player reading the roster count is wrong by the whole amount; and the suspended penalty was never the main pressure on a neglectful prison, since it could only withhold a share of an income that neglect had already zeroed — what is actually missing is that neglect has **no downside at all**. The ruling itself is observed working: prisoners at `Safety · 0%` still paid the full 300 per place, +1,800 at three consecutive day boundaries. Weakest claim, named in the record: that the wall accepted at the floor was actually *built* — the queue read empty at run's end, which is equally consistent with a quiet failure, and a Remove-tool press at that edge would settle it |
| [2026-09-04 does anyone answer an incident](./2026-09-04-does-anyone-answer-an-incident.md) | `src/simulation/incidents/incident.ts:23-28` makes `'lapsed'` reachable from every non-terminal state, `'responding'` included, so an incident can end with nobody having done anything. **Does that happen in ordinary play, how often, what does it look like on screen, and is a player told which of the two endings they got?** Played at v0.0.439 (`ce7fe617`) across three acts differing in one variable — six guards, no guards, and a mid-incident Release — with the HUD channel (read out of the DOM) and the STATE channel (read off the worker's projections) reported separately and **never mixed**. | **None as an ADR; the mechanism works and the telling of it does not.** **A guard does answer, reliably and in under a second — and the player is told nothing that separates the two endings.** Six guards: **15 of 15 incidents ended `resolved`, zero injuries**. No guards: **19 of 19 ended `lapsed`, 114 prisoner-injuries and three escapes**. **Both prisons' alert columns hold the same rows, in the same order, saying the same sentence** — a lapse with two prisoners injured is announced by *"The prison is under control again — no incident is still open."*, and the Incidents chip reads `0 INCIDENTS Clear` either way. **The only observable difference is a duration and no surface names it**: a resolved incident closes ~70 ticks after it opens, a lapsed one at `responseDeadlineTicks` plus one interval — **exactly 610 ticks in all nineteen of act 2's lapses** — so the gap between the "opened" and "all clear" rows is ~0.9s against ~7.6s at 4x, and a player who looks away for one second sees neither end of it. The escape is the exception and works exactly as #683 says, because `lapse` is the only route to `escaped: true`. **The commissioning brief's *"a guard who was on the way and then wasn't"* is real and reachable**: pressing Release on the two held responders of a `'notified'` assault gave `active@10350 → notified@10350 → lapsed@10960` with two injured — but **`'responding' → 'lapsed'` specifically was not reached in 39 incidents**, for a structural reason the note gives: the `'responding'` branch never checks the deadline, so an incident whose response record survives to arrival always resolves. Also established, and re-verified independently by the integrator before merge: **nothing under `src/ui/` or `src/main.ts` requests `hud/incidents` or `hud/incident-detail`** — both are declared at `protocol/types.ts:344-345` and built at `projection-catalog.ts:417,435` and consumed by nobody, so **no surface on a player's screen can be asked which ending an incident got**. Weakest claim named in the record: the DOM read runs a few hundred milliseconds after the projection read, so a screen sample is paired with a transition rather than timestamped against it — which is why only *terminal* pairings are quoted |
| [2026-09-04 what the game shows nobody](./2026-09-04-what-the-game-shows-nobody.md) | Two read models were found producer-only by accident in two days — `staffUnassigned` (#870) and `hud/incidents`/`hud/incident-detail`, whose absence is why a handled incident and an expired one say the same sentence. **Which read models and protocol fields does this game compute, publish, and then show to nobody?** A mechanical census of every `PROJECTION_CATALOG` entry, every `statusCountsSchema` field, every declared member of the 52 view-model interfaces, and both message-kind directions. | **None as an ADR; a census, and the sharpest orphans are not where anybody was looking.** **10 of 15 projections consumed, 18 of 23 status-count fields, and 223 of 398 view-model members** — the 223 stated as a **ceiling**, because absence was measured mechanically and presence partly by bare-word grep, which over-reported twice in the run itself (`PrisonerDetailViewModel.sentence` matched the English word in 43 files; `ClockViewModel.dayProgress` matched a local DOM variable). **The highest-value orphans live INSIDE painted projections, where the gate correctly reports the route painted**: `hud/staff`'s `deploymentMetrics.deploymentFailures` and `patrolMetrics.loopsMissed` cross the boundary on every request and are discarded, and they answer exactly what ADR 0036 open question 1 says *"Nothing tells the player"* about. **Ranked first, though, is `statusCountsSchema.conditions`**: ADR 0087's four-member standing-conditions union, recomputed every publication (`status-strip-projection.ts:833`), emitted (`:908`) and **diffed field-by-field for change detection** (`worker/status-counts.ts:135`), read by nothing — its stated deliverable, a `Record<PrisonCondition, LocalizationKey>` in `src/ui/simulation-alerts.ts`, was never built. And `src/ui/simulation-events.ts:867-872` defers the durable figure to it in terms, saying those conditions *"exist to keep answering after this notice has scrolled away"* — **verified by the integrator: nothing in `src/ui/` or `src/main.ts` reads that field, so the transient notice defers to a channel that does not exist and the figure a player would want is simply gone.** Also: `hud/security` (65 members incl. `controlState` and the whole `accessPolicy`), `hud/contraband` (72, incl. `metrics.itemsMissed`, the only place the game says what it MISSED), and `prisonersInIntake` — which `src/main.ts:2828` **mis-attributes**, the arrivals readout actually being built from `hud/prisoner-population.byIntakeStage`. **The commissioning brief's premise that a gate had a hole is refuted**: `projection-reachability-contract.test.ts` keeps a deliberate, narrower promise — *"every unpainted route says why"* — policed in both directions, and both incidents ids are declared exemptions with stated blockers. **All five blockers still hold** (re-verified), but **three carry stale line citations** (`HUD_PROJECTIONS.md:1130-1133` and `:1117-1120` now land on ADR 0028 room prose; ADR 0036's claim is at `:508`, not `:490`) and nothing polices them, because those anchors sit inside a TypeScript string. **No gate covers producer-only fields at all** — the nine-file reachability family all enumerate closed vocabularies of *ids*, and `statusCountsSchema`'s `.strict()` polices producer-to-schema while schema-to-consumer is silent by construction: a coverage gap rather than a defect. `staffUnassigned` ranks only sixth, because a free-guard count IS painted from `hud/held-guards.totals.unassigned` under a **different definition**, so wiring it without settling #870 would put two disagreeing numbers on one screen |
| [2026-09-04 the first ten minutes](./2026-09-04-the-first-ten-minutes.md) | The owner's standing design directive is *"gra ma być łatwa przyjazna do grania, a nie jakieś ukryte funkcje"*, so the question is not whether the game can be played — earlier passes settled that — but **whether a newcomer is led or left to guess.** Played twice at v0.0.442 (`3d2a8bda`): once naively, using only what the screen says, and once already knowing the order. The gap between those two runs is the finding. | **None as an ADR; five findings ranked, and the verdict is more favourable than the brief or this repository's own record assumed.** **A newcomer IS led — but only ever retrospectively, by refusals — and the naive/informed gap is not effort but outcome and money**: both runs took **33 interactions**, and the naive one ended at `roomCapacity 0 / roomOccupants 0`, **0 earned**, **1,815 poorer** (20,680 against 22,495), holding a prisoner with nowhere to sleep. **The one true dead end is a stopped clock**: 24 orders placed, 1,920 already spent, `24 waiting · 0 being built`, tick `-1` for 21 seconds, and `/clock/i` matches nothing in the whole laid-out HUD — one press of Play builds it. **The sentence that would say so is shipped, keyed, ruled on by the owner on 2026-08-30 (#639), and rendered by nothing** — `'hud.build.note': 'An order is queued now and built while the clock runs.'` at `default-locale-en.ts:1040`, keyed at `messages.ts:345`, with the only other mention in `src/ui/` being the docblock at `build-panel.ts:1207-1230` that admits it and dates the loss to `67e366e` (2026-08-23), when a refactor deleted the `.hud-build__footer` it shared with the submit button. **Verified independently by the integrator.** Second: *"enclosed"* is never turned into *"wall"* — at the enclosure refusal the string `wall` appears nowhere on screen. Two new findings: **the Rooms catalogue arrives selected on Staff Room** (`rooms-panel.ts:444`), so the requirement block — the best guidance in the game — teaches *a desk and two chairs, 3×3* to a player whose only instruction said "cell"; and **two tabs disagree about a bed**, Intake saying *"It does not need a free bed"* while Regime says *"Build a cell with a bed to take somebody in"*, both true of the code, with the actionable one on the tab a newcomer least opens. **Four of the brief's premises are refuted with numbers**: the materials dead end is gone (wall orders buy their own bricks, 25,000 → 23,080, and the purchase is now stated **unfolded**); the 18-of-21 refused build rows never come up, because a wall needs no room and is the first row the panel hands you; **`Cell` is NOT below the fold on the Rooms tab at 1440x900** (5th of 18, fully visible — #902's ratio confirmed, that consequence not); and an earlier note's stale-band finding is **half fixed in both directions** — a success at the same rectangle clears the band, a refusal about a different action survived 30 interactions and ~10,000 ticks, which is exactly what the keyed supersede predicts and explains why two earlier notes disagreed. **Two instrument failures recorded rather than hidden**: a queue poll matching only `0 waiting · 0 being built` mistook an absent block for a non-empty queue and printed 23,417 ticks where the correct figure is **495** (that run discarded); and `innerText` does **not** respect a scroll container's clipping (21 labels dumped against `fullyVisible: 5`), so every guidance reading in the note is an upper bound. Weakest claim, named: the naive run is a *model* of a newcomer that makes the "enclosed → wall" leap instantly, so it shows the recovery is short and cannot show that a person would find it |
| [2026-09-04 does a prison come back](./2026-09-04-does-a-prison-come-back.md) | Determinism and the save format are heavily tested and the schema is at version 5, but **every existing check sits below the UI** — nobody had watched a save and a reload happen as a player experiences them. Played at v0.0.443 (`2732e81e`), with the HUD channel (read out of the DOM) and the STATE channel (read off the worker and the save payload) reported separately and **never mixed**. | **None as an ADR; eight findings, and the round trip itself is exonerated.** **The prison comes back exactly** — kernel tick **17690 in, 17690 out**, every roster row, name, need percentage and strip figure identical, and the load's restore report accurate; 6 of 12 differing HUD lines are all explained (two known, three main-thread UI state, one a *stale pre-save* counts publication where the restore was the **more** accurate reading, one layout reflow). **What is not honest is the word "Saved".** The autosave is scheduled **only by an accepted command** — `commandSender?.onCommandAccepted(() => controller.markDirty())` at `src/main.ts:3226`, whose callback fires only on `status === 'queued'` (`simulation-commands.ts:347`), **both verified independently by the integrator** — so a prison that is merely *watched* is never saved while the panel keeps reading `Saved (generation N)`: **80 s of running clock, nothing pressed, 1,626 ticks, zero saves**, and a reload in that state **lost 1,983 ticks** with `Saved (gen-…-2)` as the last thing the panel said. `pagehide`'s best-effort save did not land. **Two other presses destroy work with no warning at all**: Load discards unsaved play (2,461 ticks, **0 dialogs**, verdict *"Loaded"*), and **Delete leaves a live, playable, unsaveable prison on screen** — no confirmation before, no notice after, the strip still painting it, **Play still advancing the kernel** (1063 → 1140) and **Admit still enabled and submitting**, refused with a real sentence, while the panel says *"No active prison — create or load one first."* The panel also contradicts itself: status `Saved (generation gen-…-9)` beside list `New Prison (1 gen)`, because `reportBackgroundSave` sets the status and never calls `refresh()`. **The mid-incident restore window is real, visible and actionable** — saved at tick 15434 with an assault `responding`, the incident returns mid-response and the response does not: two guards `'on-search'`/`'unattributed'`, the Staff panel reading **`Guard · Unclaimed`** with a live unobstructed Release button (rectangle proved clear via `elementFromPoint`, and a Release press inside the window landed, 3 held → 2). **Width ≤12 ticks observed, 6 derived** (0.3 s at 1x) — **but unbounded in wall clock, because a Load leaves the session paused**, with 13 consecutive samples all reading tick 15434. Its close is a *re-dispatch* (`respondersDispatched` 2 → 4) and **nothing on any surface records that the first response ever existed**. **Three of the commissioning brief's premises are false**: there is **no autosave notice anywhere** — a full text-node sweep found exactly two mentions of saving on the whole page, the Save button and the status line, so **the game autosaves and never says that it does**, with the status sentence identical whether the player pressed Save or a background save fired; loading an older save over a newer prison is **not a player-reachable gesture** (`loadPrison` walks `loadCurrent`, and there is one Load button for three retained generations, which is what makes the label `(3 gen)` misleading); and the window is **not** interface-silent — `Unclaimed` is a deliberate, correct word with its own docblock naming this exact case, so the dishonest part lies elsewhere. Weakest claim, named: the Release press landed on a `Sector Post` row, not an `Unclaimed` one, so *"the panel accepts a Release during the window"* is measured while *"a Release aimed at an Unclaimed guard succeeds"* is UNKNOWN |
| [2026-09-04 is there a way back](./2026-09-04-is-there-a-way-back.md) | The owner ruled on 2026-09-04 *"Zawsze musi istnieć droga powrotu"*, and [ADR 0096](../adr/0096-what-a-way-back-is-and-what-guarantees-one.md) reads that sentence as an **economic** guarantee. **Does the game keep it in the other sense** — when a player wants to take back the thing they just did, can they? Played in a real browser rather than read off the undo stack. | **None as an ADR; two issues filed, and the brief's central premise refuted.** **Ways back are not absent — they are priced, silent, keyboard-only or folded.** A delivery in flight has an exemplary one (`1 bought · 400 back if cancelled`, cancelled, treasury back to 25,000 exactly), and a queued order refunds +80 exactly as its row advertises. **Two findings became issues.** #927: `Z` on a *finished* wall destroys the materials and says only *"The last change to the build queue was undone."* — while the sentence for that loss is **already shipped and owner-approved** (`default-locale-en.ts:881`), withheld on two premises that are both dead. The owner withdrew *"the materials went into the container (ADR 0076 decision B)"* on 2026-09-01 and `construction/system.ts:740-748` already follows the withdrawal in its own words; *"no control can reach that press"* is refuted by `undo()`'s own comment, *"including a `completed` one"* (`system.ts:568-575`). The cause is channel-level: `CancelBuildOrder` records `recordBuildOrderCancelled(stateAtCancellation, …)` and picks its sentence from the state (`handler.ts:156`), while `Undo` records `recordConstructionUndone(tick)`, blind to what it destroyed (`:174`) — **all four citations opened by the integrator.** #928: that same press is the **only** route to a finished wall, so on a touch device it is permanent — six armed `Remove` presses on the tiles a standing wall bounds, each proved to land on `canvas`, each answered *"there is no object on that tile"*, and `RemoveObject` is the **only** removal command in the protocol (no `RemoveWall` exists), which makes `construction-projection.ts:82-93`'s hand-off — *"taking a finished wall down is the Remove gesture's job"* — false, and reopens `AGENTS.md` boundary 10 for walls after ADR 0028 phase 3 closed it for objects and called it *"not worth repeating"*. Also: `Z` after a hire keeps the hire and takes the last wall down instead, against two live ADR sentences saying Undo *"reaches nothing"* for a non-build action; `Escape` leaves the tool armed and says nothing (reported at low weight — `input.action.build.cancel` is scoped to a wall run and promises no more); and there is **no stock readout anywhere**, which is what makes the destroyed materials invisible rather than merely unrefunded. **ADR 0096's own §4 claim is confirmed by playing** — a sweep of five tabs, 109 controls each, found 0 controls and 0 text nodes for sell/refund/return/buy-back on landed stock. **Three brief premises refuted**, including two operational traps: tile (12,12) **is** on screen at 1440x900 (centre 496,226, `elementFromPoint` → `canvas`), and at zero lead with the clock paused every press landed on the state its row advertised. Weakest claim, named: §5 reads the folded Cancel as a way-back gap where it may be a height-budget trade the repository made knowingly, so the geometry is measured and the reading is handed on. One reading was **withdrawn and kept rather than deleted** — the Rooms enclosure line reads the same before zoning, so it proves nothing. Four instrument failures recorded, one of which found §5's fold |
| [2026-09-04 is there anything to do](./2026-09-04-is-there-anything-to-do.md) | Earlier passes settled that the game can be played, that a newcomer can be led to a cell, that a prison can fail and that a save comes back. **Nobody had asked the next question: once the first cell works and the first prisoner sleeps in it, is there anything to do?** The owner's mandate says the game must be *fun and great, not done on the cheap*, and `AGENTS.md` makes playability a correctness question. | **None as an ADR; twelve findings, the commissioning hypothesis refuted on one half and sharpened on the other, and one live defect that silently breaks the core loop.** **The mid-game is not thin on the asking**: eight admissions took `prisonersCovered` 4→0 and `prisonersUnderstaffed` 0→12 in one step, the strip read `COVERAGE 0 · Understaffed`, Security named the remedy with a figure (*"Hire 1 more to cover this population."*), and ignoring it produced **five assault/lapse pairs over ~5 in-game days** — against a refuting sample of the identical prison at four prisoners running **8,271 ticks with zero events**. **It is thin on the answering, and the reason is arithmetic rather than taste**: 300 per occupied place-day (`income.ts:115`) against a guard's 80/day covering 8 (`staff-role-catalog.ts:150`, `sector-staffing.ts:147`) makes full staffing **3.3%** of the income it protects, wages are the only recurring cost of four `SpendClass` members, and the treasury rose monotonically in every measurement **including through five uncontained assaults** (27,460→43,470). **The load-bearing defect: a second room does not work without a door, and every readout says it does.** A 3×3 shower room, 12 wall segments, designated and accepted on attempt 1, two shower heads placed — of 21 published counts **one a player can see moved, `ROOMS` 1→2** — and hygiene went 8–11% to **0%** on all four prisoners with **no room-targeted action ever appearing on the roster across five acts**. The same prison with **one wooden door per room** reached **98% / 100% / 96%** with the roster showing `Showering`, replicated in three runs. `traversal.ts:23-36` is explicit — *"any non-zero value with no registered door is an impassable wall"* — and `enclosure.ts:70-78` says in its own words that **a `'sealed'` answer no longer implies "no way in"**, while the one string a player gets is `'Walled in on every side'` (`default-locale-en.ts:1792`), **identical in both prisons**; the panel's only completion warning fires on a missing *object*, never on a missing way in. **And the diagnostic for exactly that state exists and is published to nobody**: `ActionMetrics.unmetDemandCycles`, *"cycles where no legal action had a reachable, available target"* (`action-system.ts:104-110`), has no projection at all — so it is in **none** of the three sets #918's census enumerates, checked against that note first. Also: **no reason to build a second cell** (`deriveRoomCapacity` sums footprint width with no per-room ceiling and no tiles-per-occupant term, so 12 beds in one 6×6 gave capacity 12 with `rooms` still 1); **room requirements are advisory** — a `room.cell` with zero beds and zero toilets was accepted on attempt 1 and took the badge 1→2, and objects can only be placed *inside* an already-zoned room, so `NEEDS 1 × BED` can never be satisfied at designation time; **fifteen verbs and not one sets a policy**, the Regime panel contributing **0 of 116** HUD controls while visibly deciding behaviour; **the map is one 32×32 chunk** with no command to add another, so `zone.unowned-land` is a sentence about land a player can never own; **seven of eighteen room types have no reference in `src/` outside the catalogue**; and **nothing a player does right is ever acknowledged** — all 18 `SIMULATION_EVENT_TYPES` are bad news, undo, or recovery from bad news, with **zero events across 28,726 ticks in the best-run prison in the record**, read with a *fixed* reader. **The best news: when the shower room worked, the roster's lowest-need column re-pointed itself off Hygiene onto Recreation** and fell 89→30 / 74→31 / 76→33% over four days in three runs — a working to-do list, whose two costs are a four-row cap and that nothing ever converts a need's name into a room's name. **Nothing states what a player is trying to achieve**: zero locale matches for goal/objective/milestone/unlock. Weakest claim, named: the step from 3.3% to *"no choice is being made"* is a judgement, and a fifth `SpendClass` member would refute it. **Seven instrument failures recorded**, two of which had produced false readings that were caught and discarded — an event reader filtered on `'simulation/events'` where the kind is singular, and a `designate` helper that accepted `rooms > 0` and so called a *refused* yard "accepted on attempt 1" |
| [2026-09-04 a tablet and nothing else](./2026-09-04-touch-only.md) | `AGENTS.md` boundary 10 says touch is supported and the README's product targets say so too, but nobody had played the game with **no keyboard and no mouse at all**. **How far into Lockstate does a tablet player get?** Played end to end at v0.0.451 (`0e614c7`) at **1024×768 landscape and 768×1024 portrait** — one device rotated, so every difference is the orientation — with `hasTouch: true`, `page.touchscreen` for taps and CDP `Input.dispatchTouchEvent` for every drag and multi-finger gesture, and **no `page.mouse` or `page.keyboard` anywhere**, because `playtest-harness.ts`'s helpers are all mouse-driven and `locator.click()` is a mouse click even under `hasTouch`. | **None as an ADR; ten findings, and the shape of the answer is that a tablet player can do everything and undo almost nothing.** **The whole game came out at BOTH orientations with nothing but fingers** — new prison, materials, clock, four wall drags, a room designated on the first attempt, beds, a guard, and a prisoner housed in a bed (`roomOccupants=1`, not merely `prisoners=1`) — at the price of four finger drags on catalogue lists at landscape and two at portrait, and with the drawable cell being **4×4 at landscape against 6×6 at portrait**. **The gesture layer is the strongest part of the game and is exonerated with numbers**: a one-finger pan delivers exactly what it is asked for (+4 tiles for 256px) *including when the finger ends on the HUD rail* (−5 of −6) *or on the status strip* (+3 of +3.3), so **#878's truncation really is mouse-only, confirmed independently on touch**; a one-finger drag while armed builds (4 wall orders) and **two fingers pan the camera while the wall tool is armed submitting 0 build commands**; a pinch reaches **both** `ZOOM_BOUNDS` ends by finger (~240px/tile clamped out, ~13px/tile clamped in against the 12.8px the 0.2 minimum predicts); the minimap takes a tap (`hud.ts:1513` is a `click` listener); and across **all five tabs at both viewports not one visible control is below the 44px tap target**. **What fails is everything around the gesture.** The Build catalogue at landscape is a **124px box on 924px of rows — 2 of 21 rows tappable (`wall-brick`, `door-wooden`)**, 88px once the Buy disclosure is open, and the Rooms catalogue a **153px box on 837px — 3 of 18, `room.cell` not among them**; portrait gives 4 of 21 and 8 of 18 with `Cell` included, and drops to 2 rows the moment materials are bought. That is **#902's ratio at the viewport #902 did not measure** (its corrected figures are 5 of 21 and 5 of 18 at 1440×900, with `Cell` the fifth and last wholly-visible row) — and **the fix that closed #902 was withdrawn before merge** (`13c50289`), so `grep background-attachment src/ui/hud/hud.css` is empty at v0.0.451 and nothing paints an edge sign; on a tablet #902's own concession about overlay scrollbars (*"no width … paint only during a scroll the player has no reason to start"*) means the originally-filed condition is this device's real one. **A tap where a hidden row's coordinates sit lands on a live control**, which is how act 4 died twice — `bed-wooden` → `button.ui-action hud-build__remove`, `wall-brick` → `div.save-panel__actions` and → `select.hud-build__category` — 22 such controls on the landscape Build tab, 20 on Rooms, 0 on Overview and Regime. **On three of five tabs the save panel's Export, Import, Load and Delete are all outside its own 87px box** (240px of content), none of them on Overview or Regime. **Six controls on the arrival screen carry their only words in a hover tooltip** — `Pause`, `Play at normal speed`, `Fast forward`, two `Collapse` chevrons and `Change the interface scale`, all with `paintedText: ""` once `.ui-sr-only` is stripped — and `src/ui/primitives/action-button.ts:8-10` states the rule they break in terms: *"Touch has no hover, so a tooltip is not a label."* The clock is the one dead end this project already measured, and on a tablet its three buttons are unlabelled glyphs. **The one sentence teaching the world tool is written for the wrong devices**: `hud.build.arm-hint` says *"**Click** a tile edge … Two fingers, **the middle button** or **the arrow keys** still move the camera"* — one of three camera routes exists here — and `hud.rooms.arm-hint` names no camera route at all, on the tab where a **6×6 canteen** (`room-catalog.ts:124`) must be drawn while the largest cell a finger can draw in one camera position at landscape is **5×5** (92 reachable tiles, reproducing #899's 92/192 to the tile, but a wall run aims at a tile *edge*, so the usable figure is smaller than the reachable one). **The WHERE readout says `Point at the world` until the finger is already down** — `world-scene.ts:583-588` says touch never reaches the hover branch — and a run extends from where the finger landed, so reading it too late cannot be corrected. Weakest claim, named in the record: that the unreachable rows are a trap a *person* falls into rather than a tester's artefact, since a player aims at what is painted; the geometry survives either way |
| [2026-09-04 can I see my prison](./2026-09-04-can-i-see-my-prison.md) | Every earlier pass read the game out of `innerText`; nobody had looked at the pixels. **A player builds a prison, sets it running, and then just watches it — looking at the world rather than the panels, can they tell what is going on?** Played with a mouse at v0.0.451 (`0e614c7`) across four acts, with every claim about the screen made from a saved PNG that was actually opened, and every claim about the simulation made from the render-actors keyframe decoded off the boundary. LFS verified first (`PNG image data`, not `ASCII text`), because a worktree without it draws a world with no sprites and passes anyway. | **None as an ADR; nine findings, and the split is clean: the building reads, the prison does not.** **28 actors, two tiles, two figures.** With 22 prisoners and 6 guards alive, the worker put 6 prisoners on tile (12,12) and 16 prisoners plus all 6 guards on (16,16), and the canvas drew **exactly two figures** — one orange, one blue — while the rail announced *"A riot has broken out — 22 prisoners have stopped taking orders"* twice (`incidents.riot-opened`, `participantCount: 22`, ticks 18150 and 25450) and the event log carried 14 messages the world marked in no way at all. Co-located actors get the **identical** depth (`actor-layer.ts:146` over `depth.ts:14-31`), Phaser's sort is stable and prisoners precede guards in the array by construction, so on a shared tile the guard always wins and the prisoners are always the ones hidden. **Nothing walks, measured on the render channel for both populations**: across **400 consecutive keyframes** spanning 3,533 ticks, every velocity was zero, every position an exact integer tile, and `furthest any actor ever got` is `0.00` or `5.66` **and nothing in between** — so the six moves observed were teleports, and the eight-frame walk cycle that exists in both atlases can never be selected. That confirms **#740** visually and on the wire and widens it to prisoners, without contradicting **ADR 0088**, whose own status section warns that a guard hired into an early-game prison has no distance to cross. Three building states draw nothing or next to nothing: a queued wall is a 0.35-alpha brown slab on brown dirt (`PLANNED_ALPHA`, `appearance.ts:276`); a queued **bed inside a cell is invisible**, because `PLANNED_OBJECT_TINT` is `CATEGORY_FALLBACK.object.topFill` (blue-grey, `:225`) and the `housing` zoning tint is blue (`:85`); and **the room rectangle a player has just dragged is cleared on mouse-up** (`world-scene.ts:998`), so the state they must confirm in exists only as a size in a panel. On the credit side, ADR 0052's art works: walls, doors and zoned floors are unmistakable at 1x and good at 8x, the owned plot outlines and shades correctly when you stand back, and at true in-game size (64x96 on a 64px tile) an orange prisoner and a capped blue guard are told apart instantly. **Marked separately as "the art is not done yet"**: all twenty catalogued objects are on the colour fallback (`environment-art.ts:146-167`, `SPRITE_BY_OBJECT_ID` is `{}`), so a bed, a chair, a desk, a stove and a toilet are one slate-blue block differing only in footprint; and the zoning tint is keyed by **category**, 11 tints over 18 room types, so seven groups of rooms — cell/holding-cell, kitchen/canteen, shower/laundry, yard/common-room, the three logistics rooms, solitary/security-office — are the same colour on the world. **Two instrument failures are recorded rather than hidden**: a queue readout answering `"not laid out"` was taken for an empty queue and produced two byte-identical screenshots that could not distinguish *"a built wall looks like a planned one"* from *"the wall was never built"* (the act was rewritten to a tick-stamped sequence); and the actor tee read a guessed envelope path (`payload.payload.body` instead of `payload.delta.data`, `protocol/types.ts:538-559`), which threw nothing and made a prison of nine actors report *"the worker has published no actor keyframe at all"* — that run is kept at `act2-broken-tee.log`. The ordering reading is corroborated by a natural experiment inside act 3: tile (12,12) held the same six prisoners throughout, drew an **orange prisoner** while no guard shared it (`30-watching-t0.png`), and drew a **blue guard** in the very next sample after one did (`30-watching-t2.png`). Weakest claim, named: *"the move is a teleport"* is an argument from an absence over one 3,533-tick window in one prison shape, and one keyframe anywhere carrying a non-zero velocity or a sub-tile position would overturn it |
| [2026-09-04 hour two](./2026-09-04-hour-two.md) | Nearly every playtest here stops around the first prisoner; this one starts there. A prison grown to 17-30 residents and run across seventeen in-game days: is there anything to do, does it hold, and does it get better or worse? | None yet — it reports one defect (a run of presses on a 4× clock loses some of them to a `past-tick` rejection that desynchronises the command sequence, 7 of 25 admissions and 5 of 8 hires, answered by a refusal that does not say to try again), one panel that states a guard count at which every incident lapses and calls it covered, and the numbers behind an inversion of [#503](https://github.com/matmaxalez/lockstate/issues/503): the grant no longer erodes, the roster still reads `Hunger 0%`, and the treasury credits the same figure at seven consecutive day boundaries |
| [2026-09-04 the misplay](./2026-09-04-the-misplay.md) | **Every mistake a real player makes in the first half hour, made deliberately, and the route out of each one measured.** Two dated records had already played the *finished* wall (#927, #928) and priced every cancellable order state against the execution tick (#853); nobody had sat down and made *all* the mistakes in one session to see the recovery surface whole. Five acts at v0.0.451 (`0e614c7`): a queued run on the wrong line, the impatient double-press on six controls, a room zoned wrong four ways, sixty bricks instead of six, and a hire and an admission then unwanted. Sentences read layout-aware off `.hud`; every figure off the worker tee; paused unless an act says otherwise, so no refund figure is a lead artefact. | **None as an ADR; nine findings, and the headline is that the ways back that *pay* speak and the ways back that *destroy* are silent.** Six walls on the wrong line returned **480 of 480** one press at a time, each answered *"The order was cancelled — the money it cost is refunded."*; sixty bricks returned **2,400 of 2,400**, answered *"The delivery was cancelled — 2400 back."* Against that: removing a mis-placed room moved no number and put **no sentence on the screen at all** (`event: "(not laid out)"`); removing a standing bed destroyed the **65** it cost, `24,935 → 24,935`, and said nothing; dismissing an unwanted guard kept the **80** already paid and said nothing. The mechanism is one line — `grep -n "events\.record" src/simulation/runtime/session-commands.ts` returns **exactly one hit, `:515`, the delivery cancellation** — so `UnzoneRoom`, `RemoveObject`, `DismissStaff`, `HireStaff`, `AdmitPrisoner`, `PlaceObject`, `ZoneRoom`, `PurchaseMaterials` and `ReleaseGuardAssignment` are silent on success by construction, and **the standing-object case is on neither channel #927/PR #932 fixes**: `ObjectPlacementService.remove` takes a *pending* order through `cancelOrder` (`object-placement-service.ts:608`) and a *standing* object through `placedObjects.remove` (`:586`), which never reaches it. **#780 confirmed live and worse than filed**: one refusal from a missed `Remove` press survived the entire session — six successful cancellations, a hire, an admission, five tab switches, 5,980 ticks — and in act 3 *"it overlaps a room that is already there"* was still the only sentence on screen **after the player removed that very room**, the recovery path contradicted at the moment it succeeded. **An admitted prisoner has no player route back at all**: 141 visible controls across the five tabs, **zero** naming one, and the fifteen `z.literal` commands in the protocol contain no inverse of `AdmitPrisoner`. **The route back from an unwanted hire is behind a fold shut on arrival** (`staff-panel.ts:1389`), so the screen right after the mistake reads `ON THE PAYROLL / 80 a day` with three roster rows in the DOM and **none laid out** — #862's argument in the panel it was not filed about. **Un-zoning orphans the furniture**: a bed inside a removed yard stayed standing on bare ground, appears on no panel, and was proved present-then-gone by the refusal changing from *"something is already standing there"* to *"it has to stand in a room you have zoned"*. **The double-press splits four/two** — the same wall drag, object tile, Admit and Designate are each refused with a named reason and cost nothing; **Buy buys twice (−400 for 10 bricks) and Hire hires twice (−160, `staff 0 → 2`)** with no confirm, while the *dismissal* is a two-press confirm, so the game asks "are you sure" about the reversible press and not the unreversible one. **And a large refutation, backed by numbers:** a 2×2 cell is stopped *before* the press — `Designate 2 × 2` `disabled=true` with its own note *"TOO SMALL — THIS ROOM NEEDS AT LEAST 2 × 3 TILES."* — `Remove rooms` is a visible peer control on arrival exactly as `rooms-panel.ts:996` promises, and **#772's behavioural half is shipped**: the Buy control goes `aria-disabled=true` with *"Not enough money — you need 13,815 more."*, a figure that looks wrong and is exactly right (spendable is `balance − overdraftFloor`, `affordability.ts:298`, and the starter deliveries floor is −1,185 from `treasury.ts:354`+`:504`). One new small false claim: the refund confirmation renders `2400 back` where the row renders `2,400 back`, against `simulation-events.ts:869-876`'s own docblock promising *"the two render alike"*. Weakest claim, named: that the three silences are silences a player would *notice* rather than silences that are correct — the standing-object case is the one defended under pressure, because 65 is destroyed with no figure anywhere on the page and PR #932 will not reach it |
| [2026-09-04 many prisons](./2026-09-04-many-prisons.md) | `docs/research/2026-09-04-does-a-prison-come-back.md` played the save and reload of **one** prison and stopped there, while the README's product targets promise *"multiple prisons per account; five free save slots"*. So: **a player who already has one prison wants a second** — make several, switch between them, come back, name one, run past five, delete one, and find out what a player can see and do about having more than one. Played at v0.0.451 (`0e614c7`) in six acts, HUD and STATE reported separately and never mixed. | **None as an ADR; seven findings, and the split is sharp: switching works and choosing does not.** **A prison switched away from and returned to comes back exact** — kernel tick **13622 in, 13622 out**, funds 23,015, 1 room, 4 prisoners with the same names and the same 46% bladders, 2 guards, day 6 at 67%, **and the same tab** — with only the two differences the previous record already explained (speed 4× → 1×, ADR 0051; a stale pre-switch `EARNED TODAY` where the restore is the *more* accurate reading). Everything around that is unlit. **The press that makes the second prison silently destroys the first one's unsaved play**: prison A at tick **211** with **1,600** spent came back at tick **0** with **25,000**, **0 dialogs** of any kind (DOM *and* native `confirm` both counted, because Playwright auto-dismisses an unlistened one) — `createPrison` calls `host.startNew` and `adoptSession` without capturing the outgoing session (`session-controller.ts:166-167`), and `WorkerPerSessionHost` terminates it (`main.ts:3164-3166`). That is a **third** button with the property the previous record found on Load, and the worst-placed one: the only gesture the game has for *keeping* a second prison is the one that discards progress in the first. **Six prisons produce six rows reading the identical string `New Prison (1 gen)`**, and three prisons in measurably different states (funds 24,600 / 23,800 / 22,800) gave **1 distinct row label of 3** — the name is the literal `'New Prison'` at `save-panel.ts:726`, and the page has **no way to set one**: `inputsInPanel: 0`, no `contenteditable`, and the words *rename* and *name* match nowhere in the page text. The row's only varying field, the generation count, is the retention window rather than the play **and is stale**: status `Saved (generation gen-…-4)` beside a row still reading `(1 gen)` until *Save now* refreshed it to `(3 gen)` with no further play. **The only thing marking the prison you are in is font weight and colour** (`700`/`rgb(134,178,207)` vs `400`/`rgb(219,226,233)`, `styles.css:148`) — `data-active` is not an ARIA attribute, so every row's accessible name is the same string. **The list order does not mean what its own docblock says**: `orderPrisonsForDisplay` is documented *"most recently played first"* (`save-panel.ts:456`) and sorts on `updatedAt` (`:473-475`), which a load deliberately does not bump (`repository.ts:721-723`) — MEASURED, after switching back, the day-6 prison being played sat **below** an untouched one. **Delete asks nothing and then names nothing**: 0 dialogs, *"Prison deleted."*, and the two survivors read exactly what the deleted row read, with no undo, tombstone or recycle control anywhere on the panel — #582's LS-03 knew the missing dialog *from reading*; what is new is that a player cannot find out afterwards which prison went. **The five-slot figure is not a promise this build breaks**: six presses gave six prisons and the words *slot*, *limit* and *five* appear **nowhere** on screen, because `BASE_SAVE_SLOTS` bounds a cloud capacity nothing local reads and ADR 0043 records there is no client identity at all — reported as a direction not yet started, not a defect. **And the one honest sentence about where prisons live is gone after the first press**: `'Local saves only — no network required.'` is the *idle* status, replaced by the first `setStatus` and never restored, so a filter of the whole page for *save/local/network/account/cloud/browser/device/sign* after one prison returned only `["Save now","Saved (generation …)"]`; a second browser context on the same machine shows `rows = 0` with no explanation, and two tabs of one profile neither warn nor mention each other. Weakest claim, named in the record: that the ordering defect costs a player anything — one list in one act, plus JUDGEMENT leaning on the identical labels; and the `git log -S` dating §3 asks for is **UNKNOWN by construction here**, the checkout being shallow at `f00c7d1` so all three searches return the graft boundary |
| [2026-09-04 why they stack](./2026-09-04-why-they-stack.md) | Issue [#944](https://github.com/matmaxalez/lockstate/issues/944) §3, the half #944 says must be answered *before* the draw order is touched, because fixing depth makes twenty-two prisoners on one tile **visible** rather than **correct**: why does the simulation put a whole population on one tile at all? Answered by playing three acts on `9ef67944` (**v0.0.458**) with nothing under `src/` changed, reading every position off the worker rather than off pixels — Git LFS is unprovisioned in this container, so a browser run here draws no actor sprites at all and passes anyway. | **None as an ADR; one mechanism, and the discriminating comparison refutes the intuition the issue was written on.** A prisoner's position is written at exactly three places and **none of them is a place inside a room**: once on admission to the arrival tile, once per tile crossed by the walker, and once on arrival to the room instance's `anchorTile` — the north-west corner of the rectangle the player dragged, which is also the tile the instance id is minted from. `PrisonerColdState.getAccommodation` returns a **room instance id**, an action's destination is `instance.anchorTile`, and there is no per-object, per-bed or per-seat position anywhere in the simulation. So every prisoner acting in one room stands on one tile, and every prisoner for whom no action ever resolves stands on the arrival tile for ever. **MEASURED: giving every prisoner a bed put them on FEWER tiles, not more** — six prisoners with two beds stood on two tiles (2 holding `room.cell:12:12` at `stage=completed`, 4 holding nothing at `stage=accommodation-assignment` with **no action at all**), and six prisoners with six beds stood on **one**, all six `action.free-association` on (12,12), while the six beds themselves stand on six distinct tiles. Fed into #944: the stack is not a rendering defect and a rendering change cannot remove it — PR #954 spreads a shared tile so every actor on it is *drawn*, which is #944 §2 and is worth having, and this note is the record that the twenty-two are genuinely **on** one tile and would still be after it. Whether the simulation should give a prisoner a position inside a room is a question this note opens and does not answer |
| [2026-09-04 what to do next](./2026-09-04-what-to-do-next.md) | An external deep-research audit of the repository arrived with a P0 list, a `CommandOutcome` refactor proposal and a *"6–10 weeks with 2–3 people, 12–18 solo"* estimate. The owner asked for one merged direction. So: which of that audit's load-bearing claims survive an independent reading, what order does the remaining work actually go in, and which of its recommendations are not ours to act on at all? | **None as an ADR; an ordered list, three refusals with reasons, and a correction to the integrator's own brief on every count that mattered.** The audit is right that the corpus is large and that the game applies no pressure, and its branch inventory is right where the integrator's brief was wrong — **159** branches, not ~348, and branch GC is not blocked by anything: `branch-gc.yml` ran four times on 2026-09-03 and took 475 → 159. Refused with reasons: the `CommandOutcome` refactor as proposed (**two of its three arms already exist**, one in a better form, and **ADR 0003 decision 9 blocks the `applied` arm exactly where the audit puts it** — an ADR is owed saying so, which is the alternative); a linter (a recorded deliberate absence, would touch every file, cheapest thing available and the least valuable, which is the combination that makes it attractive for the wrong reasons); and the hour and week figures (this repository has **no velocity history anyone can honestly estimate from** — the one measured datum about capacity is a *concurrency cap* of two agents per workflow on a four-CPU container, not a throughput). **The audit recommends action inside three of `AGENTS.md`'s four reservations and does not mention that any reservation exists** — branch protection and the CSP/rate-limiting change are deploy configuration, migration failure injection is `supabase/migrations/`, and any second server route is reservation 1. Two decisions are named and handed up: **O-1**, whether the prison is ever allowed to be in trouble — balance, so the owner's by kind rather than by rule, and *nothing below it is worth starting until it is answered*; and **O-2**, protected `main` in a form that is not the bypass §2.5 measures. Weakest claim, named in the note itself. Fed the owner's 2026-09-04 rulings: **"Zmierzcie to najpierw"** on O-1 (measure the pressure before deciding it) and **"Włącz oba od razu"** on O-2 |
| [2026-09-04 what the game acknowledges](./2026-09-04-what-the-game-acknowledges.md) | Two falsifiable claims were put to this pass as coming from two different passes and are in fact two sections of one landed note (`2026-09-04-is-there-anything-to-do.md` §7 and §13): that **every** event type is bad news, an undo or a recovery — *"so nothing a player does right is ever acknowledged"* — at **eighteen** types; and that nothing in the game ever states what the player is trying to achieve. A census, not a playtest: nothing was played, so the strongest tier anything in it carries is **VERIFIED, read**, and the one claim that would need a run is named and *not made*. | **None as an ADR; the taxonomy is refuted by exactly one member and the claim underneath it comes out sharper.** The authoritative registry is `SIMULATION_EVENT_TYPES` (`src/simulation/protocol/types.ts`) — not the producer and not `EVENT_PRESENTATION`, which is typed `Record<SimulationEventType, …>` and therefore can be neither authoritative nor incomplete. The count is **twenty**, and **nineteen are reachable**: `incidents.gang-retaliation-opened`'s `register` has no caller in `src/` and `addMember`'s only one is `loadSnapshot`. *Eighteen* was **genuinely true for five hours** on the morning it was written — `889ff5f9` made it 19 and `d9770e61` made it 20, both the same day, both adding an undo or bad news, **so the count moved twice in a day and the shape did not move at all**: 9 bad news, 6 undo, 2 recovery, 1 neither, **0 acknowledgements**. The refuting member is `prisoners.discharged`, and it makes the finding worse rather than better: `SENTENCE_BEARING_STAGES` includes `'accommodation-assignment'` and `releasePrisoner` requires no room, bed or guard, **so the game's one good sentence fires for a prison that houses nobody — the gate is a clock**. Three sites are named where a true claim is already computed and nothing is published, with what a sentence there would have to prove and no sentence authored: `insolvency-rung-system.ts:159-167` (the up-crossing is computed in an `if`/`else` whose *other* branch publishes); `session-commands.ts:194-216` (the accepted `ZoneRoom` outcome **withdraws two standing refusals** as *"a fact the world just confirmed"* and publishes nothing, with `events` already in lexical scope — one line is the whole change, and it is #749's asymmetry unapplied to zoning); and `intake-system.ts:563-571`, whose mirror image `prisoners.relocated` already speaks. Two leads were declined on evidence: `staffUnassigned` is a real orphan but an idle guard is a wage paid for nothing, so #870 owns it correctly; and the yard incentive is one-third right — the no-object claim holds, *"unmet → served"* is false at zoning time because `unmetNeedCount` reads a level against 51 raised only by a prisoner performing the action, and *"returns 40 a prisoner a day"* is false today because `STATE_INCOME_WITHHELD_PER_UNMET_NEED_MINOR_UNITS = 0`. Weakest claim, named: that the events channel is where an acknowledgement belongs at all — every site is a place where a true claim is available, none is established as a place where a *notice* would improve the game. Filed as #960 (the census and the product question) and #966 (the three sites) |
| [2026-09-04 what pressure there is](./2026-09-04-what-pressure-there-is.md) | The owner suspended the unmet-need penalty on 2026-09-03 (*"usuń na razie kary, zobaczymy jak pogram i ocenię łatwość"*), and on 2026-09-04, asked whether the prison should ever be allowed to be in trouble, ruled **"Zmierzcie to najpierw"** — measure it first. So: **does this game apply any pressure at all, and what would restoring that one constant do to the treasury curve?** Five prisons played from scratch, every figure off the worker's `simulation/status-counts` channel and the Regime panel's per-need inspector, with `STATE_INCOME_WITHHELD_PER_UNMET_NEED_MINOR_UNITS` restored to `40` as a **mutation** and restored by hand after (`sha256sum -c` OK twice, and `git diff origin/main..HEAD -- src/` empty). | **None as an ADR; two curves, a break-even the repository had never measured, and a correction to the integrator's arithmetic on every count.** **MEASURED, shipped (`withheld = 0`): +1,120 at nine consecutive day boundaries, `4 × 300 − 80` exactly** — and at the last of them **all four prisoners had five of six needs at permille 0** (hunger, sleep, hygiene, bladder, recreation all `unmet: true`; only `safety` served) **and the state paid 300 apiece anyway.** With the penalty restored the curve is a staircase to a floor: +1,080, +800, +640, +640, +480, +320, +320, +320, +320, every price a row of the schedule the constant computes. **The direct comparison rather than curve-minus-curve: at four housed prisoners with five of six needs unmet, shipped pays 1,200/day and restored pays 400/day — a 71% cut, and still growth.** The yard incentive is real to the unit — `480 − 320 = 160 = 4 × 40`, with `recreation` at 949–996 permille against act P's 0, and the 8×8 zoned **first attempt on the bare ground a new prison starts with**, no walls, no objects, no money. **And the first falling treasury this repository has measured on a fully housed, fully earning prison:** penalty plus six guards (480 of a 25,000 grant) rose eight days then went −40, −80, −80, −80, since `4 × 100 − 480 = −80`. Pure neglect is not reachable at all — all twelve Admit presses were refused with *"This prison has no room to hold a prisoner"*, so capacity gates everything and neglect requires a cell first. **The unlooked-for finding:** the three prisons at `1 held · 0 free` closed **every** incident with *"the last one ran out of time instead of being contained, and everyone caught in it was hurt"*, while the six-guard prison closed every one with *"The prison is under control again"* and never showed the lapse line — so with the penalty restored the two prisons on offer are **+320/day with nothing contained or −80/day with everything contained, and neither figure is on screen** (the FUNDS chip carries no badge while sinking, and `EARNED TODAY` is a within-day accrual that is *rising*). **Four corrections to the brief, all of them the integrator's arithmetic:** the 3.33%/30× figures are the asymptote, not this game — `resolveOccupancyScaledGuardCount` is `max(scheduledGuardCount, ceil(n/8))` with a schedule floor of one, so a four-prisoner prison is asked for **1** guard and wages are **6.7%** of income with **15×** headroom; the documented floor of 60 is unreachable for any prison with a guard, because a covered sector returns `safety` at 0.08/tick against a 0.05 decay and it read `1000` permille on every prisoner in every closing reading, making the staffed floor **100**, break-even 1.25 guards per prisoner rather than 0.75 and the penalty a 67% cut rather than 80%; the role id is `staff-role.guard`; and **the framing was half wrong — the penalty is not what makes *neglect* cost anything**, since both curves grew with five of six needs at zero, so restored it creates a **staffing** pressure and none at all on how anybody is treated. Fed the owner's ruling of 2026-09-04: restore the constant to `40`, but measure a fifty-prisoner prison first, since every ratio here is at `n = 4` |
| [2026-09-04 what pressure there is at fifty](./2026-09-04-what-pressure-there-is-at-fifty.md) | Shown the four-prisoner curves, the owner ruled on 2026-09-04 to restore `STATE_INCOME_WITHHELD_PER_UNMET_NEED_MINOR_UNITS` to `40` **but to measure a fifty-prisoner prison first**, because [the four-prisoner note](./2026-09-04-what-pressure-there-is.md) had said in terms that every ratio in it was at `n = 4` and that fifty was the one balance actually needs. So: **can fifty even be housed, what are both curves at that population, and which of the four-prisoner findings survive the scale?** Five acts on v0.0.467, every figure off the worker's `simulation/status-counts` channel and the Regime panel's per-need inspector, with the constant restored as a **mutation** and restored by hand after (`sha256sum -c` OK, `git diff origin/main..HEAD -- src/` empty). | **None as an ADR; fifty is housable, both break-evens are measured rather than derived, and three of the four-prisoner findings do not survive.** **Fifty can be housed, against the expectation in the brief:** a **10×10** cell — the largest rectangle the pointer can wall in at 1440×900 — is 100 tiles, which is 50 beds at one place per two tiles, **and leaves no tile for the toilet**; `accommodationCapacity: 50`, `occupiedPlaces: 50`, all fifty Admit presses accepted, on a room the Rooms panel calls *"NOT READY · missing 1 × Toilet"*. At 7 guards (`ceil(50/8)`, which does dominate the schedule floor the four-prisoner pass was trapped by): shipped settles at **+14,440**/day over 8 boundaries, restored at **+10,440** over 4; wages are **3.7%** of income against **5.1%**; and the break-even, found by overhiring until the sign flips rather than by division, is **187 → 188** guards shipped and **137 → 138** restored — two stages of one run, one press apart, same world and same composition. Headroom over what the game asks falls from **26.8×** to **19.6×** — the arithmetic crossing at 187.5 guards over the 7 the game asks, which the measured pair 187 (still rising) and 188 (falling) brackets. **Three refutations of the four-prisoner note:** the penalty costs **27.7%** here rather than 71%, because the composition at fifty is **two** of six unmet rather than five — `hunger`, `sleep` and `bladder` all read 855+ permille in a cell with **no toilet**, where at four they read 0; **the yard incentive is worth exactly nothing**, zoned first attempt, settled delta identical to the no-yard act *to the unit*, `recreation` at **0 permille and unmet for 50 of 50**, with capacity ruled out as the explanation because four rotating places would leave some non-zero reading; and the brief's *"#957 says the clear area is 12×11"* measures clear tile **centres** while walls run on **edges**, so the enclosable rectangle is 10×10 and #957 is not wrong. `safety` still saturating is the one finding that survives — a first reading of 39-of-50 unsafe beside `COVERAGE 50 Covered` turned out to be a build-phase transient across 520 s of building with no guards, and is recorded with its own refutation rather than deleted. **The strongest result is one nobody asked for:** in one world across one hire, the **ten** assaults before it each closed `all-clear-after-lapse` at ~610 ticks and the **six** after it each closed `all-clear` at ~70 — an **8.7× collapse**, replicated in a second act, which promotes the four-prisoner §5.1 from a correspondence across four worlds to a within-world result. **And the caveat is load-bearing:** merging `main` mid-write brought #938's fix (v0.0.472), which establishes that a sealed room with no doorway is dead — hygiene 254.8/255 with a door, **0** without — and **neither playtest ever builds a door**. So the two unmet needs priced throughout are *a dead cell's*, and the yard's zero means "a yard pays nothing to a prison whose cell has no door". The direction is knowable: a door can only reduce the unmet count, so **27.7% is the pessimistic end**, and the shipped column is immune because it pays 300 per occupied place at any composition |
| [2026-09-04 the five-tester round, audited](./2026-09-04-audit-of-the-five-tester-round.md) | A coordinator ran five play-testers against `main` at **v0.0.451** (`0e614c7`) and wrote a consolidated report ranking their findings against each other. `docs/AGENT_WORKFLOW.md` §1 requires a coordinator to verify agents' load-bearing claims rather than accept them; this asks the same question **of that report**: which of its load-bearing claims survive an independent reading of the code, which do not, and what the round produced that its own report does not say. Every citation below was opened by the integrator on `main`. | **None as an ADR; the report is largely sound and three of its claims are withdrawn rather than corrected in place, so a reader knows which parts to re-check.** Confirmed and sharper than the report framed it: `DEFAULT_SECURITY_SECTOR_REQUIRED_GUARD_COUNT`'s own docblock predicts that *"a player who hires exactly the requirement will watch every incident lapse"*, and the session measured 17 residents with **4** guards — `ceil(17/8) = 3`, so the requirement **plus one reserve** — reading `3 of 3 / Covered`, `3 held · 1 free`, and lapsing **7 of 7** (open as #941; PR #955 replaces the sentence, and the arithmetic it opened is worse than the comment — `requiredResponderCount` is `max(1, ceil(severity * 0.5))` and every incident this build can open is severity 3 or worse, so requirement **+ 1** leaves no reserve large enough for **any** of them). Withdrawn: the report's shallow-clone claim, which was re-verified by its coordinator and is false at audit time — `git rev-parse --is-shallow-repository` returns `false`, no `.git/shallow` exists, and that suite passes 8/8. What the round produced and its report does not say: **none of the five records was in the repository**, and all five index themselves in this file, which appends at one point — so four merge conflicts were structural whatever order they landed in, each resolved by keeping every row in landed order and editing no row's text. That is **#428's unimplemented one-file-per-entry fix showing its cost for the second time in one day**, now with the arithmetic plain: *n* self-indexing notes cost *n − 1* hand resolutions. Weakest claim, named: that roughly thirty re-verified citations generalise to the rest — every citation this audit opened matched and none was withdrawn, which is a good record and is not proof of the Tier C and D ones it did not open |
| [2026-09-05 where the yard incentive breaks](./2026-09-05-where-the-yard-incentive-breaks.md) | Issue [#997](https://github.com/matmaxalez/lockstate/issues/997). The integrator handed over a balance finding — the yard incentive is worth `4 × 40 = 160`/day at four prisoners and **exactly nothing** at fifty — and the owner ruled on 2026-09-05 **"zmierz najpierw, gdzie się łamie"**, measure where it breaks before touching any constant. So: where is the boundary, what runs out, does more yard fix it, does the same shape reach the other five needs, and what should the constant's docblock say afterwards? A headless instrument (`tests/research/2026-09-05-yard-at-scale.research.ts`, its own vitest config so `pnpm test` does not collect it), seed `0x997`, 20 in-game days, money read from the production `stateIncomeForCompletedDay`, **no production file edited and no balance constant moved**. | **None as an ADR; the premise the ruling was made on is refuted, and the refutation makes the game better rather than worse.** **There is no boundary in 4..50: recovered income is `n × 40` to the unit at all eight populations** (160, 320, 480, 640, 960, 1280, 1600, 2000) with `recreation` unmet for 0 of n at every one — reproduced by the integrator byte-identically, twice. **The fifty-prisoner zero was a cell with no doorway, not the population.** Same seed, same prison, one edge different: sealed 11,000/day with **0** ticks of `action.yard-recreation`, `recreation` 0 permille for 50 of 50 and **11,558 `routeFailures`**; doored 13,000/day with 29,043 yard ticks, `routeFailures` **0** and `recreation` unmet for **0** of 50. That confirms candidate 2 of [the fifty note](./2026-09-04-what-pressure-there-is-at-fifty.md) §6 and demotes its capacity candidate — and it means the integrator's handover, this issue's premise and the owner's ruling all rested on a defect [#938](https://github.com/matmaxalez/lockstate/issues/938) had already named. What *does* degrade with population is the **margin**, not the grant: the lowest `recreation` level in the prison falls 999 → 436 permille against a 200 threshold, and pushed past fifty the shortfall is a **slope and not a step** — 0 short at 50/60/70, **1 at 80, 12 at 90, 20 at 100** — with the grant equal to `(n − short) × 40` exactly in every row. **What runs out, named rather than guessed:** floor area, `Math.max(1, Math.floor((width * height) / TILES_PER_OPEN_GROUND_PLACE))` at `src/simulation/prisoners/room-instance-registry.ts:341` with the constant `16` at `:222` — so an 8×8 yard is 4 places — but **the number that binds is throughput, not the pool**: 4 places × the 600 ticks/day `GENERAL_POPULATION_REGIME` allows recreation (`regime.ts:110`, `:113`, `:115`) ÷ a 100-tick minimum dwell ≈ 24 visits/day against a demand of one visit per prisoner per 5.67 days. **Queueing, not pathing**: `routeFailures` is 0 in every doored arm while `contendedSubstitutionCycles` reaches 10,279 at n=100 and falls to 0 the moment places are spare. **The class is one line of code, and it has exactly two members:** `action-system.ts:1157` reads `if (action.target.kind !== 'room-catalog-id') return true;`, so only a need served by a *room* can hit a ceiling at all — `hunger`, `sleep` and `bladder` target `own-accommodation` and are exempt (one toilet serves fifty), while **`recreation` and `hygiene`** are not, and hygiene breaks harder because a shower room is 2 places rather than 4. More yard is free (`room.yard` authors no object requirement, `room-catalog.ts:138-141`, and `ZoneRoom` moves no money): **one 8×8 per fifty prisoners**, and ground *at the door* is worth about four times ground across the map for the same grant. Proposed docblock text is in §6 of the note and deliberately **not** written into `income.ts`. Weakest claim, named by the note itself: the 80/90/100 slope is six points from **one seed and one command order**. Side finding recorded and not pursued: at 100 prisoners with 7 guards `safety` fell to 2 permille and the classification systems promoted the **whole** population to `high-risk`, whose regime allows recreation 200 ticks/day instead of 600 — understaffing compounding into a two-thirds cut of recreation time |
| [2026-09-05 does the game say there is no door](./2026-09-05-does-the-game-say-there-is-no-door.md) | A sealed cell silently destroys a prison — [the yard note](./2026-09-05-where-the-yard-incentive-breaks.md) measured 11,000/day against 13,000, `recreation` 0 permille for 50 of 50 and **11,558 `routeFailures`** on one missing edge — and **two agent play-testers in one session never built a door and both blamed something else**, one "scale" and one "capacity". [#938](https://github.com/matmaxalez/lockstate/issues/938)/PR #980 shipped `'hud.rooms.needs-doorway': 'a door — nobody can get in'` and nobody had checked by playing whether it reaches a player. So: **does the game tell the player their cell has no door, in time, in a way they act on?** Four acts on v0.0.485, played **with the art** — the first play-test in this repository that could, `git lfs checkout` taking 0.965s at 0 B/s — with every screen reading written down before any code was opened. | **None as an ADR; the sentence is true, immediate and durable, and the player will not see it — and a door that leads nowhere silences the panel completely.** The message fires at **tick 4689, day 2**, the earliest possible moment, and survives a tab change, a collapse, a scroll, a save, a full page reload and nine game days. It also has exactly **one** render site, `rooms-panel.ts`, and `.hud-rooms` is not laid out at all while OVERVIEW is open — where Admit and the alerts are, and where a player sits. Measured: **eight game days on OVERVIEW produced two messages** (`Contraband found: Tool.` and `Cell designated. Day 2`) and **not one word about the door**, with the strip reading `1 ROOMS` unqualified while the panel read `NOT READY 1 of 1` and the treasury rising the whole time. **The strongest finding is one the brief did not ask for:** build the door, wait for the warning to clear, then wall off the single tile behind it — the panel goes **completely silent** (`needs: []`, no `NOT READY`, 3.4 further game days of nothing), and the screenshot of "fixed" and the screenshot of "walled in behind its own door" are **byte-identical files**. `roomPerimeterAccess` (`src/simulation/rooms/enclosure.ts:288`) is four adjacency scans of the rectangle's sides with **no reachability step** — the first door found returns `'doorway'` — so the sentence is **true in one direction only**: shown, it is right; absent, it says nothing about whether anyone can get in, and a player who repaired exactly what was named has been told they are done. Three more, each measured: the panel **contradicts itself two lines below the warning**, where `ENCLOSURE  Walled in on every side` is the same wording the game uses elsewhere as the *passing* state (and `default-locale-en.ts:1171-1174` already documents that it "renders identically for a reachable room and a sealed box", citing #938 and hygiene 0 of 255 with 162 route failures — so half of this was known and shipped anyway); the repair is **never acknowledged**, the block vanishes rather than being replaced, which `messages.ts:1145` states as deliberate on a 7.9px panel budget (ADR 0022) and which is not an argument against a one-shot alert in the column already carrying "Cell designated. Day 2"; and `.hud-rooms__enclosure` is **clipped mid-word**, `scrollWidth 268` against `clientWidth 238` with `text-overflow: clip`. Also: a room zoned and immediately removed leaves a **false standing alert**, there is a lifecycle alert for designation and none for removal. **And the world view gives nothing** — the first stacking screenshots this repository has (#944, whose own note says it could not take them) show four prisoners and a guard piled on one tile, **identical before and after the door**, so a working cell and a dead one draw the same. Weakest claim, named by the tester: that a player *would* miss it — what is measured is what the game *showed*; ROOMS is one click away. Filed as [#1006](https://github.com/matmaxalez/lockstate/issues/1006), with the reachability question split out as the owner's and an ADR's rather than a fix |
| [2026-09-05 where the shower runs out](./2026-09-05-where-the-shower-runs-out.md) | Issue [#1003](https://github.com/matmaxalez/lockstate/issues/1003). [The yard note](./2026-09-05-where-the-yard-incentive-breaks.md) ended by naming a class rather than a room — a need served only by a `room-catalog-id` action, whose members it gave as `recreation` and `hygiene` — and said the second member scales harder, because a shower room holds two places against the yard's four. So: where does `hygiene` break, how much shower does fifty prisoners take, how does the rule compare with the yard's, can a player find out, and does the class really have exactly two members? The #997 instrument **extended rather than a second one written** (`tests/research/2026-09-05-yard-at-scale.research.ts`, its own vitest config so `pnpm test` does not collect it), three seeds, populations 4..100, **no production file edited and no balance constant moved**. Recovered work: the branch was finished and pushed by an agent whose session was then deleted, and it had no pull request until [#1012](https://github.com/matmaxalez/lockstate/pull/1012). | **None as an ADR; `hygiene` breaks inside the range the issue names where `recreation` does not, and the issue's own premise about *why* was wrong in the direction that makes the repair cheaper.** One authored-minimum shower room — 3×3, two heads, two places — serves everybody up to **24** prisoners on all three seeds and leaves somebody short at **32** on two of the three, while the same prison's authored-minimum yard leaves nobody short anywhere up to 50. At fifty the single room recovers **1,720 of a possible 2,000**; at a hundred **1,160 of 4,000**, and past sixty the total it recovers *falls* as population rises — 1,720 at n=60, 1,640 at 70, 1,440 at 80 and 90, 1,160 at 100 — which the yard never does inside a hundred. **Measured repair: two 3×3 shower rooms for fifty, six shower heads for a hundred**, both restoring the full `n × 40` to the unit. **The ceiling is not floor area, which is what the issue got wrong:** `room.shower-room` carries no `openArea: true` (`src/content/room-catalog.ts:128`, its three requirements being `enclosed`, `minimum-size` 3×3 and `{ type: 'object', objectId: 'object.shower-head', minQuantity: 2 }` at `:131`) while `room.yard` does at `:138` — so `openGroundCapacityOf` is never called for a shower and its capacity comes from its **heads**. The room therefore does not need to be bigger, it needs more heads: capacity went 2 → 8 with the rectangle unchanged. Per *place* hygiene is no worse than recreation and at a hundred is cheaper (six places against eight); what is worse is the price of the authored minimum — **1,025 minor units** for two places against **0** for the yard's four — and the player can afford it at every population measured, two rooms being 2,050 against a 25,000 starting treasury and returning 2,000 a day. **And distance helps rather than hurts**, the opposite of the yard: the same room 17 tiles away serves all fifty where the one 6 tiles away leaves seven short — filed as a measurement with a named candidate (arrival synchronisation under `compareByNeedUrgency`) and a test to write, not as a diagnosis. Class census confirmed as exactly `hygiene` and `recreation`, derived rather than quoted, with three refinements #997 did not carry: `hygiene` has a **second** gated provider (`action.laundry-work` in `room.laundry`, +1/tick, **unmeasured — the note names this as its own biggest hole**), there is a **third** exempt target kind (`job-board`), and `safety` is outside the class because **no action serves it at all**. **Nothing in any of this reaches a player**, and the projection layer says so about itself — the concurrent-use figure is *"not projected at all yet"*, a gap it attributes to the Rooms tab readout ADR 0028 phase 5 owes — while neither contention counter has a reader outside the action system and `ActionSystem.getMetrics()` is called only by `tests/helpers/determinism-state.ts`. **A citation correction that is the finding, not a typo:** the #1003 comment places that sentence at `room-projection.ts:214` and says it was opened and stands there; it is at **`:235`**, and was at `:235` on every commit `main` carried that day (`206f350`, `5acd153`, `1480896`, `89d4b35`), so it was wrong when written rather than rotted — one more argument for `docs/adr/STATUS-QUEUE.md` §6 recommendation 1, citing by symbol rather than by line. Weakest claim, named by the note itself: that sixty days is an honest read — there is no convergence by day 140 and the spread between seeds is as wide as the change between populations, so **every per-population number is one sample from a band**. What survives that is the identity `recovered = (n − unmet) × 40`, which held to the unit in every row, and the class derivation |
| [2026-09-05 what a guard short costs](./2026-09-05-what-a-guard-short-costs.md) | Issue [#1004](https://github.com/matmaxalez/lockstate/issues/1004), opened from a side observation the [#997](https://github.com/matmaxalez/lockstate/issues/997) pass recorded and deliberately did not chase: at 100 prisoners and 7 guards `safety` fell to 2 permille, the classification systems promoted the **whole** population to `high-risk`, and that regime allows recreation for 200 ticks a day instead of 600. The issue names a feedback loop — fewer guards, lower `safety`, promotion, shorter recreation, more unmet needs, less income, **less money for the guards that were missing** — and asks whether it has a point of no return, where the threshold is, whether the promotion is reversible when staffing returns, and whether the player can see any of it. Five acts (P–T) on `tests/research/2026-09-05-yard-at-scale.research.ts`, three seeds, `n` = 50 and 100, 30 and 50 in-game days, money read from the production `stateIncomeForCompletedDay`, **no balance constant moved**. | **None as an ADR; the loop does not close, two of its four links are wrong, and the correction is sharper than the claim.** **It cannot close: the marginal guard returns about 68× its wage.** At `n = 100` the thirteenth guard is worth **5,420/day** and costs **80** — `28,080` against `22,660`, both exact from the unmet-need histogram — and **every prison in every arm ended richer than it started** — the 100-prisoner, **no-guard**, nine-lapsed-riot prison at **664,030** and the poorest row anywhere in the record (50 prisoners, zero guards) at **385,080**, both from a 25,000 opening balance. **`reviewClassification` has no `safety` term at all** (`classification.ts:191`: sentence, intake history, findings, clean conduct): the promoter is a **riot**, whose `participantIds` is the sector's occupants — measured at **100 participants a riot**, and disciplinary points came out at exactly `3 × riots` in every row (12 at 4 riots, 21 at 7, 27 at 9). **`ClassificationEarlyWarningSystem` cannot promote anybody**, contra the issue: it is capped at tier 2, which `classificationGroupIdForTier` maps to `general-population`. **And the promotion does not cost money** — act T derives both schedules' windows and `high-risk` cuts `recreation` to 33% (12.5% once `education`, which also serves it, goes to zero) while **quadrupling** `hygiene` (440%) and more than doubling `meal` (733%): at `n = 100`, **176 unmet needs at twelve guards against 150 at seven**, so the promoted prison earns slightly more. **What costs is a cliff with no gradient:** `resolveSectorCoverageState` tests only `shortage > 0`, so twelve of thirteen guards and one of thirteen are the same rung — `safety 2/2/2 permille` and the whole population unmet at 12, 10, 7, 4 and 1 guards on all three seeds — **so hiring five of six missing guards buys nothing and the sixth recovers `n × 40`/day.** Mutating `SAFETY_COVERAGE_PROVISION_MULTIPLIER.understaffed` 0.5→1 removes the entire loop: every understaffed row goes to `safety 1000`, unmet 0, and no whole-population promotion. **The promotion is reversible on a timer and not on staffing:** a prison stripped to one guard promoted 49 of 49 at day 20 and was back to **37 general-population / 8 high-risk at day 40 while still at one guard of six** — `MAX_CLEAN_CONDUCT_CREDIT`'s two clean review periods, confirmed by mutating it to 0 (stays 0/43 through day 53). **The player sees four of the five links and not the fourth:** `describeStaffCoverage` attaches its consequence sentence to the `unguarded` rung **only** (`staff-panel.ts:442-457`), so the rung that costs `n × 40`/day says just *"Hire 1 more to cover this population."*; and of 425 authored strings exactly **two** connect staffing to anything a prisoner experiences, neither on `understaffed`, while `disciplinary`, `finding` and `conduct` appear in none — so nothing names the riot that promoted the population twenty days earlier. Weakest claim, named: the net-favourable direction in §4 rests on overlapping seed bands, and the defensible form is the weaker *"does not measurably cost money"*, with the 176-against-150 mechanism table as the evidence rather than the grant comparison |
| [2026-08-22 architecture pattern audit](./ARCHITECTURE_PATTERN_AUDIT.md) | Of the owner-supplied architecture audit dated 2026-08-22, what direction does Lockstate adopt, and what may not be copied from the GPLv3 and Prison Architect material it surveys? | **None as an ADR; it is a standing distillation the rest of the repository cites as authority.** `THIRD_PARTY_NOTICES.md:3` names it as the source of the rule that Apache-2.0 or MIT-family code may be adapted only with an entry there carrying source, commit, licence and modifications, and issue [#141](https://github.com/matmaxalez/lockstate/issues/141) exists because it named that destination before the file did. Audited back at `docs/research/audit-2026-08-26/04-architecture.md:265` (ARC-14, CONFIRMED, Informational): Phaser-as-renderer-only, no Grid Engine dependency, topology below zoning and deterministic work units are all honoured, while *"introduce a serializable `ActionPlan`/`ActionRunner` and an explicit reservation model"* is unbuilt — no such symbol exists in `src/`, and six systems call `requestRoute` directly instead. The oldest document in this directory and the only one whose filename carries no date |
| [2026-08-29 design search corpus](./design-search-2026-08-29/README.md) | The owner sent one brief to nineteen independent models and returned their answers: what else could Lockstate's economy and progression be, and where do nineteen readings agree? A directory rather than a single note — the brief verbatim plus four batch extracts reducing the answers to **251 deduplicated mechanism entries** on a fixed six-field schema, written by four readers who did not see each other's work | **None directly — every entry is a candidate and the corpus says so** (*"Nothing in the corpus is a decision"*); it fed the [#584](https://github.com/matmaxalez/lockstate/issues/584) family of eleven owner-decision issues that `docs/research/2026-09-02-the-open-issue-backlog.md:398` still lists as awaiting a call. Its convergence is the load-bearing part: **fourteen of the nineteen put the same thing first**, by five different routes, and it is one line's meaning — that an occupied place should mean a resident backed by residency capacity that exists. It also refutes the brief's own arithmetic: the withholding schedule floors at 60 per resident per day, so twelve paid places cannot gross the 150/day the brief reported, and the reading with the fewest assumptions is that most of that population was never housed |
| [2026-08-30 does a prison survive being reopened](./2026-08-30-does-a-prison-survive-being-reopened.md) | [#569](https://github.com/matmaxalez/lockstate/issues/569) established that a save reloads with its counts intact, which does not prove the restored prison still *works*: income is derived per occupied place, so the expensive failure is a prison that loads looking whole and quietly stops earning. Does a closed and reopened prison still earn? Played in real Chromium at 1440x900 on `603b718` (v0.0.226) through `index.html` + `src/main.ts` | **None — a negative result, and the class it rules out is the one no count-based check can see.** Nine compared fields are identical across the reload and the restored prison credits the same **1,120** minor units at its next day boundary as the original did at its last (4 residents x 300, minus an 80 wage bill), checked against the source rather than taken on trust. The record corrects itself in place on the paying method — `residentIdsWithExistingPlace()` (`src/simulation/economy/income.ts:407`), not `residentIds()` — and keeps the wrong sentence, noting the correction makes the source *stricter* than assumed and moves no number. Two UNKNOWNs are promoted rather than glossed: a prison built by the naive route, and one whose residents and occupied places disagree. Cited by `docs/AGENT_WORKFLOW.md` §2 for a different finding entirely — its browser run went green with every actor sprite failing to decode, because the simulation lives in the worker and does not care whether anything was drawn |
| [2026-08-30 pricing the way out](./2026-08-30-pricing-the-way-out.md) | [ADR 0075](../adr/0075-what-a-prison-that-cannot-afford-its-first-bed-is-owed.md) decision 2 accepted a loan and named its own weakest claim — *"recovery-from-any-reachable-position remains unproved until those numbers exist"*. What diversion percentage, fixed fee and maximum duration price it? Five candidate triples driven out of the locked position through the real command router on `agent/692-negative-balance-and-loans` (cut at v0.0.263, `fd87468`), reproduced by `scripts/report-loan-recovery-pricing.mjs`, which nothing collects | **It prices five triples and chooses none**; §7's table is quoted in [ADR 0083](../adr/0083-what-opens-the-negative-balance-and-what-bounds-it.md), and §9 derives the magnitude of the principal floor this record recommended without measuring. **Every candidate houses a prisoner on in-game day 1.63** — the same figure in all twenty-five runs, because what gets a prisoner into a bed is a door, a designation and a plank rather than a rate — so what the triple decides is not whether recovery happens but the length of the tail, a factor of ten across the five (a 1,000 loan clears on day 11.6 under A and day 56.6 under E). What decides whether a candidate funds the work at all is neither the diversion nor the fee: it is whether the principal clears **the 1,040 the prison already owes its own build queue**. `diverted` equals `owed` in every row, which is the fixed fee doing what it was chosen for. Answers 3 and 4 are marked untiered by the record itself, because the sections behind them were not re-run |
| [2026-09-01 playing the rooms surface](./2026-09-01-playing-the-rooms-surface.md) | Play the whole rooms surface in a real browser — dragging a room on the world grid, the Rooms panel, the removal tool, walls, build-order execution and its ordering, and what the player is told when a build is impossible — and report what a player would see rather than what the store holds. Four acts on `docs/playtest-rooms-2026-09-01` at `0eabd591` (v0.0.327), nothing under `src/` changed | **One real, reproducible defect: a stale refusal can describe a success as a failure.** Fail to zone on open ground, build walls, succeed at a different rectangle, and `.hud__refusal` still reads the first failure verbatim more than a second after the real success. [ADR 0082](../adr/0082-what-order-build-orders-are-carried-out-in.md)'s ordering and [ADR 0076](../adr/0076-what-happens-to-a-resident-whose-bed-is-taken-away.md)'s second amendment both hold live, for a bed and for a wall. **Three of the four acts needed a correction to the instrument before they measured anything** — wrong geometry, wrong coordinates, a missing room — and each wrong reading is recorded beside its fix, because a reproduction that hides its own false starts teaches the next reader nothing. Corrects the brief in passing: ADR 0082 does not describe today's behaviour. Its named weakest claim, that the stale band is the *ordinary* shape of play and not a corner case, was confirmed independently by `2026-09-02-the-first-five-minutes.md` |
| [2026-09-01 what the funds chip promises](./2026-09-01-what-the-funds-chip-promises.md) | Ruling 19 landed that morning as [#747](https://github.com/matmaxalez/lockstate/pull/747), giving ADR 0017 decision 8's rungs their own thresholds inside the standing overdraft — deliveries refused below −1,250, construction halted below −2,000, wages unpaid at −2,500 — and **no part of it had ever been driven in a browser**. Play money, deliveries, purchasing and insolvency on v0.0.323 (`109467aa`) and read what the game says when a spend is refused | **Six findings, four proposals, none taken.** The FUNDS badge offers exactly **1,250 that no press can spend**: it counts room to the −2,500 floor while the shop closes at the −1,250 rung, so it can read `1,265 left` beside a refused 40. The shop is shut while **the build queue is still spending** — ten wall orders funded at a balance where a 40 brick was refused, with nothing on either control saying why. Payroll crosses **both rungs in one step** while the alerts log reads `No active alerts`. The Buy control looks identical whether the purchase will work or not, and its verdict already carries `spendableMinorUnits` against the right floor with nothing reading it. `danger` is the tone for a state no press can reach. Two things measured right so the category is not empty by assumption. Recommendation: re-base the badge on the deliveries rung *and* move the `danger` threshold to it — the two halves of one claim — with the truest option (two figures) rejected because the strip already overflows |
| [2026-09-01 what act six never reached](./2026-09-01-what-act-six-never-reached.md) | `2026-09-01-playing-after-the-rulings.md` closed by naming exactly what its act 6 never reached: escapes, `Undo` and `Cancel` on a queued build order, deliveries and refunds, a prison past in-game day 7, and the keyboard entirely. Play those five, in that order, on the real assembled page — `playtest/what-act-six-never-reached` at v0.0.309 (`4ac09736`), `git diff --stat -- src/` empty | **Four ranked findings and the pass's strongest result is not one of them.** Ruling 6 holds on a real escape: *"Jonas Quintero broke out — no guard reached them in time."* was written 52 times and **painted across 126 animation frames**, standing at least 4,478ms — the reverse of [#700](https://github.com/matmaxalez/lockstate/issues/700)'s pre-ruling measurement of `"written three times, painted zero times"`. Then: a reload drops the alerts log at day 4 and again at day 15, with four riots and five other rows lost, and the log is the only record of what happened while the player was away; `Cancel`, `Undo`, `Redo` and a delivery cancellation are **all silent on success**, the only feedback being a row vanishing from a fold that starts collapsed; arrow-key catalogue navigation pans the world camera; and **the game cannot be built without a mouse or touch**, recorded as the current boundary rather than an urgent defect. Argues against a new CI gate for its own instrument |
| [2026-09-01 what the corner and strip cost](./2026-09-01-what-the-corner-and-strip-cost.md) | [ADR 0085](../adr/0085-what-the-hud-corner-is-for-and-what-the-strip-may-drop.md) had to decide what the HUD corner is for ([#739](https://github.com/matmaxalez/lockstate/issues/739)) and what the status strip may drop ([#719](https://github.com/matmaxalez/lockstate/issues/719)), and nobody had costed either. What would widening the corner or the strip actually cost? Source-level geometry off `hud.css`, `tokens.css`, `projection.ts` and `default-locale-en.ts` at v0.0.309 — **no Playwright was run**, another agent held the browser, so every figure is either cited from an existing measurement or computed from declared CSS and tiered as such | **Four costed candidates for the corner and a correction to #719's premise, feeding ADR 0085's four decisions.** The corner's footprint is **250px** at every desktop viewport, cross-checked against a mobile-collision comment that had measured it twice for an unrelated reason. Widening it structurally costs the right rail **nothing** — both sit in `grid-area: middle` and neither constrains the other's width — until the two footprints meet, and that claim is read off the grid rules rather than derived. **Route 1 of #719 is already done** by ruling 21, seven hours after #719 opened, so #719's own cost table prices a badge length that no longer exists — **and the overflow persists anyway**, 1,389px in 1,256px at day 7, 133px over, in a mid-game state carrying none of the strip's most expensive badges. Drop order is declaration order, which puts `funds` eighth of nine — silently lost first in the worst measured case. Names four measurements it could not take, precisely enough to schedule, and its weakest claim is that the ≈290px rail footprint is arithmetic on tokens rather than a rendered pixel |
| [2026-09-01 copy variants for the owner](./2026-09-01-copy-variants-for-the-owner.md) | Ruling 19 (#747) and [#749](https://github.com/matmaxalez/lockstate/issues/749) each owe player-facing sentences nobody had drafted — the insolvency ladder's three rungs, and four controls that say nothing when they succeed. What words could they carry, and what does each cost in the surface it lands in? Candidates for the owner to choose between, written under `AGENTS.md`'s fourth exclusion: nothing here is written into `src/content/default-locale-en.ts` and no locale key is added | **It chooses nothing and closes with three questions for the owner (§6), each shaped as one clickable choice.** The constraint it establishes is why the candidates look as they do: the alerts log is **88 pixels wide and wraps at about ten characters a line**, the refusal and event bands are full width and wrap rather than truncate, and the funds chip's `{remaining} left` badge has **no pixel measurement at all** — so a sentence's cost differs by an order of magnitude across the three surfaces the same ruling touches. Several candidates need code before their words could be true — rung 2 has no `RefusalReason` and no `REFUSAL_LABEL_KEYS` entry — which the record states rather than leaving to whoever implements it, and it corrects its own brief: both rulings were already merged to `main`, so every fact cited is read from the implementation and not from a proposal |
| [2026-09-05 what a sweep costs the response](./2026-09-05-what-a-sweep-costs-the-response.md) | [ADR 0073](../adr/0073-who-orders-a-contraband-search.md) asked for a measurement it never had — *"how long a sector sweep ties up a guard … whether a prison with one guard can afford one at all"* — and [#996](https://github.com/matmaxalez/lockstate/issues/996) found `IncidentResponseSystem.claimableResponders` and `SearchSystem.assignQueuedOrders` calling the same `claimableGuardIds` for the same finite pool. How many ticks a day does a running sweep leave the responder pool empty, at one spare guard and at two? Headless kernel runs through `createNewSimulationRuntime` and the real command handler, on v0.0.483 (`1ad2189a`) and on the fix branch | **The defect is real and its measured price in incidents contained is zero, which is the finding.** A sector sweep lasts **110–130 ticks, not the 1,500 the issue assumed** — mean 114 over 40 sweeps — claims exactly one guard, and is ordered every 600 ticks, so it holds a guard about 19% of a day. At exactly one spare guard the responder pool stands empty **466 ticks a day** (520 in the smaller prison) with a sweep as sole cause; at two or more it is **0**. But every incident that opened across 24 runs carried severity 3 or more and asked for **two** responders, so the one-guard prison could answer nothing whether it searched or not. The reserve fix is therefore a no-op except at exactly one spare guard, where searching stops instead. Landed as `c42f219`, *"a sweep draws on its own pool, so a riot always has somebody to send (#996)"* |
| [2026-09-05 what the world shows](./2026-09-05-what-the-world-shows.md) | Every visual claim in this repository was made either from a tree whose `public/assets/**` was Git LFS pointer text — which loses **every** actor atlas, logs a decode error and passes anyway — or by reading code, and [#944](https://github.com/matmaxalez/lockstate/issues/944)'s world-view attempt was one of the former. So, with all 62 LFS paths carrying real bytes and a browser console holding nothing but Phaser's banner: **can a player tell the people apart, does the prison read as a place, and does the world show state or only the HUD?** | None yet — it reports what the screen shows and names what a fix would have to decide |
| [2026-09-06 the first furnished cell](./2026-09-06-the-first-furnished-cell.md) | [#1028](https://github.com/matmaxalez/lockstate/pull/1028) drew the first catalogued object from the atlas and **its own author left the weakest claim standing** — *"it does not prove the bed is drawn right way up … its pixel probe would pass on a frame rotated 180° or mirrored"* — while [#1026](https://github.com/matmaxalez/lockstate/pull/1026) put a zoom control on screen for a fifteen-fold range nobody had used while playing, and [ADR 0097](../adr/0097-what-the-world-view-is-required-to-communicate.md)'s option D had become a measurement that could be taken. **Is the bed the right way up, does a furnished cell read as furnished, how far does one object get, and is the zoom usable?** Played on v0.0.504 (`9b43fb0d`), art present, one console line an act | **The bed is the right way up and #1028's standing claim is discharged.** Pillow at the north end, established three ways that do not share a failure: the shipped sheet's crop has it at the **west**, the packer's own turn reproduced in the page puts it at the **north**, the screenshot agrees, and a luminance-profile correlation prefers un-flipped by **0.0953 against 0.3379**. A furnished cell differs from the same cell unfurnished by **30,828 px of 147,456 — 20.91% — of which 16.93% is the four beds**, against the 4.11% that `2026-09-05-what-the-world-shows.md` measured for a door. One object answers *what is this room for* and *how many fit* and touches **nothing** about whether the room works; the next object is not a matter of taste — `object.toilet` is the only other thing `room.cell` requires and its sheet already ships. The zoom pair is findable and **exactly correct at both ends**, measured off the screenshots at **3.000** (192 px tile pitch) and **0.199** (the whole 32×32 world in 408 px), and at 0.2 four beds are still countable. Four defects found by playing, the sharpest being that **`roomCapacity` counts a bed the instant it is ordered** while the world honestly draws it as a ghost for thousands of ticks and no panel says so |
| [2026-09-06 what a finished door is drawn as](./2026-09-06-what-a-finished-door-is-drawn-as.md) | [#1027](https://github.com/matmaxalez/lockstate/issues/1027) argues from the code path that a **completed** door is painted as a full-tile **opaque** slab, because `tile-layer.ts`'s duplicate-suppression guard is conjunctive and no `dirty` mark fires on a build finishing, and closes *"the reproduction is the first thing to do here"*. **Is it there, and how is a finished door told apart from the simpler reading that the world has not caught up?** Played on v0.0.505 (`02490b6e`), art present, pixels sampled through the compositor | **Not reproduced, and not reachable.** Zero samples at full opacity across three runs; the guard's two operands are read off one message, and `SimulationSnapshotFeed.apply` builds `world` and `structures` in a single literal from a single bundle, `captureSessionSnapshot` takes both in one synchronous literal, and `ConstructionSystem` completes an order and writes its edge in two adjacent statements — so staleness moves both halves together and a stale frame shows a stale *phase*. What is there, measured three times at **22.0 s, 26.1 s and 28.0 s**, is a finished door still drawn as its **translucent `planned` ghost** (`[112,90,63]`, `PLANNED_ALPHA` = 0.35) for the rest of the thirty-second consistency poll after the Build panel stopped listing it — which is [#1018](https://github.com/matmaxalez/lockstate/issues/1018)'s observation and the rival reading #1027 set out to rule out. #1027's named fix cannot be written where it says: the feed learns the completed set only from the snapshot it is deciding whether to request. Recommends re-scoping #1027; the invariant that makes it impossible is now pinned by `tests/integration/completed-edge-structures-arrive-with-their-edge.test.ts`, which **ADR 0040 slice 4 would break** |
| [2026-09-06 can you name the room](./2026-09-06-can-you-name-the-room.md) | [ADR 0098](https://github.com/matmaxalez/lockstate/pull/1032) (proposed, unmerged) names its own weakest claim and the experiment that would falsify it — *"comparing a **4.06 mean shift** against a **25.4 per-pixel spread** … the inference to 'a player cannot tell' is established nowhere in this repository … If it is wrong, option A is the whole answer and options C and D are waste"* — and says it is settled by **rendering one prison under two palettes and asking someone to name the rooms**. **Can the pixel-identical pair be told apart, is 4.06 units visible, would keying by room id be enough, and does the Build panel's legend have the same defect?** Played on v0.0.506 (`052531ae`), art present, one console line per run, three prisons of identical geometry | **4.06 units is not visible, and neither is what option A can buy.** Kitchen against Reception measured **ΔRGB 0.00 / −3.57 / 0.00**, Euclidean **3.57** against a per-pixel σ of **15.54** — 0.23 σ — and is absent to the eye at zoom 1, at zoom 3.0 and at 3× magnification across the party wall. Two rooms of one category came back **2.11** apart, which is this instrument's measurement floor rather than a tint. **Eighteen evenly spaced hues were rendered and are equally invisible**: their worst pair is 20° apart, which is **6.02** arithmetic and **5.30** measured, and the bound is the circle rather than the ordering — so keying by room id closes the collision and buys no legibility, and options C and D are not waste. Two further numbers: the pipeline delivers **0.879** of the arithmetic (3.57/4.06 and 5.30/6.02), so the ADR's effective-unit table is optimistic by an eighth throughout; and ADR 0098's option B, generated and rendered, leaves the 4.06 pair **untouched** while creating a **4.20** pair that does not exist today. **The legend does not have the same defect — it has no colour at all**: `HudRoomViewModel.tint` is computed at `src/main.ts:980` and read by nothing in `src/` or `tests/`, and all eighteen catalogue rows measured byte-identical computed colours. Also found by playing: roughly **x 0–420, y 230–820** of a 1440×900 canvas is unbuildable by drag because HUD islands eat the gesture silently, and "Use these tiles" stages a rectangle without zoning it while a stale refusal line stands |
| [2026-09-11 are the four cheap readers cheap](./2026-09-11-are-the-four-cheap-readers-cheap.md) | [#595](https://github.com/woogitsu/lockstate/issues/595) prices four dead-room readers — garbage-room filth, utility-panel per-N power, security-office information, staff-room fatigue — as cheap. Checked against the tree at `c4cfc565` (v0.0.586) rather than inherited | **None hold up as priced, and none were built.** Filth and fatigue are new fields on entities already in the save payload (`RoomInstance` via `roomInstanceSchemaV5`, `GuardRecord` via its own "so the save payload can name this shape" comment) — blocked by `AGENTS.md` reservation 2 exactly as the brief anticipated, and #595's own source (`batch-b-extract.md` B45) already says "filth is a new accumulator" in the same breath it calls the build "tiny." Utility-panel needs a sitewide aggregation system wired into `applyNeedEffects`'s one static per-tick rate, with an undecided design question in the middle (per-object-instance vs. uniform half-rate) that `AGENTS.md`'s "propose an ADR instead of silently deciding inside implementation code" rule reaches. Security-office needs a room-instance-to-sector mapping that `room-projection.ts` and `worker/projection-catalog.ts` both say, in their own words, does not exist and should not be invented in wiring code — once it exists, the reader itself is nearly free, since `SectorRiskTracker.getConsecutiveHotSamples` and `ContrabandRegistry.all()` already carry the ground truth. Also corrects #595's own title: the corpus's original count was nine dead rooms; two (`holding-cell`, `infirmary`) already have their own issues, and the re-run census finds seven, not nine, still dead |
| [2026-09-11 does unreachable show in the world](./2026-09-11-does-unreachable-show-in-the-world.md) | [#1022](https://github.com/matmaxalez/lockstate/issues/1022) measured a sealed cell and a working one at **4.11%** of pixels apart before [ADR 0108](../adr/0108-what-nobody-can-get-in-should-mean.md) shipped `'unreachable'` — a door on the perimeter nothing outside can reach, with its own Rooms-panel sentence. **Now that the Rooms panel can say it, does the world view say anything at all?** Played on v0.0.586 (`c4cfc565`), art present, one Rooms-panel read and one pixel comparison per run | **The Rooms panel is right and the world is exactly as silent as #1022 found it.** Boxing in the one tile a cell's door opens onto (ADR 0108's own act-4a fixture, in miniature) makes the panel read *"a way in — nothing outside can reach its door"* verbatim, matching the shipped string. The two 384×384 room crops -- one with the door open, one with it boxed in -- are not byte-identical (**1,945 of 147,456 pixels, 1.32%**, in two bands near the beds and the door, zero pixels differing between them), but every input `TileLayer.paintChunk` reads is verified to be independent of `RoomAccess`, and the boxing walls sit entirely outside the cropped rectangle -- so whatever produced the 1.32% (candidates named, not diagnosed: shadow bleed from the extra walls, or two-independent-page-load rendering nondeterminism on this container's software rasteriser), it is not a condition cue, because no code path could draw one |
| [2026-09-13 every HUD surface and where the five sections put it](./2026-09-13-every-hud-surface-and-where-the-five-sections-put-it.md) | Stage 0 of the identity rollout ([#1156](https://github.com/woogitsu/lockstate/issues/1156), epic [#1155](https://github.com/woogitsu/lockstate/issues/1155)). The owner ruled on 2026-09-13 that the navigation moves to the delivery's five sections **without** waiting for this inventory, so it stopped being a gate and became the record of what moved where. **What exists today, what each surface reads, what it can issue, and which of Przegląd / Buduj / Strefy / Zarządzaj / Plan dnia takes it — including the rows nothing takes?** Read off the composition root at `a0ffd01b`; every `file:line` opened, every tally re-derived by grep | **The surfaces the direction does not place are the deliverable.** The status strip, the events band, the refusal line, the unavailable band, the alerts list, the minimap and its zoom, the save panel, the display scale, the brand badge and the consent prompt are chrome that spans all five sections; the Intake panel is on Overview because ADR 0022 measured the Build panel full, not because Overview is where admitting belongs; and `src/ui/account/`'s four modules have no mounted surface at all — `projectSaveList`, `projectCloudSlotAvailability`, `loadAccountPreferences` and `AccountSessionState` have no consumer in `src/` outside that directory. **Plan dnia is a section for an editing surface no command reaches**: none of the seventeen in `commands.ts` edits a regime, so "schedule keeps full function" is a promise about a readout. Ten of the fifteen projections have a reader and five do not (`hud/security`, `hud/contraband`, `hud/incidents`, `hud/incident-detail`, `world/render-snapshot`), which is the split the reachability gate already states; all seventeen commands are issued, all seventeen from `src/main.ts`, and three of them — `DismissAlert`, `Undo`, `Redo` — are issued from surfaces the five sections do not name. Three documents corrected with the commit that broke each: `hud-state.ts`'s "three of the four existing tabs render no panel" was false **68 minutes** after it was written (`84e1c614` → `c60b10e9`), `ARCHITECTURE.md:44`'s "seven readers" broke at `f8393f00`, and the reachability gate's docblock has named one reader of `hud/staff` where there are two since `a8a446ed` |
| [2026-09-13 what an undo window for a deleted prison costs](./2026-09-13-what-an-undo-window-for-a-deleted-prison-costs.md) | The owner ruled on 2026-09-13, from an option labelled *"Potwierdzenie plus cofnięcie przez okno czasowe"*, that deleting a prison gets a confirmation **and** an undo window ([#1142](https://github.com/woogitsu/lockstate/issues/1142)). The confirmation shipped; **what does the undo actually cost, and is any shape of it buildable without an ADR?** Read off the tree at `aab912e3` (v0.0.599), every `file:line` opened, nothing implemented | **Three shapes, and the only one that keeps the promise is a persistent format change.** Deferring the delete ships a sentence that is false while it is displayed (article 5) and resurrects the prison on a reload, because nothing schedules work across a page load; hiding it meanwhile needs a field on the slot record, and `prisonSlotMetadataSchema` is `.strict()` while `list()` refuses the **whole list** on one unreadable record — so a tombstone written by a new build makes every older build show the player no prisons at all. Holding the copy in memory is honest and loses the undo silently on a reload, which article 3 (*"Cofanie może obiecywać zwrot tylko wtedy, gdy system rzeczywiście go realizuje"*) rules out. Holding it in IndexedDB survives a reload and means `DATABASE_VERSION` 1 → 2 plus retention decisions nobody has taken, which is `AGENTS.md` boundary 7 verbatim — **an ADR, not an implementation decision**. A fourth shape nobody asked for, offering Export beside the confirmation, is the only one buildable under the standing mandate today. Also found: there is **no restore path** at all — `importSave` needs a slot that already exists and `create()` stamps a fresh `createdAt`, so putting a three-generation window back needs a repository method nobody has written |
| [2026-09-13 what the saves panel may say](./2026-09-13-what-the-saves-panel-may-say.md) | [#1168](https://github.com/matmaxalez/lockstate/issues/1168) asks for a saves panel in the new "Zarządzaj" section, and the owner ruled: mount what is true now — local real, cloud rendered unavailable with the reason, no control implying a flow that does not exist. `src/ui/account/` holds four modules nothing in `src/` mounts. **What do the four modules compute and what has no producer, what can the panel truthfully say for each state it must render, and are #1142 (delete has no confirmation) and #1143 (`MemoryLocalSaveStore` does not serialise overlapping readwrite transactions) real?** Checked against `08ea9dd7` (v0.0.597), grep across all of `src/` rather than the documentation, and one race reproduced by script. **Superseded same-day, in the direction of the claim going stale: `#1142` landed hours later and the document carries a 2026-09-14 correction rather than a silent rewrite — see the note at its own top** | **Both traps are real, and the sentence inventory is the load-bearing part.** `delete()` (`repository.ts:467`) and `SavePanel.requestDelete` (`save-panel.ts:797`) are both unconditional — no confirmation exists at either layer. `MemoryLocalSaveStore.runTransaction` snapshots the whole store at call time and replaces it wholesale on commit (`memory-store.ts:15-69`); a reproduced race (two overlapping calls, one delayed 10 ms, touching *different* prisons) shows the loser's commit silently erased, which is weaker than real IndexedDB's actual serialisation and means a concurrency test against this fake proves single-writer policy logic only, never real multi-writer safety. Saves-listed, no-saves, save-in-progress, save-confirmed, delete-confirmed and (mostly) load-failed already have true sentences and mounted code (`SavePanel` is live at `main.ts:3418`); cloud-unavailable and signed-out have no locale key and none should be authored beyond "not implemented yet," because `grep -rn "createClient\|VITE_SUPABASE" src/` finds nothing outside type-only imports and `tests/foundation/trusted-tier-reachability-contract.test.ts` / `documentation-claims-contract.test.ts` gate exactly that. All four account modules compute correctly on the true local-only state (`applies: false`, `sync: 'local-only'`) rather than needing a stub — what has no producer is a live `AccountSessionState` beyond its initial constant, a live cloud index, connectivity, sync failures, and an interpreter for any of the five `AccountEffect`s |
| [2026-09-14 stage 6 language gap](./2026-09-14-stage-6-language-gap.md) | Stage 6 of the identity rollout ([#1162](https://github.com/woogitsu/lockstate/issues/1162)). The delivery's voice direction — *"fakt → miejsce → następny krok"*, direct verbs, four named anti-patterns in article 5 — is in Polish; the game ships English. **What does the direction actually require, what does `default-locale-en.ts` say today measured rather than assumed, which strings are claims the code may not be able to prove, and what would stage 6 cost in separable pieces?** Checked against `236bed4e`, a stratified read (not a full audit) of roughly 190 of 466 keys, stated as such | **The 466-string count is exact (generated, not sampled) and the sampled subset already matches the fact-then-consequence half of the voice pattern — what it never carries is the delivery's third element, an attached next-step action, because no code wires a "jump to this location" control to any alert or refusal row.** One live risk found and cited: `hud.alerts.empty` ("No active alerts", `default-locale-en.ts:541`) renders from the same literal `[]` `EMPTY_HUD_VIEW_MODEL.alerts` carries before the first worker snapshot arrives, while the sibling `clock` field in the same empty view model was deliberately given an `UNKNOWN_HUD_CLOCK` sentinel for exactly the state this field lacks one for (`view-model.ts:2064-2071`) — an asymmetry read off the code, not timed in a browser. Authoring the delivery's example table's action-verb buttons ("Pokaż przejście") without first building that navigation mechanism would itself be constitution article 6's named anti-pattern, a repair button with no real implementation. Everything else sampled — save-status wording, the unguarded-sector safety sentence, the capacity-readout strings, the absence of any `account.*`/`cloud.*` key — was checked and found to already hold the line, several with their own inline code-citation comments predating the delivery. Five separable cost pieces, from a documentation-only voice-guide addition through the missing navigation mechanism to the still-nonexistent `pl` catalogue (#661) |
| [2026-09-14 the mechanical navigation move](./2026-09-14-the-mechanical-navigation-move.md) | The next layer under Stage 0's inventory (#1156): the **control-level** list an implementing agent needs to actually move panels — every `ActionButton`/`CollapsibleSection` construction site in the five tabs' panels, cited by `file:line`, plus what the move breaks and a commit sequence. Taken on `19ffb1f5` | **A second source resolves two of #1156's "unsettled" rows, and the resolution has weaker standing than a ruling.** The delivery's own nav table (`DOKUMENTACJA/projekt.md:66-72`, never edited) names Zarządzaj for intake and for the prisoner roster, and names Przegląd for finance and save — none of it put to the owner, so treated as evidence rather than as settled. Confirmed against the simulation tree rather than assumed: `src/simulation/staff/` and `src/simulation/security/` are already separate directories, and `src/simulation/prisoners/intake-system.ts` is its own module, so both named moves (staff to Manage, intake to Manage) are UI regrouping only, exactly as `projekt.md:75` states of the staff move and as this document independently checks for intake, which the delivery's table does not itself characterize as UI-only. **No tab id is persisted anywhere** — checked directly across `src/persistence/`, `src/input/storage.ts`'s three storage keys, and `HudShellState`'s only producer and consumers — so the move is not a migration question. Exactly two tests assert the tab id set as an exact array (`ui-hud-shell-state.test.ts:47`, `ui-hud-messages.test.ts:430`); every other reference, `app-shell.spec.ts` included, loops over the live export and needs no edit. Two questions left open for the owner: whether intake lands on Zarządzaj (the table) or stays on Przegląd (its only content today), and whether the Build panel's materials/deliveries block is Buduj or Zarządzaj |
| [2026-09-14 what stage 5 checked and what it found](./2026-09-14-what-stage-5-checked-and-what-it-found.md) | Stage 5 of the identity rollout (#1161) asks one question of the two lists before it: after the six changes of 2026-09-13 and -14, is anything a player could reach before unreachable now? Taken on `e221e927`, and every reachability verdict **measured in a real Chromium** at a named viewport rather than read off CSS | **All seventeen commands are still issued and all seventeen still from `src/main.ts`** — re-derived by grep, and every line number in #1156 §4 has moved, so the table is restated. All eighteen `activeTab` gates read: none is on a retired id, and the intake gate (`main.ts:2083`, `:2552`) agrees with the intake mount (`hud.ts:2511`), which is the pair a mount move most easily leaves disagreeing. **Ten of fifteen projections read, five unread — unchanged**, so "security, contraband and incidents keep full function" is still a promise about surfaces that do not exist, and nothing about it got worse. **Measured: every operational control on Manage and on Build is pressable at 1440x900, 1024x768, 900x600 and 375x812**, with the Manage rail's budget spent twice for the first time — `.hud__side` gives 413/405/391 px, the two panels want 161+577, 161+342 and 143+361, and the Staff panel absorbs a 75/177/178 px overdraft through its own scroll exactly as the 2026-09-14 move intended. **The finding: `DismissAlert` cannot be issued at phone width.** `.hud__corner` is `display: none` at 720px and below (deliberate, documented three times in `hud.css` under #1117, exempted by `app-shell.spec.ts`'s `NEVER_LAID_OUT_BELOW_720`), and the alerts list it holds is the only surface that issues that command — which no rollout document says out loud. Asked as an owner question, recorded rather than asserted in `tests/browser/unplaced-surfaces.spec.ts`. **A method warning worth more than the pass:** turning the Staff panel's `overflow-y` to `hidden` left 13 of 14 specs green, because `scrollIntoView` scrolls an `overflow: hidden` box as willingly as an `overflow: auto` one — so any spec that asks "is this reachable" by scrolling to it first inherits the hole. Six stale claims in #1156 and the navigation-move record recorded rather than edited. Names its own weakest claim as the reachability sweep being the wrong question for the brief's second definition of lost — *a control in a section nobody will look in* — which no measurement here addresses and only a playtest would |
| [2026-09-14 the Polish catalogue](./2026-09-14-the-polish-catalogue.md) | [#661](https://github.com/woogitsu/lockstate/issues/661) asks for a `pl` catalogue and reserves the words to the owner — but its approval step cites `AGENTS.md`'s fourth reservation as it stood on 2026-08-30, and that reservation was partly released on 2026-09-04. Does the approval step still bind? How many keys are there really (#661 says 387, a 2026-09-14 measurement said 466)? And which sentences cannot survive a literal rendering into an inflected language? | **None as an ADR; the catalogue itself, merged.** The approval step is **superseded**: it states no independent reason, and the premise it cites was released five days after it was written — *"the CHOICE OF WORDS is ours now; the requirement that a sentence be TRUE is not"* — with `AGENTS.md` restating that as standing on 2026-09-13 and `docs/IDENTITY_V5_ROLLOUT.md` stage 6, the language stage, naming it as the rule governing exactly this work. The owner's own Q1 ruling of 2026-08-30 (*osadzony* in labels, *więzień* in narration) is a **ruling** rather than a reservation and still binds; it is applied through the criterion in [which word for a prisoner](./2026-08-30-which-word-for-a-prisoner.md). **387 and 466 both count the literal object in `default-locale-en.ts` at two dates** (476 today); the module's *export* is 653 because `simulationEnumMessages()` merges 177 derived keys on top, and the catalogue a player actually sees is **669**, because the services layer contributes sixteen more. `pl` translates **669 of 669**, audited clean. **Twenty-six keys are reshaped, all Class A** — same parameters, same facts, no English string and no call site touched, and no grammatical-case machinery added: in every case the fix is to let the parameter land after a colon, a middle dot or an `×`, which are the positions Polish leaves a nominative alone. **The class that would have justified the ADR #661 forbids needs a colon instead**: *Heading to {activity}* wants both a case *and* a preposition that depend on the destination (*do stołówki*, *na plac spacerowy*, *pod prysznic*), so case machinery alone would not have been enough. **No plural forms**, deliberately: a content catalogue cannot hold one, `Localizer.format` would render `other` for every count, and in Polish `other` is the *fraction* category — so every counted message is written to be correct at every count instead, and the counted-message inventory does not move. Twelve open questions answered, two against the 2026-08-30 corpus (*Incydenty* over *Zajścia*; the honest thirteen-character *Pomieszczenia* over a six-character word picked to fit a tab bar). Weakest claim: the tab-bar overlap is **inherited** from the 2026-08-30 measurement and not re-taken, because re-taking it needs the catalogue wired into a page, which is #662 |

### Findings from the first four records that changed a decision

Recorded here because each contradicted something the project believed, and a
reader who only sees the resulting ADR will not know the belief was ever held.

**The first seven bullets were true on 2026-08-25 and the last three on
2026-08-26, and all of them are written in the present tense, which is a trap
this index laid for itself.** Each bullet belongs to the record it came from and
the table above says which; this sentence has to be re-read whenever a record is
added, which is the habit `docs/adr/STATUS-QUEUE.md` asks of its anchor line, for
the same reason and with the same failure available if nobody does. The rule
above — *"a record here does not
become wrong, it becomes older"* — protects the dated files, and it cannot
protect a summary that says "currently" and "today" in the index. So the bullets
keep what was found, because that is the point of the section, and each one that
the code has since overtaken says so inline. Adding a finding here means writing
it the same way.

Re-read on **2026-08-26** when the repository-audit record was added. That record fed no
decision and contributed no bullet, so the ten below still belong to the four records this
heading now names explicitly — the count moved out of the heading rather than being left to
drift, which is the failure the paragraph above describes.

Re-read again on **2026-08-28** when the risk-tier-and-income record was added. It fed no
decision and contributes no bullet either, so the ten below still belong to the same four
records. One bullet was checked against it specifically and survives: *"'Running out of
money' is not a failure mode either"* was written on 2026-08-26 and is still true on
`317f487` — the two `Treasury.spend` callers are both one-off and player-initiated, and
nothing debits on a schedule. That is expected to be overtaken by the recurring payroll
debit in progress on `agent/0042-step3-recurring-debit`; whoever lands it should mark this
bullet overtaken rather than deleting it, the way the two above it are marked.

Re-read again on **2026-08-30** when the record above, *what a classification can
reach*, was added. It fed no decision and contributes no bullet, so the ten below
still belong to the same four records. One bullet was checked against it and is
**overtaken in its first half and re-found in its second**: *"No incident can
fire in any session a player can start"* was written on 2026-08-26 and is false
on `a1d5591` -- the new record measured 58 riots and 17 assaults in one
120-in-game-day prison built only from player commands, and
`tests/integration/incident-trigger-reachability.test.ts` is the shipped gate
for that. What survives is that record's *shape* of argument -- a chain of gates
each of which alone closes the loop -- which the new record found again in a
different system; one of the gates it names,
`ESCAPE_ATTEMPT_MINIMUM_RISK_TIER = 3` against an intake tier of 0 or 1, is why
`escape-attempt` measured **zero** across four of those runs while the same
prison at `priorIncidents: 2` produced thirteen.

- **Authoring one occupancy number per room type is a no-op.** `findAvailable`
  gates on capacity *and* on a `'sleep-surface'` capability, so a capacity
  number alone unblocks nothing. The minimum content change is two fields, not
  one.
- **Four of six comparable games have no room-capacity concept at all.**
  Prison Architect is the exception, and it uses a *different rule per
  designation* rather than one model.
- **"Prisoners would starve in an abstract box" is false.** Accommodation
  actions never check capability, so five of six needs become serviceable
  without any object; only hygiene requires one.
- **No game in the sample designates a nameless area and labels it later** —
  the shape the zoning question implicitly floated is genre-unattested. The
  real split is purpose-first painting versus fully derived rooms.
- **A mis-drag is currently permanent.** No command removes a zone, `Undo`
  reaches only the construction system, and re-zoning is blocked by
  `overlaps-existing-room` — so one stray drag creates unremovable room for the
  session, with no recovery at all on touch, where there is no undo key.
  **Overtaken:** this is the finding the Rooms tab was built to answer.
  `UnzoneRoom` is a command with a producer (#312, and #317 for the touch half),
  so a stray drag is recoverable with the same gesture that made it. The
  `overlaps-existing-room` refusal and the reach of `Undo` are both unchanged;
  what changed is that removal no longer has to go through either.
- **"Pack the prison" is not the reachable failure mode.** Occupancy is
  hard-gated, so overcrowding is unrepresentable; the strategy the economy has
  to price against is sprawl.
- **Construction is effectively instantaneous** — a wall completes in about
  2.5 seconds, with no labour cap and every order in parallel. Money, not
  time, is the only constraint on building today.
  **Overtaken in its second half (#348).** A single wall still finishes on the
  same tick it always did, so "a wall completes in about 2.5 seconds" survives
  unchanged. Orders are no longer parallel: one order holds the crew at a time
  and a waiting order takes it in the canonical ascending-id sequence, so a
  twelve-wall perimeter finishes at tick 730 rather than at 70. Money is
  therefore no longer the only constraint on building, which is the half of
  this finding an economy memo would have leaned on.

**Added 2026-08-26**, from the failure-mode record. The first two extend the
"pack the prison" bullet above rather than replacing it: that bullet is right,
and it stopped one step short of its own consequence.

- **"Running out of money" is not a failure mode either — it is not even
  representable.** `Treasury.spend` refuses rather than overdrawing, the balance
  is validated non-negative in four places, and nothing debits it on a schedule,
  so a running prison's balance is monotonically non-decreasing. The premise that
  money was the *one* failure mode understated the gap: there were none. Measured
  over ten in-game days with one housed prisoner, the balance closes at 27,935
  from an opening 25,000.
- **No incident can fire in any session a player can start, and the missing
  producer is not the reason.** `IncidentTriggerSystem` iterates a watched-sector
  array that only the restore path ever writes, and none of the eleven protocol
  commands creates a sector — but wiring one by hand does not help. Contraband
  pressure has no producer, so the risk score cannot exceed
  `0.5 x needsPressure + 0.3`; `needsPressure` reads the `safety` need alone; and
  `safety` is raised twenty times faster than it decays by the sleep action,
  which requires the very bed `AdmitPrisoner` refuses an arrival without.
  **The hard gate on occupancy and the riot trigger are wired in mutual
  exclusion.** Measured fully wired, with zero guards against a four-guard
  schedule: 0.32 against a 0.6 threshold, and no incident in 25 in-game days.
- **"Until a session/scenario registers them" defers to a caller that has never
  been written.** There is no scenario type, class or module under `src/` at all.
  The same deferral leaves the escape-opportunity resolver, the incident
  summariser, the incident alert projection and the whole tunnel registry with no
  caller in `src/` — the same class as issue #287's two uncalled capabilities,
  three layers deeper.
