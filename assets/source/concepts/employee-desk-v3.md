# Employee desk refinement — 2026-09-24

`employee-desk-multiview-v3.png` is an original generated modelling reference, with matched overhead, front, side and oblique views. The built-in image generator did not disclose a backend model version. This reference is source design material and is not published as the game sprite.

The Blender model keeps the buildable `object.desk` footprint at 2×1 and the existing `furniture.office.desk.employee.variants` ID. It adds a top-visible low blue-grey lamp, a broad open ledger with page rulings, a layered olive paperwork tray, a pencil cup and cable grommet to the existing textured grey-oak desktop and steel drawer pedestal. These are modelled parts under the deterministic overhead render, not elements copied from the concept pixels. The large masses remain distinct when the native 512×256 render is shown at 128×64 world pixels.

No gameplay definition, room requirement, construction cost or asset loading path changes.
