# Guard actor concept, revision 2

`guard-actor-eight-view-v2.png` and `../textures/guard-uniform-navy-v2.png` were generated with the Codex image-generation tool on 2026-09-23. The exact image model version is not exposed. The existing prisoner concept supplied the character scale and render language; the guard design is original.

The Blender scene has a navy cap and visor, shoulder patches, chest badge, utility belt, radio pouch, key ring and cargo pockets. These larger shapes distinguish the guard from the prisoner at the 64 px game scale. The uniform swatch is packed into the source `.blend`; the final idle and walk atlases are exported through the same pinned Blender 5.2 eight-direction pipeline as the other actors. The concept sheet itself is never used as a runtime sprite.
